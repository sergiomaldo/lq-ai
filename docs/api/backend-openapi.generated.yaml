components:
  schemas:
    AdminUserListResponse:
      description: Paginated user list for ``GET /api/v1/admin/users``.
      properties:
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        total_count:
          title: Total Count
          type: integer
        users:
          items:
            $ref: '#/components/schemas/AdminUserRow'
          title: Users
          type: array
      required:
      - users
      - total_count
      - limit
      - offset
      title: AdminUserListResponse
      type: object
    AdminUserRow:
      description: One row in the admin user list (Wave B v2 — PRD §5.2).
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        deletion_scheduled_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Deletion Scheduled At
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
        email:
          title: Email
          type: string
        id:
          format: uuid
          title: Id
          type: string
        is_admin:
          title: Is Admin
          type: boolean
        last_login_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Last Login At
        mfa_enabled:
          title: Mfa Enabled
          type: boolean
        must_change_password:
          title: Must Change Password
          type: boolean
        role:
          title: Role
          type: string
      required:
      - id
      - email
      - display_name
      - role
      - is_admin
      - mfa_enabled
      - must_change_password
      - created_at
      - last_login_at
      - deletion_scheduled_at
      title: AdminUserRow
      type: object
    AliasCreateRequest:
      description: Request body for ``POST /api/v1/admin/aliases``.
      properties:
        fallback:
          anyOf:
          - items:
              $ref: '#/components/schemas/_FallbackEntry'
            type: array
          - type: 'null'
          title: Fallback
        model:
          minLength: 1
          title: Model
          type: string
        name:
          maxLength: 64
          minLength: 1
          title: Name
          type: string
        provider:
          minLength: 1
          title: Provider
          type: string
      required:
      - name
      - provider
      - model
      title: AliasCreateRequest
      type: object
    AliasUpdateRequest:
      description: Request body for ``PATCH /api/v1/admin/aliases/{name}``.
      properties:
        fallback:
          anyOf:
          - items:
              $ref: '#/components/schemas/_FallbackEntry'
            type: array
          - type: 'null'
          title: Fallback
        model:
          minLength: 1
          title: Model
          type: string
        provider:
          minLength: 1
          title: Provider
          type: string
      required:
      - provider
      - model
      title: AliasUpdateRequest
      type: object
    AttachKnowledgeBaseRequest:
      description: '``POST /api/v1/projects/{id}/knowledge-bases`` body.'
      properties:
        knowledge_base_id:
          format: uuid
          title: Knowledge Base Id
          type: string
      required:
      - knowledge_base_id
      title: AttachKnowledgeBaseRequest
      type: object
    AttachSkillRequest:
      description: '``POST /api/v1/projects/{id}/skills`` body.'
      properties:
        skill_name:
          maxLength: 200
          minLength: 1
          title: Skill Name
          type: string
      required:
      - skill_name
      title: AttachSkillRequest
      type: object
    AuditLogEntry:
      description: Wire shape for one audit_log row in the read endpoint response.
      properties:
        action:
          title: Action
          type: string
        details:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Details
        id:
          title: Id
          type: string
        ip_address:
          anyOf:
          - type: string
          - type: 'null'
          title: Ip Address
        privilege_basis:
          anyOf:
          - type: string
          - type: 'null'
          title: Privilege Basis
        privilege_marked:
          title: Privilege Marked
          type: boolean
        request_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Request Id
        resource_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Resource Id
        resource_type:
          title: Resource Type
          type: string
        routed_inference_tier:
          anyOf:
          - type: integer
          - type: 'null'
          title: Routed Inference Tier
        routed_provider:
          anyOf:
          - type: string
          - type: 'null'
          title: Routed Provider
        timestamp:
          format: date-time
          title: Timestamp
          type: string
        user_agent:
          anyOf:
          - type: string
          - type: 'null'
          title: User Agent
        user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: User Id
      required:
      - id
      - timestamp
      - user_id
      - action
      - resource_type
      - resource_id
      - privilege_marked
      - privilege_basis
      - routed_inference_tier
      - routed_provider
      - ip_address
      - user_agent
      - request_id
      - details
      title: AuditLogEntry
      type: object
    AuditLogPage:
      description: Paginated response for ``GET /api/v1/admin/audit-log``.
      properties:
        items:
          items:
            $ref: '#/components/schemas/AuditLogEntry'
          title: Items
          type: array
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - items
      title: AuditLogPage
      type: object
    AutonomousArtifactListResponse:
      description: 'Paginated list of :class:`AutonomousArtifactRead` items.


        Mirrors ``AutonomousFindingListResponse`` — total_count / limit /

        offset envelope. Artifacts are read in the same stable ``created_at

        ASC, id ASC`` order (see that schema''s note) — repeatable, not a

        guaranteed emission sequence; not a newest-first feed.'
      properties:
        artifacts:
          items:
            $ref: '#/components/schemas/AutonomousArtifactRead'
          title: Artifacts
          type: array
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        total_count:
          title: Total Count
          type: integer
      required:
      - artifacts
      - total_count
      - limit
      - offset
      title: AutonomousArtifactListResponse
      type: object
    AutonomousArtifactRead:
      description: 'ORM-read view of an ``autonomous_artifacts`` row (work-product
        ref).


        An artifact is a document-grade deliverable (a markdown memo) an

        opted-in run persisted into its target KB via the ``emit_artifact``

        chokepoint. ``name`` and ``mime`` are LLM-emitted free text (no

        CHECK constraints; the ``severity`` precedent). ``file_id`` is NULL

        if the underlying file was later hard-deleted — the name/size

        metadata survives here. ``document_id`` is NOT an ORM column: it is

        enriched at the endpoint from ``documents.file_id`` (default None),

        so the UI can deep-link the KB document. ``session_id`` is

        deliberately omitted — the endpoint is session-scoped per the Donna

        contract shape, so the caller already has it.'
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        document_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Document Id
        file_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: File Id
        id:
          format: uuid
          title: Id
          type: string
        mime:
          title: Mime
          type: string
        name:
          title: Name
          type: string
        size_bytes:
          title: Size Bytes
          type: integer
      required:
      - id
      - name
      - mime
      - size_bytes
      - created_at
      title: AutonomousArtifactRead
      type: object
    AutonomousFindingListResponse:
      description: 'Paginated list of :class:`AutonomousFindingRead` items.


        Mirrors ``AutonomousMemoryListResponse`` — total_count / limit /

        offset envelope. Findings are read in a stable ``created_at ASC, id

        ASC`` order — one run''s rows typically share ``created_at``

        (transaction-stable ``now()``), so ``id`` is the pagination

        tiebreaker; repeatable, not a guaranteed emission sequence. Not a

        newest-first feed. Uses the ``findings`` key (Donna''s UI expects

        ``findings: [...]``).'
      properties:
        findings:
          items:
            $ref: '#/components/schemas/AutonomousFindingRead'
          title: Findings
          type: array
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        total_count:
          title: Total Count
          type: integer
      required:
      - findings
      - total_count
      - limit
      - offset
      title: AutonomousFindingListResponse
      type: object
    AutonomousFindingRead:
      description: 'ORM-read view of an ``autonomous_findings`` row (work-product).


        A finding is one analysis result emitted by an autonomous run via the

        ``emit_finding`` chokepoint. ``content`` is the finding body — the

        LLM''s ``summary`` for the finding. ``severity`` is LLM-emitted free

        text (no CHECK constraint; ``info`` | ``warn`` | ``critical`` are the

        intended values but anything stores).'
      properties:
        content:
          title: Content
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        id:
          format: uuid
          title: Id
          type: string
        session_id:
          format: uuid
          title: Session Id
          type: string
        severity:
          title: Severity
          type: string
        title:
          title: Title
          type: string
      required:
      - id
      - session_id
      - severity
      - title
      - content
      - created_at
      title: AutonomousFindingRead
      type: object
    AutonomousManualRunRequest:
      description: 'Request body for ``POST /autonomous/run-now`` (Phase 1, §4.4).


        Spawns a single one-off autonomous session (``trigger_kind=''manual''``)

        so a user can test what a skill/playbook does — and inspect the

        resulting receipt — before arming it as a schedule or watch. Exactly

        one of ``playbook_id`` / ``skill_ref`` must be set (the agent runs an

        existing artifact; custom-task authoring is out of scope for Phase 1).

        ``target_kb_id`` and ``project_id`` are optional scope. ``max_cost_usd``

        is the per-run cap (NULL = fall back to

        ``settings.autonomous_default_max_cost_usd`` at spawn time, so R4 always

        trips). ``emit_artifacts`` opts the run in to document-grade artifacts

        (Donna ask #8) — a manual run has no schedule/watch row to inherit the

        flag from, so the request body IS the opt-in source; defaults off,

        matching the schedule/watch column default.'
      properties:
        emit_artifacts:
          default: false
          title: Emit Artifacts
          type: boolean
        max_cost_usd:
          anyOf:
          - type: number
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Max Cost Usd
        playbook_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Playbook Id
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        skill_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Ref
        target_kb_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Target Kb Id
      title: AutonomousManualRunRequest
      type: object
    AutonomousMemoryListResponse:
      description: 'Paginated list of :class:`AutonomousMemoryRead` items (M4-B1).


        Mirrors ``AutonomousSessionListResponse`` — total_count / limit /

        offset envelope for consistent pagination conventions across the API.

        Excludes soft-deleted entries (``deleted_at IS NULL``).'
      properties:
        entries:
          items:
            $ref: '#/components/schemas/AutonomousMemoryRead'
          title: Entries
          type: array
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        total_count:
          title: Total Count
          type: integer
      required:
      - entries
      - total_count
      - limit
      - offset
      title: AutonomousMemoryListResponse
      type: object
    AutonomousMemoryRead:
      description: ORM-read view of an :class:`~app.models.autonomous.AutonomousMemory`.
      properties:
        category:
          title: Category
          type: string
        content:
          title: Content
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        deleted_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Deleted At
        id:
          format: uuid
          title: Id
          type: string
        kept_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Kept At
        source_session_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Source Session Id
        state:
          $ref: '#/components/schemas/MemoryState'
        updated_at:
          format: date-time
          title: Updated At
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - id
      - user_id
      - state
      - category
      - content
      - created_at
      - updated_at
      title: AutonomousMemoryRead
      type: object
    AutonomousNotificationListResponse:
      description: 'Paginated list of :class:`AutonomousNotificationRead` items (M4-C1).


        Mirrors ``AutonomousMemoryListResponse`` — total_count / limit /

        offset envelope. ``read_at IS NULL`` = unread; the ``?unread=true``

        filter on ``GET /autonomous/notifications`` narrows to unread rows.'
      properties:
        limit:
          title: Limit
          type: integer
        notifications:
          items:
            $ref: '#/components/schemas/AutonomousNotificationRead'
          title: Notifications
          type: array
        offset:
          title: Offset
          type: integer
        total_count:
          title: Total Count
          type: integer
      required:
      - notifications
      - total_count
      - limit
      - offset
      title: AutonomousNotificationListResponse
      type: object
    AutonomousNotificationRead:
      description: 'ORM-read view of an :class:`~app.models.autonomous.AutonomousNotification`.


        Written by the ``notify`` chokepoint handler (A3.3). ``read_at`` IS

        NULL = unread. The read/dismiss API that marks this column, plus email

        transport and webhook dispatch, land in M4-C1.'
      properties:
        body:
          title: Body
          type: string
        channel:
          $ref: '#/components/schemas/NotificationChannel'
        created_at:
          format: date-time
          title: Created At
          type: string
        id:
          format: uuid
          title: Id
          type: string
        payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Payload
        read_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Read At
        session_id:
          format: uuid
          title: Session Id
          type: string
        title:
          title: Title
          type: string
        updated_at:
          format: date-time
          title: Updated At
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - id
      - user_id
      - session_id
      - channel
      - title
      - body
      - created_at
      - updated_at
      title: AutonomousNotificationRead
      type: object
    AutonomousScheduleCreate:
      description: 'Request body for ``POST /autonomous/schedules`` (M4-B3).


        ``cron_expr`` is required and validated by

        :func:`app.autonomous.cron.validate_cron_expr` (invalid → 422). The

        target (``playbook_id`` / ``skill_ref`` / ``target_kb_id``) and

        ``project_id`` are optional; ``enabled`` defaults to True.

        ``emit_artifacts`` opts the schedule''s sessions in to document-grade

        artifact emission (default off — zero behavior/cost change unless

        requested). ``max_cost_usd`` is the per-schedule spend cap (NULL =

        fall back to ``settings.autonomous_default_max_cost_usd`` at spawn

        time).'
      properties:
        cron_expr:
          title: Cron Expr
          type: string
        emit_artifacts:
          default: false
          title: Emit Artifacts
          type: boolean
        enabled:
          default: true
          title: Enabled
          type: boolean
        max_cost_usd:
          anyOf:
          - type: number
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Max Cost Usd
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        playbook_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Playbook Id
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        skill_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Ref
        target_kb_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Target Kb Id
      required:
      - cron_expr
      title: AutonomousScheduleCreate
      type: object
    AutonomousScheduleListResponse:
      description: 'Paginated list of :class:`AutonomousScheduleRead` items (M4-B3).


        Mirrors ``AutonomousMemoryListResponse`` — total_count / limit /

        offset envelope. Excludes soft-deleted schedules (``deleted_at IS

        NULL``).'
      properties:
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        schedules:
          items:
            $ref: '#/components/schemas/AutonomousScheduleRead'
          title: Schedules
          type: array
        total_count:
          title: Total Count
          type: integer
      required:
      - schedules
      - total_count
      - limit
      - offset
      title: AutonomousScheduleListResponse
      type: object
    AutonomousScheduleRead:
      description: ORM-read view of an :class:`~app.models.autonomous.AutonomousSchedule`.
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        cron_expr:
          title: Cron Expr
          type: string
        deleted_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Deleted At
        emit_artifacts:
          title: Emit Artifacts
          type: boolean
        enabled:
          title: Enabled
          type: boolean
        id:
          format: uuid
          title: Id
          type: string
        last_run_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Last Run At
        max_cost_usd:
          anyOf:
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Max Cost Usd
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        next_run_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Next Run At
        playbook_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Playbook Id
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        skill_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Ref
        target_kb_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Target Kb Id
        updated_at:
          format: date-time
          title: Updated At
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - id
      - user_id
      - cron_expr
      - enabled
      - emit_artifacts
      - created_at
      - updated_at
      title: AutonomousScheduleRead
      type: object
    AutonomousScheduleUpdate:
      description: 'Request body for ``PATCH /autonomous/schedules/{id}`` (M4-B3).


        All fields optional — a partial update. If ``cron_expr`` is provided

        it is re-validated (invalid → 422) and ``next_run_at`` is recomputed.

        Toggling ``enabled`` is allowed. ``emit_artifacts`` may be toggled

        to opt the schedule''s future sessions in or out of document-grade

        artifact emission. ``max_cost_usd`` may be edited

        (NULL clears the per-schedule cap → fall back to global default).

        The matter (``project_id``) may be reassigned; an explicit ``null``

        clears it (unassign). A non-null ``project_id`` the caller does not

        own is rejected (404, id-probing-safe).'
      properties:
        cron_expr:
          anyOf:
          - type: string
          - type: 'null'
          title: Cron Expr
        emit_artifacts:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Emit Artifacts
        enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
        max_cost_usd:
          anyOf:
          - type: number
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Max Cost Usd
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        playbook_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Playbook Id
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        skill_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Ref
        target_kb_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Target Kb Id
      title: AutonomousScheduleUpdate
      type: object
    AutonomousSessionDetailResponse:
      description: 'Session detail view including the live-reconstructed receipt.


        ``receipt`` is built by :func:`~app.autonomous.receipt.build_receipt`

        on every request — it works for running and completed sessions alike.

        A completed session also has the receipt persisted in ``result``.'
      properties:
        receipt:
          additionalProperties: true
          title: Receipt
          type: object
        session:
          $ref: '#/components/schemas/AutonomousSessionRead'
      required:
      - session
      - receipt
      title: AutonomousSessionDetailResponse
      type: object
    AutonomousSessionListResponse:
      description: 'Paginated list of :class:`AutonomousSessionRead` items.


        Mirrors ``AdminUserListResponse`` (total_count / limit / offset

        envelope) for consistent pagination conventions across the API.'
      properties:
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        sessions:
          items:
            $ref: '#/components/schemas/AutonomousSessionRead'
          title: Sessions
          type: array
        total_count:
          title: Total Count
          type: integer
      required:
      - sessions
      - total_count
      - limit
      - offset
      title: AutonomousSessionListResponse
      type: object
    AutonomousSessionRead:
      description: ORM-read view of an :class:`~app.models.autonomous.AutonomousSession`.
      properties:
        completed_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Completed At
        cost_cap_reached:
          title: Cost Cap Reached
          type: boolean
        cost_total_usd:
          pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          title: Cost Total Usd
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        current_phase:
          $ref: '#/components/schemas/Phase'
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
        halt_state:
          $ref: '#/components/schemas/HaltState'
        id:
          format: uuid
          title: Id
          type: string
        idle_halt_minutes:
          title: Idle Halt Minutes
          type: integer
        last_activity_at:
          format: date-time
          title: Last Activity At
          type: string
        max_cost_usd:
          anyOf:
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Max Cost Usd
        params:
          additionalProperties: true
          default: {}
          title: Params
          type: object
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        result:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Result
        status:
          $ref: '#/components/schemas/SessionStatus'
        trigger_kind:
          $ref: '#/components/schemas/TriggerKind'
        trigger_ref:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Trigger Ref
        updated_at:
          format: date-time
          title: Updated At
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - id
      - user_id
      - trigger_kind
      - current_phase
      - halt_state
      - cost_total_usd
      - cost_cap_reached
      - idle_halt_minutes
      - last_activity_at
      - status
      - created_at
      - updated_at
      title: AutonomousSessionRead
      type: object
    AutonomousWatchCreate:
      description: 'Request body for ``POST /autonomous/watches`` (M4-B4).


        ``knowledge_base_id`` is required — the KB whose document arrivals

        trigger a session. The caller must own that KB (404 otherwise;

        KB-sharing is out of scope). The target (``playbook_id`` /

        ``skill_ref``) and ``project_id`` are optional; ``enabled`` defaults

        to True. The watch is bound to its KB — there is no

        ``knowledge_base_id`` on the update schema (create a new watch for a

        different KB). ``emit_artifacts`` opts the watch''s sessions in to

        document-grade artifact emission (default off — zero behavior/cost

        change unless requested). ``max_cost_usd`` is the per-watch spend

        cap (NULL = fall back to

        ``settings.autonomous_default_max_cost_usd`` at spawn time).'
      properties:
        emit_artifacts:
          default: false
          title: Emit Artifacts
          type: boolean
        enabled:
          default: true
          title: Enabled
          type: boolean
        knowledge_base_id:
          format: uuid
          title: Knowledge Base Id
          type: string
        max_cost_usd:
          anyOf:
          - type: number
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Max Cost Usd
        playbook_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Playbook Id
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        skill_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Ref
      required:
      - knowledge_base_id
      title: AutonomousWatchCreate
      type: object
    AutonomousWatchListResponse:
      description: 'Paginated list of :class:`AutonomousWatchRead` items (M4-B4).


        Mirrors ``AutonomousScheduleListResponse`` — total_count / limit /

        offset envelope. Excludes soft-deleted watches (``deleted_at IS

        NULL``).'
      properties:
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        total_count:
          title: Total Count
          type: integer
        watches:
          items:
            $ref: '#/components/schemas/AutonomousWatchRead'
          title: Watches
          type: array
      required:
      - watches
      - total_count
      - limit
      - offset
      title: AutonomousWatchListResponse
      type: object
    AutonomousWatchRead:
      description: ORM-read view of an :class:`~app.models.autonomous.AutonomousWatch`.
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        deleted_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Deleted At
        emit_artifacts:
          title: Emit Artifacts
          type: boolean
        enabled:
          title: Enabled
          type: boolean
        id:
          format: uuid
          title: Id
          type: string
        knowledge_base_id:
          format: uuid
          title: Knowledge Base Id
          type: string
        max_cost_usd:
          anyOf:
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Max Cost Usd
        playbook_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Playbook Id
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        skill_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Ref
        updated_at:
          format: date-time
          title: Updated At
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - id
      - user_id
      - knowledge_base_id
      - enabled
      - emit_artifacts
      - created_at
      - updated_at
      title: AutonomousWatchRead
      type: object
    AutonomousWatchUpdate:
      description: 'Request body for ``PATCH /autonomous/watches/{id}`` (M4-B4).


        All fields optional — a partial update. Toggling ``enabled`` is

        allowed; ``playbook_id`` / ``skill_ref`` may be retargeted. The

        watch''s ``knowledge_base_id`` is immutable (not present here) — a

        watch is bound to its KB. ``emit_artifacts`` may be toggled to opt

        the watch''s future sessions in or out of document-grade artifact

        emission. ``max_cost_usd`` may be edited (NULL clears

        the per-watch cap → fall back to global default). The matter

        (``project_id``) may be reassigned; an explicit ``null`` clears it

        (unassign). A non-null ``project_id`` the caller does not own is

        rejected (404, id-probing-safe).'
      properties:
        emit_artifacts:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Emit Artifacts
        enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
        max_cost_usd:
          anyOf:
          - type: number
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Max Cost Usd
        playbook_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Playbook Id
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        skill_ref:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Ref
      title: AutonomousWatchUpdate
      type: object
    AvailableSourceResponse:
      description: 'Serialization of one registry entry.


        Exposes only the safe fields defined by P3 (ADR 0016):

        name, type, jurisdiction, coverage, content_kinds, enabled, egress_tier.

        Auth keys and cost fields are NEVER included.'
      properties:
        content_kinds:
          items:
            type: string
          title: Content Kinds
          type: array
        coverage:
          title: Coverage
          type: string
        egress_tier:
          anyOf:
          - type: integer
          - type: 'null'
          title: Egress Tier
        enabled:
          title: Enabled
          type: boolean
        jurisdiction:
          title: Jurisdiction
          type: string
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        type:
          title: Type
          type: string
      required:
      - name
      - type
      - jurisdiction
      - coverage
      - content_kinds
      - enabled
      - egress_tier
      title: AvailableSourceResponse
      type: object
    Body_upload_file_api_v1_files_post:
      properties:
        file:
          contentMediaType: application/octet-stream
          description: The file to upload.
          title: File
          type: string
        project_id:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional UUID of a project to attach this file to. The caller
            must own the project; cross-user or unknown ids return 422 (the upload
            is rejected before bytes touch MinIO).
          title: Project Id
      required:
      - file
      title: Body_upload_file_api_v1_files_post
      type: object
    BootstrapStatus:
      description: Wire shape for ``GET /api/v1/admin/bootstrap-status``.
      properties:
        default_password_active:
          description: True when at least one non-deleted admin user still has must_change_password=True
            — the bootstrap password printed at first start has not been rotated yet.
          title: Default Password Active
          type: boolean
        logs_hint:
          description: Shell command an operator can run to retrieve the bootstrap
            password from the API container's logs.
          title: Logs Hint
          type: string
      required:
      - default_password_active
      - logs_hint
      title: BootstrapStatus
      type: object
    CellResult:
      description: 'One cell in the tabular grid.


        Failed extraction renders as ``confidence=''failed''`` + ``error``

        populated (Decision C-10). Successful extractions carry the

        extracted ``value`` plus the list of citations the Citation

        Engine grounded it against.'
      properties:
        citations:
          items:
            $ref: '#/components/schemas/Citation'
          title: Citations
          type: array
        confidence:
          enum:
          - high
          - medium
          - low
          - failed
          title: Confidence
          type: string
        cost_usd:
          anyOf:
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Cost Usd
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
        tier_used:
          anyOf:
          - type: integer
          - type: 'null'
          title: Tier Used
        value:
          anyOf:
          - type: string
          - type: 'null'
          title: Value
        verification_method:
          anyOf:
          - type: string
          - type: 'null'
          title: Verification Method
      required:
      - confidence
      title: CellResult
      type: object
    ChangePasswordRequest:
      description: '`ChangePasswordRequest` schema from backend-openapi.yaml.


        Both the current (last-known) password and the new password are

        required. The current password gates the change so a stolen access

        token alone cannot rotate credentials — see PRD §5.1.'
      properties:
        current_password:
          maxLength: 1024
          minLength: 1
          title: Current Password
          type: string
        new_password:
          maxLength: 1024
          minLength: 1
          title: New Password
          type: string
      required:
      - current_password
      - new_password
      title: ChangePasswordRequest
      type: object
    ChatCreateRequest:
      additionalProperties: false
      description: '``ChatCreate`` from backend-openapi.yaml.


        All fields are optional. ``title`` defaults to ``"New chat"``

        (DB-side); when omitted, the API auto-renames the chat from the

        first user message.'
      properties:
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        title:
          anyOf:
          - maxLength: 200
            minLength: 1
            type: string
          - type: 'null'
          title: Title
      title: ChatCreateRequest
      type: object
    ChatListResponse:
      additionalProperties: false
      description: Paginated ``GET /api/v1/chats`` response.
      properties:
        items:
          items:
            $ref: '#/components/schemas/ChatResponse'
          title: Items
          type: array
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - items
      title: ChatListResponse
      type: object
    ChatResponse:
      description: '``Chat`` schema from backend-openapi.yaml.


        Built from the ORM row plus the rolled-up ``message_count``

        (counted in the handler with a single COUNT(*) per chat — fine on

        the M1 footprint).'
      properties:
        archived_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Archived At
        created_at:
          format: date-time
          title: Created At
          type: string
        id:
          format: uuid
          title: Id
          type: string
        message_count:
          default: 0
          title: Message Count
          type: integer
        owner_id:
          format: uuid
          title: Owner Id
          type: string
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        sticky_skills:
          items:
            type: string
          title: Sticky Skills
          type: array
        title:
          title: Title
          type: string
        updated_at:
          format: date-time
          title: Updated At
          type: string
      required:
      - id
      - owner_id
      - title
      - created_at
      - updated_at
      title: ChatResponse
      type: object
    ChatSearchHit:
      description: 'One row in the chat-search response.


        Carries the matching chat ID + title for navigation, the per-row

        relevance rank from the FTS engine, and a snippet of the matching

        message body (or the title itself when only the title matched).'
      properties:
        chat_id:
          format: uuid
          title: Chat Id
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        match_source:
          title: Match Source
          type: string
        rank:
          title: Rank
          type: number
        snippet:
          title: Snippet
          type: string
        title:
          title: Title
          type: string
        updated_at:
          format: date-time
          title: Updated At
          type: string
      required:
      - chat_id
      - title
      - snippet
      - match_source
      - rank
      - created_at
      - updated_at
      title: ChatSearchHit
      type: object
    ChatSearchResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/ChatSearchHit'
          title: Items
          type: array
        query:
          title: Query
          type: string
      required:
      - items
      - query
      title: ChatSearchResponse
      type: object
    ChatUpdateRequest:
      additionalProperties: false
      description: '``ChatUpdate`` from backend-openapi.yaml.


        PATCH applies only the fields the caller sets (``exclude_unset``

        pattern at the handler boundary). ``archived`` toggles

        ``archived_at`` on/off; ``title`` updates the chat title.'
      properties:
        archived:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Archived
        title:
          anyOf:
          - maxLength: 200
            minLength: 1
            type: string
          - type: 'null'
          title: Title
      title: ChatUpdateRequest
      type: object
    Citation:
      description: 'A single citation backing a cell value.


        Lightweight projection of the M2 Citation Engine''s

        :class:`app.models.message_citation.MessageCitation` shape — kept

        minimal here because tabular cells reference citations by ID; the

        cell renderer looks up the full citation row via the existing

        Citation Engine surface (no duplicate persistence).'
      properties:
        chunk_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Chunk Id
        citation_id:
          format: uuid
          title: Citation Id
          type: string
        confidence:
          enum:
          - high
          - medium
          - low
          - failed
          title: Confidence
          type: string
        document_id:
          format: uuid
          title: Document Id
          type: string
        source_file_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Source File Id
        source_page:
          anyOf:
          - type: integer
          - type: 'null'
          title: Source Page
        source_text:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Text
        verification_method:
          anyOf:
          - type: string
          - type: 'null'
          title: Verification Method
      required:
      - citation_id
      - document_id
      - confidence
      title: Citation
      type: object
    CitationCluster:
      properties:
        absolute_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Absolute Url
        case_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Case Name
        id:
          anyOf:
          - type: integer
          - type: 'null'
          title: Id
      title: CitationCluster
      type: object
    ClusterMeta:
      properties:
        absolute_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Absolute Url
        case_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Case Name
        cluster_id:
          title: Cluster Id
          type: integer
        court:
          anyOf:
          - type: string
          - type: 'null'
          title: Court
        date_filed:
          anyOf:
          - type: string
          - type: 'null'
          title: Date Filed
      required:
      - cluster_id
      title: ClusterMeta
      type: object
    ClusterView:
      properties:
        cluster:
          $ref: '#/components/schemas/ClusterMeta'
        opinions:
          items:
            $ref: '#/components/schemas/OpinionMeta'
          title: Opinions
          type: array
      required:
      - cluster
      title: ClusterView
      type: object
    ColumnSpec:
      description: 'One column in a tabular execution''s column spec.


        Mirrors :class:`app.skills.schema.ColumnSpec` — kept separate so

        the wire surface can evolve independently of the authoring surface

        (e.g., ad-hoc executions could add fields that don''t make sense

        in SKILL.md frontmatter).'
      properties:
        ensemble_verification:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Ensemble Verification
        minimum_inference_tier:
          anyOf:
          - maximum: 5.0
            minimum: 1.0
            type: integer
          - type: 'null'
          description: Per-column tier floor (1-5).
          title: Minimum Inference Tier
        name:
          title: Name
          type: string
        query:
          title: Query
          type: string
      required:
      - name
      - query
      title: ColumnSpec
      type: object
    CurrentTierResponse:
      description: '``GET /inference/current-tier`` response.


        ``provider`` and ``model`` echo back so the caller can correlate

        the answer to the question (the gateway may normalize names — e.g.,

        resolve an alias before returning the tier).'
      properties:
        explanation:
          default: ''
          title: Explanation
          type: string
        model:
          title: Model
          type: string
        provider:
          title: Provider
          type: string
        routed_inference_tier:
          anyOf:
          - type: integer
          - type: 'null'
          title: Routed Inference Tier
        routed_provider_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Routed Provider Type
      required:
      - provider
      - model
      title: CurrentTierResponse
      type: object
    DeleteScheduledResponse:
      description: Body for POST /users/me/delete (and the idempotent rerun).
      properties:
        grace_period_days:
          title: Grace Period Days
          type: integer
        scheduled_deletion_at:
          format: date-time
          title: Scheduled Deletion At
          type: string
      required:
      - scheduled_deletion_at
      - grace_period_days
      title: DeleteScheduledResponse
      type: object
    EasyPlaybookGeneration:
      description: 'One Easy Playbook generation row.


        Returned by ``POST /api/v1/playbooks/easy`` (at status ``pending``)

        and by ``GET /api/v1/playbooks/easy/{id}`` (the wizard''s Step 2

        polling target).


        ``draft_playbook`` is the assembled :class:`PlaybookCreate` shape

        when status is ``completed`` — the wizard''s Step 3 inline editor

        binds to this. The free-form ``dict`` typing here matches the

        JSONB column; the wizard validates by ``PlaybookCreate.model_validate``

        on render and again at save time (POST /api/v1/playbooks).'
      properties:
        completed_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Completed At
        contract_type:
          title: Contract Type
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        document_ids:
          items:
            format: uuid
            type: string
          title: Document Ids
          type: array
        draft_playbook:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Draft Playbook
        error_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Message
        id:
          format: uuid
          title: Id
          type: string
        started_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Started At
        status:
          enum:
          - pending
          - running
          - completed
          - error
          title: Status
          type: string
        user_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: User Id
      required:
      - id
      - contract_type
      - status
      - created_at
      title: EasyPlaybookGeneration
      type: object
    EasyPlaybookGenerationCreate:
      additionalProperties: false
      description: 'Request shape for ``POST /api/v1/playbooks/easy``.


        ``document_ids`` is the corpus the wizard''s Step 1 upload step

        accumulated; the user-attorney also names the playbook (via the

        optional ``name``) and identifies the contract family

        (``contract_type``) so the extractor + assembly LLM calls have

        family-appropriate context.


        Per M3-A6 §3.3 the uploaded documents are persisted to the user''s

        library by default. ``persist_documents_after_generation=False``

        is reserved for a future "ephemeral upload" mode and is not

        consumed by Phase 5 — the field is accepted to lock the wire

        shape now and avoid a body-shape migration when the mode lands.'
      properties:
        contract_type:
          minLength: 1
          title: Contract Type
          type: string
        document_ids:
          items:
            format: uuid
            type: string
          maxItems: 50
          minItems: 1
          title: Document Ids
          type: array
        name:
          anyOf:
          - type: string
          - type: 'null'
          description: Playbook name the wizard's Step 1 collected. Falls back to
            an auto-generated 'Generated {contract_type} Playbook' if omitted.
          title: Name
        persist_documents_after_generation:
          default: true
          description: Reserved for a future 'ephemeral upload' mode. Currently always
            true — uploaded documents persist to the user's library.
          title: Persist Documents After Generation
          type: boolean
      required:
      - document_ids
      - contract_type
      title: EasyPlaybookGenerationCreate
      type: object
    EnhancePromptAttachedFile:
      description: Minimal shape for an attached file.
      properties:
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        file_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: File Id
        filename:
          title: Filename
          type: string
        mime_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Mime Type
      required:
      - filename
      title: EnhancePromptAttachedFile
      type: object
    EnhancePromptAttachedSkill:
      description: 'Minimal shape the application supplies for an attached skill.


        The skill itself reads ``name`` and ``description`` — that''s enough

        for it to decide whether the expansion should defer to the

        attached skill or complement it.'
      properties:
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        name:
          title: Name
          type: string
      required:
      - name
      title: EnhancePromptAttachedSkill
      type: object
    EnhancePromptOutcomeUpdate:
      description: 'PATCH body for ``/api/v1/enhance-prompt/{id}``.


        The frontend sends this after the user acts on the preview:

        Submit → ``used=true``; Submit-after-edit → ``used=true`` +

        ``edited_before_use=true``; Skip → ``used=false`` (default —

        nothing to PATCH unless the caller wants to explicitly reset).'
      properties:
        edited_before_use:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Edited Before Use
        used:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Used
      title: EnhancePromptOutcomeUpdate
      type: object
    EnhancePromptRequest:
      description: POST body for ``/api/v1/enhance-prompt``.
      properties:
        attached_files:
          items:
            $ref: '#/components/schemas/EnhancePromptAttachedFile'
          title: Attached Files
          type: array
        attached_skills:
          items:
            $ref: '#/components/schemas/EnhancePromptAttachedSkill'
          title: Attached Skills
          type: array
        chat_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Chat Id
        jurisdiction:
          anyOf:
          - maxLength: 200
            type: string
          - type: 'null'
          title: Jurisdiction
        model:
          anyOf:
          - maxLength: 200
            type: string
          - type: 'null'
          title: Model
        raw_input:
          maxLength: 4096
          minLength: 1
          title: Raw Input
          type: string
      required:
      - raw_input
      title: EnhancePromptRequest
      type: object
    EnhancePromptResponse:
      description: 'Response body for ``/api/v1/enhance-prompt``.


        Mirrors the SKILL.md output schema plus the persisted

        ``interaction_id`` the frontend uses to PATCH ``used`` /

        ``edited_before_use`` after the user acts on the preview.'
      properties:
        expanded_prompt:
          title: Expanded Prompt
          type: string
        expansion_applied:
          title: Expansion Applied
          type: boolean
        interaction_id:
          format: uuid
          title: Interaction Id
          type: string
        preview_to_user:
          default: ''
          title: Preview To User
          type: string
        reasoning:
          items:
            type: string
          title: Reasoning
          type: array
        routed_inference_tier:
          anyOf:
          - type: integer
          - type: 'null'
          title: Routed Inference Tier
        routed_model:
          anyOf:
          - type: string
          - type: 'null'
          title: Routed Model
        routed_provider:
          anyOf:
          - type: string
          - type: 'null'
          title: Routed Provider
        skip_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Skip Reason
      required:
      - interaction_id
      - expansion_applied
      - expanded_prompt
      title: EnhancePromptResponse
      type: object
    ExportJobResponse:
      description: 'Shared shape for POST /users/me/export and GET /users/me/export/{id}.


        The ``download_url`` is populated only when ``status == ''completed''``

        and the bundle has not yet expired.'
      properties:
        download_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Download Url
        job_id:
          title: Job Id
          type: string
        status:
          title: Status
          type: string
      required:
      - job_id
      - status
      title: ExportJobResponse
      type: object
    FallbackTier:
      additionalProperties: false
      description: "One acceptable alternative to a position's standard language.\n\
        \nThe PRD's ``Position`` schema references ``List[FallbackTier]`` but\ndoes\
        \ not define ``FallbackTier``. We pick a minimal, ranked shape:\n\n* ``rank``\
        \ — 1-based order of preference (1 = preferred fallback,\n  2 = next, etc.).\n\
        * ``description`` — what makes this tier acceptable\n  (e.g., \"12-month liability\
        \ cap\" vs. the standard \"uncapped\").\n* ``language`` — the actual clause\
        \ text the playbook will accept.\n\nStored as JSONB on ``playbook_positions.fallback_tiers``\
        \ so\nadditional fields (e.g., approval workflow, escalation contact)\ncan\
        \ land in a future iteration without a schema migration."
      properties:
        description:
          description: Why this tier is acceptable.
          title: Description
          type: string
        language:
          description: The alternative clause text.
          title: Language
          type: string
        rank:
          description: 1-based order of preference.
          minimum: 1.0
          title: Rank
          type: integer
      required:
      - rank
      - description
      - language
      title: FallbackTier
      type: object
    FileMetadata:
      description: 'Wire shape for the ``File`` schema in backend-openapi.yaml.


        Mirrors the YAML''s ``File`` definition. ``page_count`` and

        ``character_count`` are populated by the document pipeline (Task C5)

        once the file moves to ``ingestion_status=''ready''``; until then they

        are NULL and serialize as JSON ``null``.'
      properties:
        character_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Character Count
        created_at:
          format: date-time
          title: Created At
          type: string
        document_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Document Id
        filename:
          title: Filename
          type: string
        hash_sha256:
          description: SHA-256 of the file bytes (hex, lowercase). Computed during
            the streaming upload; stable for the lifetime of the row.
          title: Hash Sha256
          type: string
        id:
          format: uuid
          title: Id
          type: string
        ingestion_status:
          enum:
          - pending
          - processing
          - ready
          - failed
          title: Ingestion Status
          type: string
        mime_type:
          title: Mime Type
          type: string
        owner_id:
          format: uuid
          title: Owner Id
          type: string
        page_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Page Count
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        size_bytes:
          title: Size Bytes
          type: integer
      required:
      - id
      - owner_id
      - filename
      - mime_type
      - size_bytes
      - hash_sha256
      - ingestion_status
      - created_at
      title: FileMetadata
      type: object
    FindInCaseRequest:
      additionalProperties: false
      properties:
        max_matches:
          default: 3
          maximum: 10.0
          minimum: 1.0
          title: Max Matches
          type: integer
        opinion_id:
          title: Opinion Id
          type: integer
        query:
          minLength: 1
          title: Query
          type: string
      required:
      - opinion_id
      - query
      title: FindInCaseRequest
      type: object
    FindInCaseResponse:
      properties:
        matches:
          items:
            $ref: '#/components/schemas/FindMatch'
          title: Matches
          type: array
        opinion_id:
          title: Opinion Id
          type: integer
      required:
      - opinion_id
      title: FindInCaseResponse
      type: object
    FindMatch:
      properties:
        position:
          title: Position
          type: integer
        snippet:
          title: Snippet
          type: string
      required:
      - position
      - snippet
      title: FindMatch
      type: object
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          title: Detail
          type: array
      title: HTTPValidationError
      type: object
    HaltState:
      description: 'The brake state of a session — orthogonal to ``status``.


        Matches the CHECK constraint on ``autonomous_sessions.halt_state``.'
      enum:
      - running
      - halt_requested
      - halted
      - paused
      title: HaltState
      type: string
    IngestHealthResponse:
      description: 'Aggregate ingest-status counts across the deployment.


        The four document-level buckets sum to roughly the document count

        (rows in ``documents``); ``parse_failed`` reports file-level

        failures (rows in ``files`` with ``ingestion_status=''failed''``),

        which never reach the ``documents`` table.


        Operators use this surface as the canonical "is my ingest healthy?"

        signal — a non-zero ``embed_failed`` or ``partial`` count is the

        silent-degrade signal that triggered DE-276; a non-zero

        ``parse_failed`` count is the operator''s cue to inspect specific

        files via the existing file-list UI.'
      properties:
        embed_failed:
          description: Documents whose embed worker raised before any chunk was embedded
            — hybrid retrieval degrades to FTS-only for these.
          title: Embed Failed
          type: integer
        ok:
          description: Documents with all chunks successfully embedded.
          title: Ok
          type: integer
        parse_failed:
          description: Files that failed at parse time and never produced a documents
            row (``files.ingestion_status='failed'``).
          title: Parse Failed
          type: integer
        partial:
          description: Documents whose embed worker raised mid-batch — some chunks
            have vectors, others are NULL. Operators may re-run ingest to recover.
          title: Partial
          type: integer
        total_documents:
          description: Total rows in the documents table (sum of ok + embed_failed
            + partial).
          title: Total Documents
          type: integer
      required:
      - ok
      - embed_failed
      - partial
      - parse_failed
      - total_documents
      title: IngestHealthResponse
      type: object
    IntakeBridgesList:
      description: Section-split response for the admin intake-bridges list.
      properties:
        slack_workspaces:
          items:
            $ref: '#/components/schemas/SlackWorkspaceSummary'
          title: Slack Workspaces
          type: array
        teams_tenants:
          items:
            $ref: '#/components/schemas/TeamsTenantSummary'
          title: Teams Tenants
          type: array
      required:
      - slack_workspaces
      - teams_tenants
      title: IntakeBridgesList
      type: object
    KBFileResponse:
      description: 'Wire shape for one row of ``GET /knowledge-bases/{id}/files``
        (Wave C).


        Mirrors the ``File`` schema in ``backend-openapi.yaml`` (the same

        shape ``POST /files`` returns) so the Knowledge surface can render

        per-doc ingestion status without a follow-up fetch per row. The

        underlying join carries ``attached_at`` — surfaced as ``attached_at``

        so the UI can sort by attachment time independently of file creation

        time (a user may attach an old file to a new KB).'
      properties:
        attached_at:
          format: date-time
          title: Attached At
          type: string
        character_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Character Count
        created_at:
          format: date-time
          title: Created At
          type: string
        document_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Document Id
        filename:
          title: Filename
          type: string
        hash_sha256:
          title: Hash Sha256
          type: string
        id:
          format: uuid
          title: Id
          type: string
        ingest_failure_reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Ingest Failure Reason
        ingest_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Ingest Status
        ingestion_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Ingestion Error
        ingestion_status:
          title: Ingestion Status
          type: string
        mime_type:
          title: Mime Type
          type: string
        owner_id:
          format: uuid
          title: Owner Id
          type: string
        page_count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Page Count
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        size_bytes:
          title: Size Bytes
          type: integer
      required:
      - id
      - owner_id
      - filename
      - mime_type
      - size_bytes
      - hash_sha256
      - ingestion_status
      - created_at
      - attached_at
      title: KBFileResponse
      type: object
    KBQueryRequest:
      additionalProperties: false
      description: '``POST /api/v1/knowledge-bases/{id}/query`` body.


        ``hybrid_alpha`` overrides the KB''s stored alpha for this query

        only. Out-of-bound values are caught by Pydantic''s ``ge`` / ``le``.


        ``chat_id`` is set by the chat surface so the handler can write a

        ``inference.kb_chunks_retrieved`` audit row scoped to the chat —

        that row is what Receipts (Wave D.1 T5/T6) renders as a

        "📎 KB retrieval" event (T7). Omit for standalone retrieval

        (no audit row is written).'
      properties:
        chat_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Chat Id
        hybrid_alpha:
          anyOf:
          - maximum: 1.0
            minimum: 0.0
            type: number
          - type: 'null'
          title: Hybrid Alpha
        query:
          maxLength: 10000
          minLength: 1
          title: Query
          type: string
        top_k:
          default: 10
          maximum: 50.0
          minimum: 1.0
          title: Top K
          type: integer
      required:
      - query
      title: KBQueryRequest
      type: object
    KBQueryResponse:
      additionalProperties: false
      description: '``POST /api/v1/knowledge-bases/{id}/query`` response envelope.


        The brief asks for ranked chunks with scores; we wrap them in an

        object with the resolved ``hybrid_alpha`` so consumers can verify

        which weighting produced the ranking.'
      properties:
        hybrid_alpha:
          title: Hybrid Alpha
          type: number
        results:
          items:
            $ref: '#/components/schemas/SearchResult'
          title: Results
          type: array
      required:
      - results
      - hybrid_alpha
      title: KBQueryResponse
      type: object
    KnowledgeBaseCreateRequest:
      additionalProperties: false
      description: '``KnowledgeBaseCreate`` from backend-openapi.yaml.'
      properties:
        description:
          anyOf:
          - maxLength: 2000
            type: string
          - type: 'null'
          title: Description
        hybrid_alpha:
          default: 0.5
          maximum: 1.0
          minimum: 0.0
          title: Hybrid Alpha
          type: number
        name:
          maxLength: 200
          minLength: 1
          title: Name
          type: string
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
      required:
      - name
      title: KnowledgeBaseCreateRequest
      type: object
    KnowledgeBaseResponse:
      description: '``KnowledgeBase`` schema from backend-openapi.yaml.


        ``file_count`` and ``chunk_count`` are computed at fetch-time by

        the handler; they don''t live on the row.'
      properties:
        archived_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Archived At
        chunk_count:
          default: 0
          title: Chunk Count
          type: integer
        created_at:
          format: date-time
          title: Created At
          type: string
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        file_count:
          default: 0
          title: File Count
          type: integer
        hybrid_alpha:
          title: Hybrid Alpha
          type: number
        id:
          format: uuid
          title: Id
          type: string
        name:
          title: Name
          type: string
        owner_id:
          format: uuid
          title: Owner Id
          type: string
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        updated_at:
          format: date-time
          title: Updated At
          type: string
      required:
      - id
      - owner_id
      - name
      - hybrid_alpha
      - created_at
      - updated_at
      title: KnowledgeBaseResponse
      type: object
    KnowledgeBaseUpdateRequest:
      additionalProperties: false
      description: '``KnowledgeBaseUpdate`` from backend-openapi.yaml.


        All fields optional. PATCH applies only the fields the caller sets;

        the handler uses ``model_dump(exclude_unset=True)`` to distinguish

        "absent" from "explicit null."'
      properties:
        archived:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Archived
        description:
          anyOf:
          - maxLength: 2000
            type: string
          - type: 'null'
          title: Description
        hybrid_alpha:
          anyOf:
          - maximum: 1.0
            minimum: 0.0
            type: number
          - type: 'null'
          title: Hybrid Alpha
        name:
          anyOf:
          - maxLength: 200
            minLength: 1
            type: string
          - type: 'null'
          title: Name
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
      title: KnowledgeBaseUpdateRequest
      type: object
    LoginRequest:
      description: '`LoginRequest` schema from backend-openapi.yaml.'
      properties:
        email:
          format: email
          title: Email
          type: string
        password:
          maxLength: 1024
          minLength: 1
          title: Password
          type: string
      required:
      - email
      - password
      title: LoginRequest
      type: object
    LoginResponse:
      description: '`LoginResponse` schema from backend-openapi.yaml.'
      properties:
        access_token:
          title: Access Token
          type: string
        expires_in:
          title: Expires In
          type: integer
        refresh_token:
          title: Refresh Token
          type: string
        token_type:
          default: Bearer
          title: Token Type
          type: string
        user:
          $ref: '#/components/schemas/UserPublic'
      required:
      - access_token
      - expires_in
      - refresh_token
      - user
      title: LoginResponse
      type: object
    MCPOAuthCallbackResponse:
      description: Response body for a successful OAuth callback.
      properties:
        connected:
          title: Connected
          type: boolean
        expires_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Expires At
        scopes:
          items:
            type: string
          title: Scopes
          type: array
        server:
          title: Server
          type: string
      required:
      - connected
      - server
      - scopes
      title: MCPOAuthCallbackResponse
      type: object
    MCPOAuthServerStatus:
      description: 'Per-server connection status in the per-user list endpoint (PR4d
        Ask 1).


        Token bytes are NEVER included — only the non-secret status fields.'
      properties:
        connected:
          title: Connected
          type: boolean
        expires_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Expires At
        scopes:
          items:
            type: string
          title: Scopes
          type: array
        server:
          title: Server
          type: string
      required:
      - server
      - connected
      - scopes
      title: MCPOAuthServerStatus
      type: object
    MCPOAuthServersResponse:
      description: Response body for GET /api/v1/mcp/oauth — list of all connectable
        OAuth servers.
      properties:
        servers:
          items:
            $ref: '#/components/schemas/MCPOAuthServerStatus'
          title: Servers
          type: array
      required:
      - servers
      title: MCPOAuthServersResponse
      type: object
    MCPOAuthStatusResponse:
      description: Response body for the connection status endpoint.
      properties:
        connected:
          title: Connected
          type: boolean
        expires_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Expires At
        scopes:
          items:
            type: string
          title: Scopes
          type: array
      required:
      - connected
      - scopes
      title: MCPOAuthStatusResponse
      type: object
    MCPRefreshResponse:
      properties:
        server:
          title: Server
          type: string
        tools:
          items:
            $ref: '#/components/schemas/MCPToolView'
          title: Tools
          type: array
      required:
      - server
      title: MCPRefreshResponse
      type: object
    MCPServerView:
      properties:
        auth:
          enum:
          - none
          - bearer
          - oauth
          title: Auth
          type: string
        name:
          title: Name
          type: string
        tools:
          items:
            $ref: '#/components/schemas/MCPToolView'
          title: Tools
          type: array
        type:
          title: Type
          type: string
      required:
      - name
      - type
      - auth
      title: MCPServerView
      type: object
    MCPServersResponse:
      properties:
        servers:
          items:
            $ref: '#/components/schemas/MCPServerView'
          title: Servers
          type: array
      title: MCPServersResponse
      type: object
    MCPToolEnableRequest:
      additionalProperties: false
      properties:
        enabled:
          title: Enabled
          type: boolean
      required:
      - enabled
      title: MCPToolEnableRequest
      type: object
    MCPToolView:
      properties:
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        destructive:
          title: Destructive
          type: boolean
        enabled:
          title: Enabled
          type: boolean
        name:
          title: Name
          type: string
        parameters:
          additionalProperties: true
          title: Parameters
          type: object
        read_only:
          title: Read Only
          type: boolean
        requires_confirmation:
          title: Requires Confirmation
          type: boolean
      required:
      - name
      - read_only
      - destructive
      - requires_confirmation
      - enabled
      title: MCPToolView
      type: object
    MemoryKeepRequest:
      description: 'Optional request body for ``POST /autonomous/memory/{id}/keep``.


        If ``content`` is provided, the memory entry''s text is overwritten on

        keep (edit-on-keep).  Omitting the body entirely (or sending ``{}``

        or ``{"content": null}``) leaves the existing content unchanged.'
      properties:
        content:
          anyOf:
          - type: string
          - type: 'null'
          title: Content
      title: MemoryKeepRequest
      type: object
    MemoryState:
      description: 'Review state of an autonomous-memory note.


        Matches the CHECK constraint on ``autonomous_memory.state``.'
      enum:
      - proposed
      - kept
      - dismissed
      title: MemoryState
      type: string
    MessageListResponse:
      additionalProperties: false
      description: Paginated ``GET /api/v1/chats/{id}/messages`` response.
      properties:
        items:
          items:
            $ref: '#/components/schemas/MessageResponse'
          title: Items
          type: array
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
      required:
      - items
      title: MessageListResponse
      type: object
    MessageResponse:
      additionalProperties: false
      description: '``Message`` schema from backend-openapi.yaml.


        Cost is presented as a USD float for client friendliness; the DB

        stores integer micros and :func:`micros_to_usd` translates. The

        handler is expected to convert via :func:`message_to_response` —

        we do not use ``from_attributes=True`` here because the column

        name on the ORM row (``cost_estimate_micros``) differs from the

        wire field (``cost_estimate``).'
      properties:
        applied_skills:
          items:
            type: string
          title: Applied Skills
          type: array
        chat_id:
          format: uuid
          title: Chat Id
          type: string
        citations:
          items:
            additionalProperties: true
            type: object
          title: Citations
          type: array
        completion_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Completion Tokens
        content:
          title: Content
          type: string
        cost_estimate:
          anyOf:
          - type: number
          - type: 'null'
          title: Cost Estimate
        created_at:
          format: date-time
          title: Created At
          type: string
        error_code:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Code
        id:
          format: uuid
          title: Id
          type: string
        is_enhanced:
          default: false
          title: Is Enhanced
          type: boolean
        kind:
          default: user
          enum:
          - user
          - ai
          - refusal
          - system
          title: Kind
          type: string
        prompt_tokens:
          anyOf:
          - type: integer
          - type: 'null'
          title: Prompt Tokens
        requested_model:
          anyOf:
          - type: string
          - type: 'null'
          title: Requested Model
        role:
          enum:
          - user
          - assistant
          - system
          - tool
          title: Role
          type: string
        routed_inference_tier:
          anyOf:
          - type: integer
          - type: 'null'
          title: Routed Inference Tier
        routed_model:
          anyOf:
          - type: string
          - type: 'null'
          title: Routed Model
        routed_provider:
          anyOf:
          - type: string
          - type: 'null'
          title: Routed Provider
      required:
      - id
      - chat_id
      - role
      - content
      - created_at
      title: MessageResponse
      type: object
    MfaChallenge:
      description: '`MfaChallenge` schema from backend-openapi.yaml.'
      properties:
        methods:
          items:
            type: string
          title: Methods
          type: array
        mfa_token:
          title: Mfa Token
          type: string
      required:
      - mfa_token
      - methods
      title: MfaChallenge
      type: object
    MfaDisableRequest:
      description: '`/auth/mfa/disable` body — re-proves password + a current MFA
        code.


        Disabling MFA is account-shape-changing, so we require *both* the

        password (defends against a stolen access token) and a current

        TOTP/recovery code (defends against an attacker who knows the

        password but doesn''t have the device).'
      properties:
        code:
          maxLength: 64
          minLength: 1
          title: Code
          type: string
        password:
          maxLength: 1024
          minLength: 1
          title: Password
          type: string
      required:
      - password
      - code
      title: MfaDisableRequest
      type: object
    MfaEnableRequest:
      description: '`/auth/mfa/enable` body — confirms enrollment with a fresh TOTP
        code.'
      properties:
        code:
          pattern: ^\d{6}$
          title: Code
          type: string
      required:
      - code
      title: MfaEnableRequest
      type: object
    MfaSetupResponse:
      description: '`/auth/mfa/setup` response shape — see backend-openapi.yaml.


        ``secret`` and ``provisioning_uri`` carry the same TOTP secret in

        two encodings. ``recovery_codes`` is the **plaintext** list (only

        time the user sees them) — the backend persists bcrypt hashes.'
      properties:
        provisioning_uri:
          title: Provisioning Uri
          type: string
        recovery_codes:
          items:
            type: string
          title: Recovery Codes
          type: array
        secret:
          title: Secret
          type: string
      required:
      - secret
      - provisioning_uri
      - recovery_codes
      title: MfaSetupResponse
      type: object
    MfaVerifyRequest:
      description: '`/auth/mfa/verify` body — completes a 423 login challenge.


        The ``code`` is either a 6-digit TOTP or a 14-character recovery

        code (``xxxx-xxxx-xxxx``). The handler tries TOTP first, then

        recovery; submitted format is not validated up-front so an attacker

        cannot tell from the response shape which path matched.'
      properties:
        code:
          maxLength: 64
          minLength: 1
          title: Code
          type: string
        mfa_token:
          minLength: 1
          title: Mfa Token
          type: string
      required:
      - mfa_token
      - code
      title: MfaVerifyRequest
      type: object
    NotificationChannel:
      description: 'Delivery channel for an autonomous notification.


        Matches the CHECK constraint on ``autonomous_notifications.channel``

        (migration ``0040_autonomous_notifications.py``). ``webhook`` is

        RESERVED — present in the enum so M4-C1''s fold-in is purely additive,

        but not dispatched until DE-312 (Decision M4-8).'
      enum:
      - in_app
      - email
      - webhook
      title: NotificationChannel
      type: string
    OpinionMeta:
      properties:
        char_length:
          title: Char Length
          type: integer
        opinion_id:
          title: Opinion Id
          type: integer
        text_field_used:
          anyOf:
          - enum:
            - html_with_citations
            - html_columbia
            - html_lawbox
            - xml_harvard
            - html_anon_2020
            - html
            - plain_text
            type: string
          - type: 'null'
          title: Text Field Used
      required:
      - opinion_id
      - char_length
      title: OpinionMeta
      type: object
    OpinionText:
      properties:
        cluster_id:
          title: Cluster Id
          type: integer
        opinion_id:
          title: Opinion Id
          type: integer
        text:
          title: Text
          type: string
        text_field_used:
          anyOf:
          - enum:
            - html_with_citations
            - html_columbia
            - html_lawbox
            - xml_harvard
            - html_anon_2020
            - html
            - plain_text
            type: string
          - type: 'null'
          title: Text Field Used
      required:
      - opinion_id
      - cluster_id
      - text
      title: OpinionText
      type: object
    OrganizationProfileResponse:
      description: GET response shape; PUT also returns this body on success.
      properties:
        content_md:
          title: Content Md
          type: string
        updated_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Updated At
        updated_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Updated By
      required:
      - content_md
      title: OrganizationProfileResponse
      type: object
    OrganizationProfileUpdateRequest:
      description: 'PUT body. ``content_md`` is the full Markdown body (skill body).


        Empty string is allowed — operators may want to clear the Profile

        without deleting the row. The PUT is an upsert: insert if no row

        exists yet, update in place otherwise (preserving the row id so

        audit history correlates).'
      properties:
        content_md:
          maxLength: 200000
          minLength: 0
          title: Content Md
          type: string
      required:
      - content_md
      title: OrganizationProfileUpdateRequest
      type: object
    OverrideRequest:
      description: '``POST /inference/override-tier-floor`` request body.


        ``reason`` is mandatory and persisted to ``audit_log.details.reason``

        — the operator''s audit posture relies on the override carrying a

        human-readable justification.'
      properties:
        message_id:
          format: uuid
          title: Message Id
          type: string
        reason:
          maxLength: 500
          minLength: 10
          title: Reason
          type: string
      required:
      - message_id
      - reason
      title: OverrideRequest
      type: object
    OverrideResponse:
      description: '``POST /inference/override-tier-floor`` response.'
      properties:
        ai_message:
          $ref: '#/components/schemas/MessageResponse'
        routing_log_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Routing Log Id
      required:
      - ai_message
      title: OverrideResponse
      type: object
    Phase:
      description: 'The agent''s phase machine — sessions advance through these in
        order.


        Matches the CHECK constraint on ``autonomous_sessions.current_phase``.'
      enum:
      - intake
      - analysis
      - drafting
      - ethics_review
      - delivery
      title: Phase
      type: string
    Playbook:
      description: 'Full playbook — header plus ordered positions.


        Mirrors the ``Playbook`` shape in [PRD §3.7](docs/PRD.md#37-playbooks).

        ``contract_type`` is free-form text per PRD; the canonical values

        used by the built-in playbooks are ``"NDA"``, ``"NDA-unilateral"``,

        ``"MSA-SaaS"``, ``"MSA-Commercial"``, ``"DPA"``, but operators may

        define their own.'
      properties:
        contract_type:
          title: Contract Type
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Created By
        description:
          default: ''
          title: Description
          type: string
        id:
          format: uuid
          title: Id
          type: string
        name:
          title: Name
          type: string
        positions:
          items:
            $ref: '#/components/schemas/Position'
          title: Positions
          type: array
        updated_at:
          format: date-time
          title: Updated At
          type: string
        version:
          default: 1.0.0
          title: Version
          type: string
      required:
      - id
      - name
      - contract_type
      - created_at
      - updated_at
      title: Playbook
      type: object
    PlaybookCreate:
      additionalProperties: false
      description: Request shape for ``POST /api/v1/playbooks``.
      properties:
        contract_type:
          title: Contract Type
          type: string
        description:
          default: ''
          title: Description
          type: string
        name:
          title: Name
          type: string
        positions:
          items:
            $ref: '#/components/schemas/PositionCreate'
          title: Positions
          type: array
        version:
          default: 1.0.0
          title: Version
          type: string
      required:
      - name
      - contract_type
      title: PlaybookCreate
      type: object
    PlaybookExecution:
      description: 'One execution of a playbook against a target document.


        ``results`` is a free-form JSONB payload populated by the M3-A2

        executor; this schema doesn''t pin its shape because the executor

        surface is still being defined. The wire-level guarantee at this

        layer is just "if status=''completed'', results is present; if

        status=''error'', error text is populated."'
      properties:
        completed_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Completed At
        created_at:
          format: date-time
          title: Created At
          type: string
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
        id:
          format: uuid
          title: Id
          type: string
        playbook_id:
          format: uuid
          title: Playbook Id
          type: string
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        results:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Results
        status:
          enum:
          - pending
          - running
          - completed
          - error
          title: Status
          type: string
        target_document_id:
          format: uuid
          title: Target Document Id
          type: string
        user_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: User Id
      required:
      - id
      - playbook_id
      - target_document_id
      - status
      - created_at
      title: PlaybookExecution
      type: object
    PlaybookExecutionCreate:
      additionalProperties: false
      description: 'Request shape for ``POST /api/v1/playbooks/{id}/execute``.


        ``project_id`` is optional — the executor uses it for RBAC scoping

        and to inherit the project''s ``minimum_inference_tier`` /

        ``ensemble_verification`` flags. A null ``project_id`` runs the

        playbook against the document in the caller''s personal scope.'
      properties:
        project_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
        target_document_id:
          format: uuid
          title: Target Document Id
          type: string
      required:
      - target_document_id
      title: PlaybookExecutionCreate
      type: object
    PlaybookUpdate:
      additionalProperties: false
      description: 'Request shape for ``PATCH /api/v1/playbooks/{id}`` — M3-A6 Phase
        2.


        Every field is optional; the server treats a missing field as

        "leave alone." The exception is ``positions``: when supplied, the

        server **replaces the entire positions list atomically** (drops the

        old rows, inserts the new ones in a single transaction). Per-position

        diff/patch is not supported — the wizard''s "edit then save" flow

        always sends the full set, and the alternative (PATCH per position)

        would introduce id-vs-order-vs-content ambiguities for marginal

        value. To leave positions alone, omit the field.'
      properties:
        contract_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Contract Type
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        positions:
          anyOf:
          - items:
              $ref: '#/components/schemas/PositionCreate'
            type: array
          - type: 'null'
          title: Positions
        version:
          anyOf:
          - type: string
          - type: 'null'
          title: Version
      title: PlaybookUpdate
      type: object
    Position:
      description: 'One issue in a playbook — org''s standard + fallbacks for a clause.


        Mirrors the ``Position`` shape in [PRD §3.7](docs/PRD.md#37-playbooks)

        with the ``id`` and ``position_order`` fields the storage layer

        needs. ``detection_keywords`` and ``detection_examples`` feed the

        M3-A2 executor''s retrieval step (lexical + embedding match).'
      properties:
        description:
          default: ''
          title: Description
          type: string
        detection_examples:
          description: Example clauses for the executor's embedding match step.
          items:
            type: string
          title: Detection Examples
          type: array
        detection_keywords:
          description: Keywords for the executor's lexical match step.
          items:
            type: string
          title: Detection Keywords
          type: array
        fallback_tiers:
          description: Ranked acceptable alternatives.
          items:
            $ref: '#/components/schemas/FallbackTier'
          title: Fallback Tiers
          type: array
        id:
          format: uuid
          title: Id
          type: string
        issue:
          description: e.g. "Limitation of Liability".
          title: Issue
          type: string
        position_order:
          default: 0
          description: Ascending walk order. Stable per playbook.
          minimum: 0.0
          title: Position Order
          type: integer
        redline_strategy:
          default: ''
          description: How to redline if the contract deviates.
          title: Redline Strategy
          type: string
        severity_if_missing:
          enum:
          - critical
          - high
          - medium
          - low
          title: Severity If Missing
          type: string
        standard_language:
          description: The org's preferred clause.
          title: Standard Language
          type: string
      required:
      - id
      - issue
      - standard_language
      - severity_if_missing
      title: Position
      type: object
    PositionCreate:
      additionalProperties: false
      description: Request shape for creating a position — same as :class:`Position`
        minus ``id``.
      properties:
        description:
          default: ''
          title: Description
          type: string
        detection_examples:
          items:
            type: string
          title: Detection Examples
          type: array
        detection_keywords:
          items:
            type: string
          title: Detection Keywords
          type: array
        fallback_tiers:
          items:
            $ref: '#/components/schemas/FallbackTier'
          title: Fallback Tiers
          type: array
        issue:
          title: Issue
          type: string
        position_order:
          default: 0
          minimum: 0.0
          title: Position Order
          type: integer
        redline_strategy:
          default: ''
          title: Redline Strategy
          type: string
        severity_if_missing:
          enum:
          - critical
          - high
          - medium
          - low
          title: Severity If Missing
          type: string
        standard_language:
          title: Standard Language
          type: string
      required:
      - issue
      - standard_language
      - severity_if_missing
      title: PositionCreate
      type: object
    PrecedentEntryListResponse:
      description: 'Paginated list of :class:`PrecedentEntryRead` items (M4-B2).


        Mirrors ``AutonomousMemoryListResponse`` — total_count / limit /

        offset envelope. Excludes dismissed entries (``dismissed_at IS NULL``).'
      properties:
        entries:
          items:
            $ref: '#/components/schemas/PrecedentEntryRead'
          title: Entries
          type: array
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        total_count:
          title: Total Count
          type: integer
      required:
      - entries
      - total_count
      - limit
      - offset
      title: PrecedentEntryListResponse
      type: object
    PrecedentEntryRead:
      description: ORM-read view of a :class:`~app.models.autonomous.PrecedentEntry`.
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        dismissed_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Dismissed At
        id:
          format: uuid
          title: Id
          type: string
        observed_count:
          title: Observed Count
          type: integer
        pattern_kind:
          title: Pattern Kind
          type: string
        source_session_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Source Session Id
        summary:
          title: Summary
          type: string
        updated_at:
          format: date-time
          title: Updated At
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - id
      - user_id
      - pattern_kind
      - summary
      - observed_count
      - created_at
      - updated_at
      title: PrecedentEntryRead
      type: object
    ProjectContextProposalListResponse:
      description: 'Paginated list of :class:`ProjectContextProposalRead` items (M4-B2).


        Mirrors ``AutonomousMemoryListResponse`` — total_count / limit /

        offset envelope.'
      properties:
        limit:
          title: Limit
          type: integer
        offset:
          title: Offset
          type: integer
        proposals:
          items:
            $ref: '#/components/schemas/ProjectContextProposalRead'
          title: Proposals
          type: array
        total_count:
          title: Total Count
          type: integer
      required:
      - proposals
      - total_count
      - limit
      - offset
      title: ProjectContextProposalListResponse
      type: object
    ProjectContextProposalRead:
      description: 'ORM-read view of a :class:`~app.models.autonomous.ProjectContextProposal`.


        A proposal to promote a precedent into a Project''s context document.

        ``state`` walks ``proposed → accepted | rejected``; only the user

        accepting a proposal writes ``projects.context_md`` (ADR 0013 D5).'
      properties:
        accepted_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Accepted At
        created_at:
          format: date-time
          title: Created At
          type: string
        id:
          format: uuid
          title: Id
          type: string
        precedent_id:
          format: uuid
          title: Precedent Id
          type: string
        project_id:
          format: uuid
          title: Project Id
          type: string
        rejected_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Rejected At
        state:
          $ref: '#/components/schemas/ProposalState'
        suggested_md:
          title: Suggested Md
          type: string
        updated_at:
          format: date-time
          title: Updated At
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - id
      - user_id
      - precedent_id
      - project_id
      - suggested_md
      - state
      - created_at
      - updated_at
      title: ProjectContextProposalRead
      type: object
    ProjectCreateRequest:
      additionalProperties: false
      description: '``ProjectCreate`` from backend-openapi.yaml.


        ``slug`` is optional — when omitted, the handler derives one from

        ``name`` and resolves collisions per-owner with a numeric suffix

        (``-2``, ``-3``, …).'
      properties:
        context_md:
          anyOf:
          - type: string
          - type: 'null'
          title: Context Md
        description:
          anyOf:
          - maxLength: 1000
            type: string
          - type: 'null'
          title: Description
        minimum_inference_tier:
          anyOf:
          - enum:
            - 1
            - 2
            - 3
            - 4
            - 5
            type: integer
          - type: 'null'
          title: Minimum Inference Tier
        name:
          maxLength: 200
          minLength: 1
          title: Name
          type: string
        privileged:
          default: false
          title: Privileged
          type: boolean
        slug:
          anyOf:
          - maxLength: 80
            minLength: 1
            pattern: ^(?:[a-z0-9]+(?:-[a-z0-9]+)*|__[a-z0-9-]+__)$
            type: string
          - type: 'null'
          title: Slug
      required:
      - name
      title: ProjectCreateRequest
      type: object
    ProjectResponse:
      description: '``Project`` schema from backend-openapi.yaml.


        Built from the ORM row via

        :meth:`from_row` with the attached file ids and skill names already

        materialised by the handler (a single round-trip with eager-loading

        is cleaner than a lazy load through the ORM relationship).'
      properties:
        archived_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Archived At
        attached_file_ids:
          items:
            format: uuid
            type: string
          title: Attached File Ids
          type: array
        attached_knowledge_base_ids:
          items:
            format: uuid
            type: string
          title: Attached Knowledge Base Ids
          type: array
        attached_skill_names:
          items:
            type: string
          title: Attached Skill Names
          type: array
        context_md:
          anyOf:
          - type: string
          - type: 'null'
          title: Context Md
        created_at:
          format: date-time
          title: Created At
          type: string
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        id:
          format: uuid
          title: Id
          type: string
        is_sandbox:
          default: false
          title: Is Sandbox
          type: boolean
        minimum_inference_tier:
          anyOf:
          - type: integer
          - type: 'null'
          title: Minimum Inference Tier
        name:
          title: Name
          type: string
        owner_id:
          format: uuid
          title: Owner Id
          type: string
        privileged:
          title: Privileged
          type: boolean
        slug:
          title: Slug
          type: string
        updated_at:
          format: date-time
          title: Updated At
          type: string
      required:
      - id
      - owner_id
      - name
      - slug
      - privileged
      - created_at
      - updated_at
      title: ProjectResponse
      type: object
    ProjectUpdateRequest:
      additionalProperties: false
      description: '``ProjectUpdate`` from backend-openapi.yaml.


        Every field is optional — PATCH applies only the fields the caller

        sets. The model uses an ``UNSET`` sentinel-like pattern via

        ``exclude_unset`` at the handler boundary.


        Two kinds of unset are conflated by ``Optional[...] = None`` in JSON:

        "field absent from body" and "field present with explicit null." For

        ``minimum_inference_tier`` the difference matters (a caller may want

        to *clear* the tier). FastAPI/Pydantic surfaces this through

        ``model_dump(exclude_unset=True)``: explicit-null is included, absent

        is dropped. The handler uses that pattern.'
      properties:
        archived:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Archived
        context_md:
          anyOf:
          - type: string
          - type: 'null'
          title: Context Md
        description:
          anyOf:
          - maxLength: 1000
            type: string
          - type: 'null'
          title: Description
        minimum_inference_tier:
          anyOf:
          - enum:
            - 1
            - 2
            - 3
            - 4
            - 5
            type: integer
          - type: 'null'
          title: Minimum Inference Tier
        name:
          anyOf:
          - maxLength: 200
            minLength: 1
            type: string
          - type: 'null'
          title: Name
        privileged:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Privileged
        slug:
          anyOf:
          - maxLength: 80
            minLength: 1
            pattern: ^(?:[a-z0-9]+(?:-[a-z0-9]+)*|__[a-z0-9-]+__)$
            type: string
          - type: 'null'
          title: Slug
      title: ProjectUpdateRequest
      type: object
    PromotePrecedentRequest:
      description: 'Request body for ``POST /autonomous/precedents/{id}/promote``.


        ``project_id`` is the target Project; the caller must own it (404

        otherwise). The ``suggested_md`` snippet is derived server-side from

        the precedent''s ``summary`` — the client does not supply it.'
      properties:
        project_id:
          format: uuid
          title: Project Id
          type: string
      required:
      - project_id
      title: PromotePrecedentRequest
      type: object
    ProposalState:
      description: 'Lifecycle state of a project-context promotion proposal.


        Matches the CHECK constraint on ``project_context_proposals.state``

        (migration ``0041_project_context_proposals.py``).'
      enum:
      - proposed
      - accepted
      - rejected
      title: ProposalState
      type: string
    ProviderKeyRotateRequest:
      description: Request body for ``PATCH /api/v1/admin/provider-keys/{provider}``.
      properties:
        api_key:
          minLength: 1
          title: Api Key
          type: string
      required:
      - api_key
      title: ProviderKeyRotateRequest
      type: object
    ProviderKeySetRequest:
      description: Request body for ``POST /api/v1/admin/provider-keys``.
      properties:
        api_key:
          minLength: 1
          title: Api Key
          type: string
        provider:
          minLength: 1
          title: Provider
          type: string
      required:
      - provider
      - api_key
      title: ProviderKeySetRequest
      type: object
    ReceiptEvent:
      description: 'One row in the merged event stream.


        ``detail`` is a kind-specific dict (no fixed schema across kinds —

        the UI dispatches on ``kind`` to render the right shape).'
      properties:
        detail:
          additionalProperties: true
          title: Detail
          type: object
        kind:
          enum:
          - message
          - inference
          - audit
          - skill
          - retrieval
          - error
          title: Kind
          type: string
        ts:
          format: date-time
          title: Ts
          type: string
      required:
      - ts
      - kind
      - detail
      title: ReceiptEvent
      type: object
    RefreshRequest:
      description: '`/auth/refresh` body per backend-openapi.yaml.'
      properties:
        refresh_token:
          minLength: 1
          title: Refresh Token
          type: string
      required:
      - refresh_token
      title: RefreshRequest
      type: object
    ResearchCapabilities:
      properties:
        enabled:
          title: Enabled
          type: boolean
        providers:
          items:
            $ref: '#/components/schemas/ResearchProvider'
          title: Providers
          type: array
      required:
      - enabled
      title: ResearchCapabilities
      type: object
    ResearchProvider:
      properties:
        name:
          title: Name
          type: string
        type:
          title: Type
          type: string
      required:
      - name
      - type
      title: ResearchProvider
      type: object
    SavedPromptCreate:
      description: POST body. ``tags`` is optional and normalised to ``[]`` if absent.
      properties:
        name:
          maxLength: 200
          minLength: 1
          title: Name
          type: string
        prompt_text:
          maxLength: 50000
          minLength: 1
          title: Prompt Text
          type: string
        tags:
          items:
            type: string
          maxItems: 20
          title: Tags
          type: array
      required:
      - name
      - prompt_text
      title: SavedPromptCreate
      type: object
    SavedPromptResponse:
      description: 'Mirrors the ``SavedPrompt`` component in

        ``docs/api/backend-openapi.yaml``.'
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        id:
          format: uuid
          title: Id
          type: string
        name:
          title: Name
          type: string
        prompt_text:
          title: Prompt Text
          type: string
        tags:
          items:
            type: string
          title: Tags
          type: array
        updated_at:
          format: date-time
          title: Updated At
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - id
      - user_id
      - name
      - prompt_text
      - created_at
      - updated_at
      title: SavedPromptResponse
      type: object
    SavedPromptUpdate:
      description: 'PATCH body. Every field is optional — partial updates only touch

        the supplied keys, leaving the others as-stored.'
      properties:
        name:
          anyOf:
          - maxLength: 200
            minLength: 1
            type: string
          - type: 'null'
          title: Name
        prompt_text:
          anyOf:
          - maxLength: 50000
            minLength: 1
            type: string
          - type: 'null'
          title: Prompt Text
        tags:
          anyOf:
          - items:
              type: string
            maxItems: 20
            type: array
          - type: 'null'
          title: Tags
      title: SavedPromptUpdate
      type: object
    SearchRequest:
      additionalProperties: false
      properties:
        court:
          anyOf:
          - type: string
          - type: 'null'
          title: Court
        cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
        order_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Order By
        q:
          minLength: 1
          title: Q
          type: string
      required:
      - q
      title: SearchRequest
      type: object
    SearchResponse:
      properties:
        count:
          anyOf:
          - type: integer
          - type: 'null'
          title: Count
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
        results:
          items:
            $ref: '#/components/schemas/SearchResultItem'
          title: Results
          type: array
      title: SearchResponse
      type: object
    SearchResult:
      additionalProperties: false
      description: '``SearchResult`` schema from backend-openapi.yaml.'
      properties:
        chunk:
          $ref: '#/components/schemas/SearchResultChunk'
        score:
          title: Score
          type: number
        score_components:
          $ref: '#/components/schemas/SearchResultScores'
      required:
      - chunk
      - score
      - score_components
      title: SearchResult
      type: object
    SearchResultChunk:
      additionalProperties: false
      description: 'One chunk in a search-result envelope.


        ``file_name`` is denormalized from ``files.filename`` so the UI can

        render the source label without a follow-up fetch. The chunk''s own

        fields match :class:`app.models.document.DocumentChunk`.'
      properties:
        char_offset_end:
          title: Char Offset End
          type: integer
        char_offset_start:
          title: Char Offset Start
          type: integer
        content:
          title: Content
          type: string
        document_id:
          format: uuid
          title: Document Id
          type: string
        file_id:
          format: uuid
          title: File Id
          type: string
        file_name:
          title: File Name
          type: string
        id:
          format: uuid
          title: Id
          type: string
        page_end:
          anyOf:
          - type: integer
          - type: 'null'
          title: Page End
        page_start:
          anyOf:
          - type: integer
          - type: 'null'
          title: Page Start
      required:
      - id
      - document_id
      - file_id
      - file_name
      - content
      - char_offset_start
      - char_offset_end
      title: SearchResultChunk
      type: object
    SearchResultItem:
      properties:
        absolute_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Absolute Url
        case_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Case Name
        citation:
          anyOf:
          - {}
          - type: 'null'
          title: Citation
        cluster_id:
          anyOf:
          - type: integer
          - type: 'null'
          title: Cluster Id
        court:
          anyOf:
          - type: string
          - type: 'null'
          title: Court
        date_filed:
          anyOf:
          - type: string
          - type: 'null'
          title: Date Filed
        snippet:
          anyOf:
          - type: string
          - type: 'null'
          title: Snippet
      title: SearchResultItem
      type: object
    SearchResultScores:
      additionalProperties: false
      description: 'Normalized score components for a search result.


        ``vector`` is the normalized cosine-similarity (1 - cosine_distance,

        clamped to [0, 1]); ``fts`` is the normalized ``ts_rank_cd`` score.

        Both are min-max-normalized across the candidate set per ADR 0008.'
      properties:
        fts:
          title: Fts
          type: number
        vector:
          title: Vector
          type: number
      required:
      - vector
      - fts
      title: SearchResultScores
      type: object
    SessionStatus:
      description: 'Terminal-or-running lifecycle of a session.


        Matches the CHECK constraint on ``autonomous_sessions.status``.'
      enum:
      - running
      - completed
      - halted
      - failed
      title: SessionStatus
      type: string
    SkillAutocompleteItem:
      description: 'One row in the autocomplete response.


        Mirrors the shape the chat composer needs: ``slug`` to dispatch,

        ``slash_alias`` to render the badge, ``title`` + ``description`` for

        the picker, ``scope`` so the UI can disambiguate (a user shadow''s

        icon vs the built-in''s icon, for example). ``icon`` is optional and

        rides ``frontmatter_extra``-style — present for built-ins that ship

        one, ``None`` otherwise.'
      properties:
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        icon:
          anyOf:
          - type: string
          - type: 'null'
          title: Icon
        scope:
          title: Scope
          type: string
        slash_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Slash Alias
        slug:
          title: Slug
          type: string
        title:
          title: Title
          type: string
      required:
      - slug
      - title
      - scope
      title: SkillAutocompleteItem
      type: object
    SkillAutocompleteResponse:
      description: 'The envelope. Always present, even when ``results`` is empty —

        saves the frontend an existence check.'
      properties:
        results:
          items:
            $ref: '#/components/schemas/SkillAutocompleteItem'
          title: Results
          type: array
      required:
      - results
      title: SkillAutocompleteResponse
      type: object
    SkillForkBody:
      additionalProperties: false
      description: 'Request body for ``POST /api/v1/skills/{skill_name}/fork``.


        `extra="forbid"` rejects unknown fields with a 422 — so a typo like

        ``{"name": "x"}`` (instead of ``new_name``) surfaces as a clear

        validation error instead of silently falling back to the source

        slug and 409ing on a collision the user never asked for.'
      properties:
        new_name:
          anyOf:
          - type: string
          - type: 'null'
          description: Slug for the forked user-scope skill. Omit to shadow the source
            slug (same name; the user-scope copy wins on lookup).
          title: New Name
        scope:
          default: user
          description: Target scope. M1 only supports 'user'; 'team' returns 400 until
            D8.1 (ADR 0012).
          enum:
          - user
          - team
          title: Scope
          type: string
      title: SkillForkBody
      type: object
    SkillInputDef:
      additionalProperties: true
      description: 'One declared input — the shape the skill-input form renders against.


        Fields per ``docs/skill-authoring-guide.md`` + observed corpus

        variance. ``name`` is the only strictly required field; the form can

        render a text input as the default when ``type`` is absent.'
      properties:
        default:
          anyOf:
          - {}
          - type: 'null'
          title: Default
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        enum:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Enum
        name:
          title: Name
          type: string
        required:
          default: false
          title: Required
          type: boolean
        type:
          anyOf:
          - type: string
          - type: 'null'
          title: Type
      required:
      - name
      title: SkillInputDef
      type: object
    SkillInputs:
      description: The shape ``GET /api/v1/skills/{name}/inputs`` returns.
      properties:
        name:
          title: Name
          type: string
        optional:
          items:
            $ref: '#/components/schemas/SkillInputDef'
          title: Optional
          type: array
        required:
          items:
            $ref: '#/components/schemas/SkillInputDef'
          title: Required
          type: array
      required:
      - name
      title: SkillInputs
      type: object
    SlackWorkspaceCreate:
      additionalProperties: false
      description: 'Workspace record the slack-bridge POSTs after OAuth completes.


        Field shape matches what ``slack-bridge/app/oauth.py`` produces from

        Slack''s ``oauth.v2.access`` response (see lines 184-191 in that

        file). Renames here would break the bridge → api contract.'
      properties:
        bot_token:
          description: The xoxb- bot user OAuth token Slack returned. Plaintext on
            the wire; encrypted under LQ_AI_BRIDGE_MASTER_KEY before persistence.
          minLength: 1
          title: Bot Token
          type: string
        bot_user_id:
          description: Slack user id of the bot user.
          minLength: 1
          title: Bot User Id
          type: string
        installer_slack_user_id:
          description: Slack user id of the operator who completed install (audit
            only).
          minLength: 1
          title: Installer Slack User Id
          type: string
        scope:
          description: Comma-separated scope list Slack returned (e.g. 'commands,chat:write').
          minLength: 1
          title: Scope
          type: string
        team_id:
          description: Slack workspace id (T0...).
          minLength: 1
          title: Team Id
          type: string
        team_name:
          description: Workspace display name at install time.
          minLength: 1
          title: Team Name
          type: string
      required:
      - team_id
      - team_name
      - bot_token
      - bot_user_id
      - installer_slack_user_id
      - scope
      title: SlackWorkspaceCreate
      type: object
    SlackWorkspaceResponse:
      description: 'Persisted-workspace response. Bot token deliberately omitted.


        The bridge does not need the ciphertext echoed back; the response

        exists so the bridge can log the api-side workspace id for

        correlation and confirm the upsert path it took.'
      properties:
        bot_user_id:
          title: Bot User Id
          type: string
        id:
          format: uuid
          title: Id
          type: string
        installed_at:
          format: date-time
          title: Installed At
          type: string
        installer_slack_user_id:
          title: Installer Slack User Id
          type: string
        scope:
          title: Scope
          type: string
        team_id:
          title: Team Id
          type: string
        team_name:
          title: Team Name
          type: string
      required:
      - id
      - team_id
      - team_name
      - bot_user_id
      - installer_slack_user_id
      - scope
      - installed_at
      title: SlackWorkspaceResponse
      type: object
    SlackWorkspaceSummary:
      description: One live Slack workspace install for the admin list.
      properties:
        id:
          format: uuid
          title: Id
          type: string
        installed_at:
          format: date-time
          title: Installed At
          type: string
        installer_slack_user_id:
          title: Installer Slack User Id
          type: string
        team_id:
          title: Team Id
          type: string
        team_name:
          title: Team Name
          type: string
      required:
      - id
      - team_id
      - team_name
      - installer_slack_user_id
      - installed_at
      title: SlackWorkspaceSummary
      type: object
    SourcesResponse:
      description: Envelope for GET /api/v1/research/sources.
      properties:
        sources:
          items:
            $ref: '#/components/schemas/AvailableSourceResponse'
          title: Sources
          type: array
      required:
      - sources
      title: SourcesResponse
      type: object
    TabularExecutionCreate:
      description: 'Request body for ``POST /api/v1/tabular/execute``.


        Either ``skill_name`` (resolved at execution start to snapshot

        the skill''s ``lq_ai.columns``) OR ``columns`` (ad-hoc spec) is

        required. The handler resolves to the columns list either way

        before persisting.


        ``confirmed_cost_usd`` is the echo of the

        ``POST /api/v1/tabular/preview-cost`` response value; persisting

        it gives an audit trail of the operator confirming a specific

        cost ceiling before kickoff.'
      properties:
        columns:
          anyOf:
          - items:
              $ref: '#/components/schemas/ColumnSpec'
            type: array
          - type: 'null'
          title: Columns
        confirmed_cost_usd:
          anyOf:
          - type: number
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Confirmed Cost Usd
        document_ids:
          items:
            format: uuid
            type: string
          minItems: 1
          title: Document Ids
          type: array
        skill_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Name
      required:
      - document_ids
      title: TabularExecutionCreate
      type: object
    TabularExecutionResponse:
      description: 'Wire shape returned by every tabular execution endpoint

        (``POST /execute``, ``GET /executions/{id}``, etc.).'
      properties:
        columns:
          items:
            $ref: '#/components/schemas/ColumnSpec'
          title: Columns
          type: array
        completed_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Completed At
        cost_actual_usd:
          anyOf:
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Cost Actual Usd
        cost_estimate_usd:
          anyOf:
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Cost Estimate Usd
        created_at:
          format: date-time
          title: Created At
          type: string
        document_ids:
          items:
            format: uuid
            type: string
          title: Document Ids
          type: array
        document_names:
          items:
            type: string
          title: Document Names
          type: array
        error_text:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Text
        id:
          format: uuid
          title: Id
          type: string
        parent_execution_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Parent Execution Id
        results:
          anyOf:
          - $ref: '#/components/schemas/TabularResults'
          - type: 'null'
        skill_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Name
        started_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Started At
        status:
          enum:
          - pending
          - running
          - completed
          - failed
          - cancelled
          title: Status
          type: string
        user_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: User Id
      required:
      - id
      - user_id
      - parent_execution_id
      - skill_name
      - status
      - document_ids
      - document_names
      - columns
      - created_at
      title: TabularExecutionResponse
      type: object
    TabularExecutionSummary:
      description: 'Compact wire shape for the list endpoint

        (``GET /api/v1/tabular/executions``).


        Drops the (potentially large) ``results`` payload — operators

        fetch the full execution row when they open one.'
      properties:
        column_count:
          title: Column Count
          type: integer
        completed_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Completed At
        cost_actual_usd:
          anyOf:
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Cost Actual Usd
        cost_estimate_usd:
          anyOf:
          - pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
            type: string
          - type: 'null'
          title: Cost Estimate Usd
        created_at:
          format: date-time
          title: Created At
          type: string
        document_count:
          title: Document Count
          type: integer
        id:
          format: uuid
          title: Id
          type: string
        parent_execution_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Parent Execution Id
        skill_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Name
        status:
          enum:
          - pending
          - running
          - completed
          - failed
          - cancelled
          title: Status
          type: string
        user_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: User Id
      required:
      - id
      - user_id
      - parent_execution_id
      - skill_name
      - status
      - document_count
      - column_count
      - created_at
      title: TabularExecutionSummary
      type: object
    TabularPreviewCostRequest:
      description: 'Request body for ``POST /api/v1/tabular/preview-cost``.


        Cheaper synchronous endpoint; no execution row is created. The

        UI calls this before showing the cost-confirmation modal.'
      properties:
        columns:
          anyOf:
          - items:
              $ref: '#/components/schemas/ColumnSpec'
            type: array
          - type: 'null'
          title: Columns
        document_ids:
          items:
            format: uuid
            type: string
          minItems: 1
          title: Document Ids
          type: array
        skill_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Skill Name
      required:
      - document_ids
      title: TabularPreviewCostRequest
      type: object
    TabularPreviewCostResponse:
      description: 'Response from ``POST /api/v1/tabular/preview-cost``.


        The UI uses ``estimated_cost_usd`` to decide whether to render

        the confirmation-checkbox gate (Decision C-5: gate above $1.00,

        no friction below).


        Ensemble columns cost more (Donna #6). When a column''s effective

        ``ensemble_verification`` flag is true, each of its cells runs one

        Stage-4 ensemble pass (N judge-model calls) on top of the base

        extraction. ``estimated_cost_usd`` is the TOTAL — base extraction

        plus the ensemble premium — so the operator''s confirmation reflects

        the real spend. ``ensemble_premium_usd`` and ``ensemble_cells_count``

        surface that premium explicitly.'
      properties:
        cells_count:
          title: Cells Count
          type: integer
        ensemble_cells_count:
          default: 0
          title: Ensemble Cells Count
          type: integer
        ensemble_premium_usd:
          default: '0'
          pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          title: Ensemble Premium Usd
          type: string
        estimated_cost_usd:
          pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          title: Estimated Cost Usd
          type: string
        estimated_tokens:
          title: Estimated Tokens
          type: integer
        per_tier_breakdown:
          additionalProperties:
            type: integer
          title: Per Tier Breakdown
          type: object
      required:
      - cells_count
      - estimated_tokens
      - estimated_cost_usd
      - per_tier_breakdown
      title: TabularPreviewCostResponse
      type: object
    TabularResults:
      description: 'Aggregated grid shape persisted in ``tabular_executions.results``

        once status is ``completed``.'
      properties:
        rows:
          items:
            $ref: '#/components/schemas/TabularRow'
          title: Rows
          type: array
      required:
      - rows
      title: TabularResults
      type: object
    TabularRow:
      description: 'One row in the tabular grid — all cells for a single document.


        Rows are returned in the order of the execution''s

        ``document_ids`` array (NOT sorted by document name), so the grid

        matches the operator''s selection order.'
      properties:
        cells:
          additionalProperties:
            $ref: '#/components/schemas/CellResult'
          title: Cells
          type: object
        document_id:
          format: uuid
          title: Document Id
          type: string
        document_name:
          title: Document Name
          type: string
      required:
      - document_id
      - document_name
      - cells
      title: TabularRow
      type: object
    TeamCreate:
      properties:
        description:
          anyOf:
          - maxLength: 2000
            type: string
          - type: 'null'
          title: Description
        name:
          maxLength: 200
          minLength: 1
          title: Name
          type: string
        slug:
          maxLength: 80
          minLength: 1
          title: Slug
          type: string
      required:
      - slug
      - name
      title: TeamCreate
      type: object
    TeamMemberAdd:
      properties:
        role:
          default: member
          title: Role
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - user_id
      title: TeamMemberAdd
      type: object
    TeamMemberResponse:
      properties:
        added_by_user_id:
          format: uuid
          title: Added By User Id
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
        email:
          title: Email
          type: string
        role:
          title: Role
          type: string
        user_id:
          format: uuid
          title: User Id
          type: string
      required:
      - user_id
      - email
      - role
      - added_by_user_id
      - created_at
      title: TeamMemberResponse
      type: object
    TeamMemberRoleUpdate:
      properties:
        role:
          title: Role
          type: string
      required:
      - role
      title: TeamMemberRoleUpdate
      type: object
    TeamResponse:
      description: Full team payload including the membership roster.
      properties:
        caller_role:
          anyOf:
          - type: string
          - type: 'null'
          title: Caller Role
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by_user_id:
          format: uuid
          title: Created By User Id
          type: string
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        id:
          format: uuid
          title: Id
          type: string
        member_count:
          default: 0
          title: Member Count
          type: integer
        members:
          items:
            $ref: '#/components/schemas/TeamMemberResponse'
          title: Members
          type: array
        name:
          title: Name
          type: string
        slug:
          title: Slug
          type: string
        updated_at:
          format: date-time
          title: Updated At
          type: string
      required:
      - id
      - slug
      - name
      - created_by_user_id
      - created_at
      - updated_at
      title: TeamResponse
      type: object
    TeamSummary:
      description: 'Compact team shape for list responses.


        ``caller_role`` (D8.1c) carries the caller''s role on this team —

        ``admin`` or ``member`` for user-facing list/detail responses, and

        ``None`` for operator-admin views where the caller isn''t a

        member. The UI uses this to render the admin badge + filter the

        team-scope skill creation picker without a second round-trip.'
      properties:
        caller_role:
          anyOf:
          - type: string
          - type: 'null'
          title: Caller Role
        created_at:
          format: date-time
          title: Created At
          type: string
        created_by_user_id:
          format: uuid
          title: Created By User Id
          type: string
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
        id:
          format: uuid
          title: Id
          type: string
        member_count:
          default: 0
          title: Member Count
          type: integer
        name:
          title: Name
          type: string
        slug:
          title: Slug
          type: string
        updated_at:
          format: date-time
          title: Updated At
          type: string
      required:
      - id
      - slug
      - name
      - created_by_user_id
      - created_at
      - updated_at
      title: TeamSummary
      type: object
    TeamUpdate:
      properties:
        description:
          anyOf:
          - maxLength: 2000
            type: string
          - type: 'null'
          title: Description
        name:
          anyOf:
          - maxLength: 200
            minLength: 1
            type: string
          - type: 'null'
          title: Name
      title: TeamUpdate
      type: object
    TeamsTenantCreate:
      additionalProperties: false
      description: 'Tenant record the teams-bridge POSTs after admin consent.


        Field shape matches what ``teams-bridge/app/oauth.py`` produces

        from the ``id_token`` claims (``tid`` for tenant_id, ``oid`` for

        installer) and the Microsoft Graph ``/organization`` lookup for

        the display name.'
      properties:
        installer_oid:
          description: M365 object id (`oid` claim) of the admin who completed consent.
            Audit only — does not grant LQ.AI permissions.
          minLength: 1
          title: Installer Oid
          type: string
        tenant_id:
          description: Microsoft tenant id (`tid` claim — the M365 directory GUID).
          minLength: 1
          title: Tenant Id
          type: string
        tenant_name:
          description: Tenant display name at install time (from Graph /organization).
          minLength: 1
          title: Tenant Name
          type: string
      required:
      - tenant_id
      - tenant_name
      - installer_oid
      title: TeamsTenantCreate
      type: object
    TeamsTenantResponse:
      description: Persisted-tenant response. Mirrors :class:`SlackWorkspaceResponse`.
      properties:
        id:
          format: uuid
          title: Id
          type: string
        installed_at:
          format: date-time
          title: Installed At
          type: string
        installer_oid:
          title: Installer Oid
          type: string
        tenant_id:
          title: Tenant Id
          type: string
        tenant_name:
          title: Tenant Name
          type: string
      required:
      - id
      - tenant_id
      - tenant_name
      - installer_oid
      - installed_at
      title: TeamsTenantResponse
      type: object
    TeamsTenantSummary:
      description: One live Teams tenant install for the admin list.
      properties:
        id:
          format: uuid
          title: Id
          type: string
        installed_at:
          format: date-time
          title: Installed At
          type: string
        installer_oid:
          title: Installer Oid
          type: string
        tenant_id:
          title: Tenant Id
          type: string
        tenant_name:
          title: Tenant Name
          type: string
      required:
      - id
      - tenant_id
      - tenant_name
      - installer_oid
      - installed_at
      title: TeamsTenantSummary
      type: object
    TierConfigResponse:
      description: '``GET /inference/tier-config`` response — the operator''s tier
        policy.'
      properties:
        allowed_tiers_global:
          items:
            type: integer
          title: Allowed Tiers Global
          type: array
        default_minimum_tier:
          title: Default Minimum Tier
          type: integer
        privileged_minimum_tier:
          title: Privileged Minimum Tier
          type: integer
        warn_on_tiers:
          items:
            type: integer
          title: Warn On Tiers
          type: array
      required:
      - allowed_tiers_global
      - default_minimum_tier
      - privileged_minimum_tier
      - warn_on_tiers
      title: TierConfigResponse
      type: object
    TierPolicyPatchRequest:
      description: '``PATCH /admin/tier-policy`` body — partial update (Wave B).'
      properties:
        allowed_tiers_global:
          anyOf:
          - items:
              type: integer
            type: array
          - type: 'null'
          title: Allowed Tiers Global
        default_minimum_tier:
          anyOf:
          - maximum: 5.0
            minimum: 1.0
            type: integer
          - type: 'null'
          title: Default Minimum Tier
        privileged_minimum_tier:
          anyOf:
          - maximum: 5.0
            minimum: 1.0
            type: integer
          - type: 'null'
          title: Privileged Minimum Tier
        warn_on_tiers:
          anyOf:
          - items:
              type: integer
            type: array
          - type: 'null'
          title: Warn On Tiers
      title: TierPolicyPatchRequest
      type: object
    TierPolicyResponse:
      description: '``GET /admin/tier-policy`` body (Wave B).'
      properties:
        allowed_tiers_global:
          items:
            type: integer
          title: Allowed Tiers Global
          type: array
        default_minimum_tier:
          title: Default Minimum Tier
          type: integer
        privileged_minimum_tier:
          title: Privileged Minimum Tier
          type: integer
        warn_on_tiers:
          items:
            type: integer
          title: Warn On Tiers
          type: array
      required:
      - allowed_tiers_global
      - default_minimum_tier
      - privileged_minimum_tier
      - warn_on_tiers
      title: TierPolicyResponse
      type: object
    TokenResponse:
      description: '`TokenResponse` schema from backend-openapi.yaml.


        NOTE: the OpenAPI sketch''s `TokenResponse` does not include `refresh_token`.

        PRD §5.1 mandates that refresh tokens are rotated on each refresh — and a

        rotated token must be returned to the client or the client cannot use the

        new session. We therefore extend `TokenResponse` with `refresh_token`,

        additive to the OpenAPI sketch (existing fields are unchanged). This drift

        is flagged in the B1 task report so the OpenAPI sketch can be corrected.'
      properties:
        access_token:
          title: Access Token
          type: string
        expires_in:
          title: Expires In
          type: integer
        refresh_token:
          title: Refresh Token
          type: string
        token_type:
          default: Bearer
          title: Token Type
          type: string
      required:
      - access_token
      - expires_in
      - refresh_token
      title: TokenResponse
      type: object
    ToolProviderPatchRequest:
      additionalProperties: false
      description: Request body for ``PATCH /api/v1/admin/tool-providers/{type}``.
      properties:
        api_key:
          anyOf:
          - minLength: 1
            type: string
          - type: 'null'
          title: Api Key
        enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
      title: ToolProviderPatchRequest
      type: object
    ToolProviderSetRequest:
      additionalProperties: false
      description: Request body for ``POST /api/v1/admin/tool-providers``.
      properties:
        api_key:
          anyOf:
          - minLength: 1
            type: string
          - type: 'null'
          title: Api Key
        type:
          minLength: 1
          title: Type
          type: string
      required:
      - type
      title: ToolProviderSetRequest
      type: object
    TriggerKind:
      description: 'How an autonomous session was started.


        Matches the CHECK constraint on ``autonomous_sessions.trigger_kind``.'
      enum:
      - watch
      - schedule
      - suggestion
      - manual
      title: TriggerKind
      type: string
    UsageResponse:
      properties:
        group_by:
          title: Group By
          type: string
        rows:
          items:
            $ref: '#/components/schemas/UsageRow'
          title: Rows
          type: array
        total_cost_estimate:
          title: Total Cost Estimate
          type: number
        total_request_count:
          title: Total Request Count
          type: integer
        total_tokens_in:
          title: Total Tokens In
          type: integer
        total_tokens_out:
          title: Total Tokens Out
          type: integer
      required:
      - rows
      - group_by
      - total_request_count
      - total_tokens_in
      - total_tokens_out
      - total_cost_estimate
      title: UsageResponse
      type: object
    UsageRow:
      description: One aggregated row from /admin/usage.
      properties:
        cost_estimate_sum:
          default: 0.0
          title: Cost Estimate Sum
          type: number
        group_key:
          title: Group Key
          type: string
        request_count:
          title: Request Count
          type: integer
        tokens_in_sum:
          default: 0
          title: Tokens In Sum
          type: integer
        tokens_out_sum:
          default: 0
          title: Tokens Out Sum
          type: integer
      required:
      - group_key
      - request_count
      title: UsageRow
      type: object
    UserPreferencesResponse:
      description: 'The preferences slice of the user profile.


        Mirror of the corresponding fields on ``UserPublic`` so the frontend

        can subscribe to preferences without watching the whole user object.'
      properties:
        autonomous_enabled:
          title: Autonomous Enabled
          type: boolean
        featured_tools:
          enum:
          - prominent
          - inline
          title: Featured Tools
          type: string
        provenance_pills:
          enum:
          - always
          - collapsed
          title: Provenance Pills
          type: string
        reasoning_visibility:
          enum:
          - always_show
          - disclosure
          - on_request
          title: Reasoning Visibility
          type: string
        trust_pills:
          enum:
          - labels
          - dots
          title: Trust Pills
          type: string
        workspace_layout:
          enum:
          - three_pane
          - two_pane
          - one_pane
          title: Workspace Layout
          type: string
      required:
      - reasoning_visibility
      - featured_tools
      - workspace_layout
      - trust_pills
      - provenance_pills
      - autonomous_enabled
      title: UserPreferencesResponse
      type: object
    UserPreferencesUpdate:
      description: 'PATCH body for ``/users/me/preferences`` — all fields optional.


        Structured as a forward-compatible preferences object: future Wave A+

        preferences (e.g., default jurisdiction, default model) ride here

        rather than each getting its own endpoint.'
      properties:
        autonomous_enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Autonomous Enabled
        featured_tools:
          anyOf:
          - enum:
            - prominent
            - inline
            type: string
          - type: 'null'
          title: Featured Tools
        provenance_pills:
          anyOf:
          - enum:
            - always
            - collapsed
            type: string
          - type: 'null'
          title: Provenance Pills
        reasoning_visibility:
          anyOf:
          - enum:
            - always_show
            - disclosure
            - on_request
            type: string
          - type: 'null'
          title: Reasoning Visibility
        trust_pills:
          anyOf:
          - enum:
            - labels
            - dots
            type: string
          - type: 'null'
          title: Trust Pills
        workspace_layout:
          anyOf:
          - enum:
            - three_pane
            - two_pane
            - one_pane
            type: string
          - type: 'null'
          title: Workspace Layout
      title: UserPreferencesUpdate
      type: object
    UserProfileUpdate:
      description: "PATCH body for ``/users/me`` — edit the caller's own profile.\n\
        \nCurrently scoped to ``display_name`` only. Email self-service editing\n\
        is intentionally **out of scope** for this surface: changing the login\nemail\
        \ pulls in re-verification, MFA, and uniqueness concerns that are\nauth-adjacent\
        \ and warrant their own dedicated flow. ``display_name``\nalone unblocks the\
        \ editable-profile experience.\n\nValidation (mirrors how ``UserPreferencesUpdate``\
        \ constrains its\nfields, but for a free-text field):\n\n* ``display_name``\
        \ is optional — omitting it means \"no change\".\n* When supplied, leading/trailing\
        \ whitespace is trimmed and the result\n  must be non-empty (empty or whitespace-only\
        \ → 422).\n* The trimmed value must be at most ``_DISPLAY_NAME_MAX_LEN`` chars\n\
        \  (over-length → 422).\n* At least one updatable field must be present; since\
        \ ``display_name``\n  is the only field today, an all-omitted body → 422 (\"\
        no fields to\n  update\")."
      properties:
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
      title: UserProfileUpdate
      type: object
    UserPublic:
      description: '`User` schema from backend-openapi.yaml.


        Mirrors the OpenAPI `User`. We lift this into a Pydantic model rather

        than serializing the ORM row directly so the API surface is decoupled

        from the storage shape — adding a column to `users` won''t accidentally

        leak it to the client.'
      properties:
        created_at:
          format: date-time
          title: Created At
          type: string
        deletion_scheduled_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Deletion Scheduled At
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
        email:
          title: Email
          type: string
        featured_tools:
          default: prominent
          title: Featured Tools
          type: string
        id:
          title: Id
          type: string
        is_admin:
          title: Is Admin
          type: boolean
        last_login_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Last Login At
        mfa_enabled:
          title: Mfa Enabled
          type: boolean
        must_change_password:
          default: false
          title: Must Change Password
          type: boolean
        provenance_pills:
          default: always
          title: Provenance Pills
          type: string
        reasoning_visibility:
          default: disclosure
          title: Reasoning Visibility
          type: string
        role:
          default: member
          title: Role
          type: string
        trust_pills:
          default: labels
          title: Trust Pills
          type: string
        workspace_layout:
          default: three_pane
          title: Workspace Layout
          type: string
      required:
      - id
      - email
      - is_admin
      - mfa_enabled
      - created_at
      title: UserPublic
      type: object
    UserRoleResponse:
      description: '``PATCH /admin/users/{user_id}/role`` response.'
      properties:
        email:
          title: Email
          type: string
        is_admin:
          title: Is Admin
          type: boolean
        role:
          title: Role
          type: string
        user_id:
          title: User Id
          type: string
      required:
      - user_id
      - email
      - role
      - is_admin
      title: UserRoleResponse
      type: object
    UserRoleUpdate:
      description: '``PATCH /admin/users/{user_id}/role`` body.'
      properties:
        role:
          title: Role
          type: string
      required:
      - role
      title: UserRoleUpdate
      type: object
    UserSkillCreate:
      description: 'POST body. Required fields mirror the minimum needed to render

        a Skill-shaped payload to the gateway during prompt assembly.


        ``scope`` defaults to ``''user''`` (the D8 path). ``scope=''team''``

        requires ``owner_team_id`` and the caller must be a team-admin

        member of that team (gated at the handler). ``owner_team_id`` is

        rejected when ``scope=''user''`` so accidental hybrid rows can''t be

        constructed via the API.'
      properties:
        body:
          maxLength: 200000
          minLength: 1
          title: Body
          type: string
        description:
          maxLength: 2000
          minLength: 1
          title: Description
          type: string
        display_name:
          maxLength: 200
          minLength: 1
          title: Display Name
          type: string
        forked_from:
          anyOf:
          - type: string
          - type: 'null'
          description: Documentary slug of the source skill if forked. Write-once.
          title: Forked From
        frontmatter_extra:
          additionalProperties: true
          title: Frontmatter Extra
          type: object
        owner_team_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Owner Team Id
        scope:
          default: user
          pattern: ^(user|team)$
          title: Scope
          type: string
        slash_alias:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional /slug invocation alias; ^/[a-z0-9-]{1,32}$.
          title: Slash Alias
        slug:
          maxLength: 80
          minLength: 1
          title: Slug
          type: string
        source_message_id:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Capture metadata: source AI message id when this skill was
            distilled from chat. Documentary only — not persisted as a column; rides
            the create-time audit-log row.'
          title: Source Message Id
        tags:
          items:
            type: string
          maxItems: 20
          title: Tags
          type: array
        version:
          default: 1.0.0
          maxLength: 50
          minLength: 1
          title: Version
          type: string
      required:
      - slug
      - display_name
      - description
      - body
      title: UserSkillCreate
      type: object
    UserSkillResponse:
      description: 'Full row view — what the management UI consumes.


        Distinct from the ``Skill`` shape returned by the merged picker

        endpoint (``GET /api/v1/skills/{slug}``) because this view is

        designed for *editing*: every column is here, no synthesis,

        nothing dropped for compactness.


        Exactly one of ``owner_user_id`` / ``owner_team_id`` is set — the

        DB''s ``ck_user_skills_scope_owner_consistency`` CHECK enforces

        this. The matching slot is ``None``.'
      properties:
        archived_at:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Archived At
        body:
          title: Body
          type: string
        created_at:
          format: date-time
          title: Created At
          type: string
        description:
          title: Description
          type: string
        display_name:
          title: Display Name
          type: string
        forked_from:
          anyOf:
          - type: string
          - type: 'null'
          title: Forked From
        frontmatter_extra:
          additionalProperties: true
          title: Frontmatter Extra
          type: object
        id:
          format: uuid
          title: Id
          type: string
        owner_team_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Owner Team Id
        owner_user_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Owner User Id
        scope:
          title: Scope
          type: string
        slash_alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Slash Alias
        slug:
          title: Slug
          type: string
        tags:
          items:
            type: string
          title: Tags
          type: array
        updated_at:
          format: date-time
          title: Updated At
          type: string
        version:
          title: Version
          type: string
      required:
      - id
      - scope
      - slug
      - display_name
      - description
      - version
      - body
      - created_at
      - updated_at
      title: UserSkillResponse
      type: object
    UserSkillUpdate:
      description: 'PATCH body. Every field is optional; only the supplied keys move.


        ``forked_from`` and ``source_message_id`` are intentionally absent —

        they are write-once at create time so the documentary lineage cannot

        be rewritten after creation. The audit log carries the actual lineage

        record per ADR 0012 §5.'
      properties:
        body:
          anyOf:
          - maxLength: 200000
            minLength: 1
            type: string
          - type: 'null'
          title: Body
        description:
          anyOf:
          - maxLength: 2000
            minLength: 1
            type: string
          - type: 'null'
          title: Description
        display_name:
          anyOf:
          - maxLength: 200
            minLength: 1
            type: string
          - type: 'null'
          title: Display Name
        frontmatter_extra:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Frontmatter Extra
        slash_alias:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional /slug invocation alias; ^/[a-z0-9-]{1,32}$.
          title: Slash Alias
        tags:
          anyOf:
          - items:
              type: string
            maxItems: 20
            type: array
          - type: 'null'
          title: Tags
        version:
          anyOf:
          - maxLength: 50
            minLength: 1
            type: string
          - type: 'null'
          title: Version
      title: UserSkillUpdate
      type: object
    UserSkillVersionItem:
      description: 'One audit-log row, projected onto the version-history view.


        The ``details`` blob is the raw JSONB column from ``audit_log`` —

        callers consuming this endpoint already trust the action vocabulary

        so re-shaping per action would only add coupling. ``version`` is

        surfaced as a top-level convenience because every create / update

        row carries it and the timeline UI hangs the version pill off it.'
      properties:
        action:
          title: Action
          type: string
        actor_email:
          anyOf:
          - type: string
          - type: 'null'
          title: Actor Email
        actor_user_id:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Actor User Id
        details:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Details
        timestamp:
          format: date-time
          title: Timestamp
          type: string
        version:
          anyOf:
          - type: string
          - type: 'null'
          title: Version
      required:
      - timestamp
      - action
      title: UserSkillVersionItem
      type: object
    UserSkillVersionsResponse:
      description: Wrapper so future cursors / counts can land without a breaking
        change.
      properties:
        items:
          items:
            $ref: '#/components/schemas/UserSkillVersionItem'
          title: Items
          type: array
      required:
      - items
      title: UserSkillVersionsResponse
      type: object
    ValidationError:
      properties:
        ctx:
          title: Context
          type: object
        input:
          title: Input
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          title: Location
          type: array
        msg:
          title: Message
          type: string
        type:
          title: Error Type
          type: string
      required:
      - loc
      - msg
      - type
      title: ValidationError
      type: object
    VerifiedCitation:
      properties:
        citation:
          anyOf:
          - type: string
          - type: 'null'
          title: Citation
        clusters:
          items:
            $ref: '#/components/schemas/CitationCluster'
          title: Clusters
          type: array
        error_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Message
        normalized_citations:
          items:
            type: string
          title: Normalized Citations
          type: array
        status:
          anyOf:
          - type: integer
          - type: 'null'
          title: Status
      title: VerifiedCitation
      type: object
    VerifyCitationsRequest:
      additionalProperties: false
      properties:
        text:
          maxLength: 64000
          minLength: 1
          title: Text
          type: string
      required:
      - text
      title: VerifyCitationsRequest
      type: object
    VerifyCitationsResponse:
      properties:
        citations:
          items:
            $ref: '#/components/schemas/VerifiedCitation'
          title: Citations
          type: array
      title: VerifyCitationsResponse
      type: object
    WordAddinVersionResponse:
      description: Wire shape for the version handshake (M3-B8).
      properties:
        addin_max_compatible_version:
          description: Highest add-in version this deployment recognizes. Task pane
            bundles newer than this should still load (forward compatibility is best-effort)
            but the add-in surfaces a soft warning so the operator knows to update
            the deployment.
          title: Addin Max Compatible Version
          type: string
        addin_min_compatible_version:
          description: Lowest add-in version (semver string) this deployment accepts.
            The task pane refuses to render features when its bundled version is lower.
          title: Addin Min Compatible Version
          type: string
        deployment_version:
          description: LQ.AI deployment version (api package ``__version__``). Informational
            — the add-in doesn't use this for compatibility decisions; it surfaces
            the value in the 'Update needed' overlay so the user can quote it to support.
          title: Deployment Version
          type: string
        taskpane_bundle_hash:
          anyOf:
          - type: string
          - type: 'null'
          description: Optional SHA-256 hash of the deployed task-pane bundle JS.
            When present, the add-in can verify it loaded the bundle the deployment
            expects (catches Office's occasional stale-cache behavior). M3-B8 ships
            with this field nullable; M3-B7 / signing CI populates the value from
            the build manifest. Null means 'don't enforce' — not an error condition.
          title: Taskpane Bundle Hash
        taskpane_bundle_url:
          description: Canonical URL of the task-pane bundle's HTML entry point. The
            task pane reaches this from its `window.location` today; the value exists
            in the handshake so a future deployment can serve the bundle from a CDN
            or operator-chosen path without changing the manifest.
          title: Taskpane Bundle Url
          type: string
      required:
      - deployment_version
      - addin_min_compatible_version
      - addin_max_compatible_version
      - taskpane_bundle_url
      title: WordAddinVersionResponse
      type: object
    _FallbackEntry:
      description: One ``fallback`` list entry on an alias write.
      properties:
        model:
          minLength: 1
          title: Model
          type: string
        provider:
          minLength: 1
          title: Provider
          type: string
      required:
      - provider
      - model
      title: _FallbackEntry
      type: object
    app__api__projects__AttachFileRequest:
      description: '``POST /api/v1/projects/{id}/files`` body.'
      properties:
        file_id:
          format: uuid
          title: File Id
          type: string
      required:
      - file_id
      title: AttachFileRequest
      type: object
    app__schemas__knowledge__AttachFileRequest:
      additionalProperties: false
      description: '``POST /api/v1/knowledge-bases/{id}/files`` body.'
      properties:
        file_id:
          format: uuid
          title: File Id
          type: string
      required:
      - file_id
      title: AttachFileRequest
      type: object
  securitySchemes:
    OAuth2PasswordBearer:
      flows:
        password:
          scopes: {}
          tokenUrl: /api/v1/auth/login
      type: oauth2
info:
  description: Backend API for the LQ.AI platform. The full surface is specified in
    docs/api/backend-openapi.yaml. M1 implementation lands progressively per docs/M1-IMPLEMENTATION-ORDER.md;
    until then, all endpoints under /api/v1 return 501 with a structured 'not implemented'
    body that names the implementing task.
  title: LQ.AI Backend API
  version: 0.7.1
openapi: 3.1.0
paths:
  /api/v1/admin/aliases:
    get:
      description: List configured aliases via the gateway's admin surface.
      operationId: list_aliases_api_v1_admin_aliases_get
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response List Aliases Api V1 Admin Aliases Get
                type: object
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List Aliases
      tags:
      - admin
    post:
      description: Create a new alias. 409 propagates from the gateway.
      operationId: create_alias_api_v1_admin_aliases_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AliasCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Create Alias Api V1 Admin Aliases Post
                type: object
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Create Alias
      tags:
      - admin
  /api/v1/admin/aliases/{name}:
    delete:
      description: Remove an alias. 404 propagates from the gateway.
      operationId: remove_alias_api_v1_admin_aliases__name__delete
      parameters:
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Remove Alias
      tags:
      - admin
    get:
      description: Return a single alias. 404 propagates from the gateway.
      operationId: get_alias_api_v1_admin_aliases__name__get
      parameters:
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Get Alias Api V1 Admin Aliases  Name  Get
                type: object
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Get Alias
      tags:
      - admin
    patch:
      description: Update an existing alias. 404 propagates from the gateway.
      operationId: update_alias_api_v1_admin_aliases__name__patch
      parameters:
      - in: path
        name: name
        required: true
        schema:
          title: Name
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AliasUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Update Alias Api V1 Admin Aliases  Name  Patch
                type: object
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update Alias
      tags:
      - admin
  /api/v1/admin/audit-log:
    get:
      description: 'GET /api/v1/admin/audit-log — admin-only read of the audit log.


        Filters compose with AND. The result is ordered by

        ``timestamp DESC`` (most recent first). Pagination is cursor-based

        against the timestamp+id pair, so concurrent inserts during a

        paginated walk don''t shift the page boundaries.


        Privilege filter semantics: ``privilege_marked=true`` returns only

        privileged-project rows (per PRD §3.11 / D3 verification step);

        ``privilege_marked=false`` returns only non-privileged rows; omitting

        the parameter returns both.'
      operationId: get_audit_log_api_v1_admin_audit_log_get
      parameters:
      - description: Filter to entries with the given privilege flag. Omit to return
          both privileged and non-privileged rows.
        in: query
        name: privilege_marked
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          description: Filter to entries with the given privilege flag. Omit to return
            both privileged and non-privileged rows.
          title: Privilege Marked
      - description: Filter to entries with the given routed inference tier (1-5).
          Omit to return all tiers (and non-inference entries).
        in: query
        name: routed_inference_tier
        required: false
        schema:
          anyOf:
          - maximum: 5
            minimum: 1
            type: integer
          - type: 'null'
          description: Filter to entries with the given routed inference tier (1-5).
            Omit to return all tiers (and non-inference entries).
          title: Routed Inference Tier
      - description: Exact-match filter on ``action`` (e.g., ``chat.message_sent``).
        in: query
        name: action
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Exact-match filter on ``action`` (e.g., ``chat.message_sent``).
          title: Action
      - description: Filter to entries authored by the given user (UUID string).
        in: query
        name: user_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Filter to entries authored by the given user (UUID string).
          title: User Id
      - description: ISO-8601 timestamp; only entries at or after this time.
        in: query
        name: since
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          description: ISO-8601 timestamp; only entries at or after this time.
          title: Since
      - description: ISO-8601 timestamp; only entries at or before this time.
        in: query
        name: until
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          description: ISO-8601 timestamp; only entries at or before this time.
          title: Until
      - description: Page size (1-500).
        in: query
        name: limit
        required: false
        schema:
          default: 50
          description: Page size (1-500).
          maximum: 500
          minimum: 1
          title: Limit
          type: integer
      - description: Opaque pagination cursor (the previous page's ``next_cursor``).
          Omit on the first page.
        in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Opaque pagination cursor (the previous page's ``next_cursor``).
            Omit on the first page.
          title: Cursor
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogPage'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List audit-log entries with privilege / tier filters (D3)
      tags:
      - admin
  /api/v1/admin/bootstrap-status:
    get:
      description: Unauthenticated probe used by the login UI to render fresh-install
        hints.
      operationId: get_bootstrap_status_api_v1_admin_bootstrap_status_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BootstrapStatus'
          description: Successful Response
      summary: Report whether the first-run bootstrap admin password is still active.
      tags:
      - admin
  /api/v1/admin/config:
    get:
      description: Return the gateway's sanitized current config (D0.5).
      operationId: get_admin_config_api_v1_admin_config_get
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Get Admin Config Api V1 Admin Config Get
                type: object
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Get Admin Config
      tags:
      - admin
  /api/v1/admin/ingest-health:
    get:
      description: 'GET /api/v1/admin/ingest-health — aggregate ingest-status counts
        (admin-only).


        The endpoint is read-only and stateless. Two queries: one grouped

        aggregate over ``documents.ingest_status``, one filtered count over

        ``files.ingestion_status``. Soft-deleted rows are excluded from

        both (the operator is asking about the live deployment surface, not

        historical state).'
      operationId: get_ingest_health_api_v1_admin_ingest_health_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngestHealthResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Get Ingest Health
      tags:
      - admin
  /api/v1/admin/intake-bridges:
    get:
      description: 'Return live (non-soft-deleted) Slack workspaces + Teams tenants.


        Sorted by ``installed_at DESC`` within each section so the most

        recently connected bridge surfaces first.'
      operationId: list_intake_bridges_api_v1_admin_intake_bridges_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntakeBridgesList'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List Intake Bridges
      tags:
      - admin-intake-bridges
  /api/v1/admin/intake-bridges/slack/{workspace_id}:
    delete:
      description: 'Soft-delete a Slack workspace by api-side id.


        Sets ``deleted_at`` to now. The row stays in the DB so a re-install

        via the slack-bridge OAuth flow revives it in place (per the

        M3-D1 upsert semantics) rather than losing the install history.


        ``response_class=Response`` + explicit ``Response(status_code=204)``

        is the M3-C2 pattern — without it, FastAPI''s default JSONResponse

        fails the import-time "204 must not have body" assertion and

        blows up the whole test suite at collection.'
      operationId: soft_delete_slack_workspace_api_v1_admin_intake_bridges_slack__workspace_id__delete
      parameters:
      - in: path
        name: workspace_id
        required: true
        schema:
          format: uuid
          title: Workspace Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft Delete Slack Workspace
      tags:
      - admin-intake-bridges
  /api/v1/admin/intake-bridges/teams/{tenant_id}:
    delete:
      description: 'Soft-delete a Teams tenant by api-side id.


        Same posture as ``soft_delete_slack_workspace`` — the row stays so

        a re-install via the teams-bridge OAuth flow revives it in place.'
      operationId: soft_delete_teams_tenant_api_v1_admin_intake_bridges_teams__tenant_id__delete
      parameters:
      - in: path
        name: tenant_id
        required: true
        schema:
          format: uuid
          title: Tenant Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft Delete Teams Tenant
      tags:
      - admin-intake-bridges
  /api/v1/admin/mcp:
    get:
      description: GET /api/v1/admin/mcp — list configured MCP servers + cached tools.
      operationId: list_mcp_api_v1_admin_mcp_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPServersResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List Mcp
      tags:
      - admin
  /api/v1/admin/mcp/{server}/refresh:
    post:
      description: 'POST /api/v1/admin/mcp/{server}/refresh — re-discover tools; audited.


        Covers ``none`` and ``bearer`` MCP servers only.  OAuth servers are

        per-user — discovery and refresh happen on the user-scoped OAuth-connect

        path (``/api/v1/mcp/oauth/{server}/authorize``).  Calling this endpoint

        for an ``auth: oauth`` server raises a typed

        :class:`~app.errors.MCPAuthorizationRequired` (409) so the caller knows

        to redirect to the user-scoped flow rather than receiving a silent failure.'
      operationId: refresh_mcp_api_v1_admin_mcp__server__refresh_post
      parameters:
      - in: path
        name: server
        required: true
        schema:
          title: Server
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPRefreshResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Refresh Mcp
      tags:
      - admin
  /api/v1/admin/mcp/{server}/tools/{tool}:
    patch:
      description: PATCH /api/v1/admin/mcp/{server}/tools/{tool} — toggle enabled;
        audited.
      operationId: set_mcp_tool_enabled_api_v1_admin_mcp__server__tools__tool__patch
      parameters:
      - in: path
        name: server
        required: true
        schema:
          title: Server
          type: string
      - in: path
        name: tool
        required: true
        schema:
          title: Tool
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MCPToolEnableRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPToolView'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Set Mcp Tool Enabled
      tags:
      - admin
  /api/v1/admin/provider-keys:
    get:
      description: List provider-key status via the gateway. No secret is returned.
      operationId: list_provider_keys_api_v1_admin_provider_keys_get
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response List Provider Keys Api V1 Admin Provider Keys Get
                type: object
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List Provider Keys
      tags:
      - admin
    post:
      description: Set/replace a provider's runtime key. 400 / 404 propagate from
        the gateway.
      operationId: set_provider_key_api_v1_admin_provider_keys_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProviderKeySetRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Set Provider Key Api V1 Admin Provider Keys Post
                type: object
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Set Provider Key
      tags:
      - admin
  /api/v1/admin/provider-keys/{provider}:
    delete:
      description: 'Revoke a provider''s runtime key. 404 / 409 propagate from the
        gateway.


        Uses the canonical DELETE-204 recipe (``response_class=Response`` plus an

        explicit empty ``Response`` return) so the 204 carries a genuinely empty

        body.'
      operationId: revoke_provider_key_api_v1_admin_provider_keys__provider__delete
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          title: Provider
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Revoke Provider Key
      tags:
      - admin
    patch:
      description: Rotate a provider's runtime key. 400 / 404 propagate from the gateway.
      operationId: rotate_provider_key_api_v1_admin_provider_keys__provider__patch
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          title: Provider
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProviderKeyRotateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Rotate Provider Key Api V1 Admin Provider Keys  Provider  Patch
                type: object
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Rotate Provider Key
      tags:
      - admin
  /api/v1/admin/teams:
    get:
      operationId: list_teams_api_v1_admin_teams_get
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TeamSummary'
                title: Response List Teams Api V1 Admin Teams Get
                type: array
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List every team in the deployment (admin)
      tags:
      - admin
      - teams
    post:
      operationId: create_team_api_v1_admin_teams_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamCreate'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Create a new team (admin)
      tags:
      - admin
      - teams
  /api/v1/admin/teams/{team_id}:
    delete:
      operationId: delete_team_api_v1_admin_teams__team_id__delete
      parameters:
      - in: path
        name: team_id
        required: true
        schema:
          format: uuid
          title: Team Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Delete a team (admin); CASCADES to memberships + team-scope skills
      tags:
      - admin
      - teams
    get:
      operationId: get_team_api_v1_admin_teams__team_id__get
      parameters:
      - in: path
        name: team_id
        required: true
        schema:
          format: uuid
          title: Team Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fetch a team with its membership roster (admin)
      tags:
      - admin
      - teams
    patch:
      operationId: update_team_api_v1_admin_teams__team_id__patch
      parameters:
      - in: path
        name: team_id
        required: true
        schema:
          format: uuid
          title: Team Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update a team's name or description (admin)
      tags:
      - admin
      - teams
  /api/v1/admin/teams/{team_id}/members:
    post:
      operationId: add_member_api_v1_admin_teams__team_id__members_post
      parameters:
      - in: path
        name: team_id
        required: true
        schema:
          format: uuid
          title: Team Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamMemberAdd'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamMemberResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Add a user to a team (admin)
      tags:
      - admin
      - teams
  /api/v1/admin/teams/{team_id}/members/{user_id}:
    delete:
      operationId: remove_member_api_v1_admin_teams__team_id__members__user_id__delete
      parameters:
      - in: path
        name: team_id
        required: true
        schema:
          format: uuid
          title: Team Id
          type: string
      - in: path
        name: user_id
        required: true
        schema:
          format: uuid
          title: User Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Remove a user from a team (admin)
      tags:
      - admin
      - teams
    patch:
      operationId: update_member_role_api_v1_admin_teams__team_id__members__user_id__patch
      parameters:
      - in: path
        name: team_id
        required: true
        schema:
          format: uuid
          title: Team Id
          type: string
      - in: path
        name: user_id
        required: true
        schema:
          format: uuid
          title: User Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamMemberRoleUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamMemberResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Change a member's role within a team (admin)
      tags:
      - admin
      - teams
  /api/v1/admin/tier-policy:
    get:
      description: 'GET /api/v1/admin/tier-policy — operator''s tier policy (admin-only).


        Returns the same shape as ``GET /api/v1/inference/tier-config``;

        the admin variant exists so the admin UI can render the policy on

        the operator console without granting the read endpoint to every

        user (it''s already user-accessible at the non-admin URL, but

        surfacing it under /admin keeps the operator UI''s permission model

        clean).'
      operationId: get_tier_policy_api_v1_admin_tier_policy_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TierPolicyResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Get Tier Policy
      tags:
      - admin
    patch:
      description: 'PATCH /api/v1/admin/tier-policy — partial-update the tier policy.


        Proxies through to the gateway''s PATCH /admin/v1/tier-config (which

        atomically rewrites ``gateway.yaml`` and reloads the live snapshot).

        Writes a ``tier_policy.updated`` audit row with before/after values

        so a privacy auditor can reconstruct the policy history.'
      operationId: update_tier_policy_api_v1_admin_tier_policy_patch
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TierPolicyPatchRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TierPolicyResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update Tier Policy
      tags:
      - admin
  /api/v1/admin/tool-providers:
    get:
      description: List authority-source status via the gateway. No secret is returned.
      operationId: list_tool_providers_api_v1_admin_tool_providers_get
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response List Tool Providers Api V1 Admin Tool Providers Get
                type: object
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List Tool Providers
      tags:
      - admin
    post:
      description: Enable an authority source (+optional key). 400/404 propagate from
        gateway.
      operationId: set_tool_provider_api_v1_admin_tool_providers_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ToolProviderSetRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Set Tool Provider Api V1 Admin Tool Providers Post
                type: object
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Set Tool Provider
      tags:
      - admin
  /api/v1/admin/tool-providers/{provider_type}:
    delete:
      description: 'Disable an authority source. 404/409 propagate from the gateway.


        Uses the canonical DELETE-204 recipe (``response_class=Response`` plus an

        explicit empty ``Response`` return) so the 204 carries a genuinely empty

        body.'
      operationId: disable_tool_provider_api_v1_admin_tool_providers__provider_type__delete
      parameters:
      - in: path
        name: provider_type
        required: true
        schema:
          title: Provider Type
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Disable Tool Provider
      tags:
      - admin
    patch:
      description: Rotate key and/or toggle enabled. 400/404/409 propagate from gateway.
      operationId: patch_tool_provider_api_v1_admin_tool_providers__provider_type__patch
      parameters:
      - in: path
        name: provider_type
        required: true
        schema:
          title: Provider Type
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ToolProviderPatchRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Patch Tool Provider Api V1 Admin Tool Providers  Provider
                  Type  Patch
                type: object
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Patch Tool Provider
      tags:
      - admin
  /api/v1/admin/usage:
    get:
      description: 'GET /api/v1/admin/usage — cost + tokens aggregations (Wave B /
        PRD §5.5).


        Aggregates ``inference_routing_log`` by one of: user, provider,

        model, tier, day. Filters on the same dimensions plus a date range.

        Refusals are excluded by default (they didn''t consume tokens

        upstream). All counts/sums are returned alongside a deployment-wide

        total so the admin UI can render percentages without a second

        query.'
      operationId: get_usage_api_v1_admin_usage_get
      parameters:
      - in: query
        name: group_by
        required: false
        schema:
          default: provider
          title: Group By
          type: string
      - in: query
        name: date_from
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Date From
      - in: query
        name: date_to
        required: false
        schema:
          anyOf:
          - format: date-time
            type: string
          - type: 'null'
          title: Date To
      - in: query
        name: user_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: User Id
      - in: query
        name: provider
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Provider
      - in: query
        name: model
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Model
      - in: query
        name: tier
        required: false
        schema:
          anyOf:
          - maximum: 5
            minimum: 1
            type: integer
          - type: 'null'
          title: Tier
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Get Usage
      tags:
      - admin
  /api/v1/admin/users:
    get:
      description: 'GET /api/v1/admin/users — paginated, filtered user list (admin-only).


        Returns all non-deleted users with their full auth state. Supports

        optional filtering by role and email substring. Default sort is

        ``email ASC`` for predictable alphabetical scanning. Pagination via

        ``limit`` + ``offset``; ``total_count`` is the full filtered count

        before pagination so the UI can show "Showing 50 of 187".


        Users with a pending deletion (``deletion_scheduled_at`` set) are

        included so admins can see the grace-period state and act if needed.'
      operationId: list_users_api_v1_admin_users_get
      parameters:
      - in: query
        name: role
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Role
      - in: query
        name: email_q
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Email Q
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminUserListResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List users for RBAC administration (Wave B v2 — PRD §5.2)
      tags:
      - admin
  /api/v1/admin/users/{user_id}/role:
    patch:
      description: 'PATCH /api/v1/admin/users/{user_id}/role — set RBAC role (Wave
        C).


        Admin-only. Updates ``users.role`` and keeps ``is_admin`` in sync

        (True iff role=''admin''). Idempotent: re-applying the same role

        returns 200 without writing an audit row. Writes

        ``user.role_updated`` on real changes with before/after values.


        Refuses to demote the last admin (lockout protection): if the

        target user is the only ``role=''admin''`` row in the deployment

        and the caller is trying to set them to non-admin, returns 409

        so the operator can promote someone else first.'
      operationId: update_user_role_api_v1_admin_users__user_id__role_patch
      parameters:
      - in: path
        name: user_id
        required: true
        schema:
          title: User Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRoleUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoleResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update User Role
      tags:
      - admin
  /api/v1/admin/word-addin/manifest:
    get:
      description: 'Render and return the Office Add-in manifest XML for sideload.


        The endpoint is admin-only (``AdminUser`` dep at router level via

        ``api_router``). Returns ``application/xml`` with a

        ``Content-Disposition: attachment`` header so the browser downloads

        the file rather than rendering it as XML in the tab.'
      operationId: get_manifest_api_v1_admin_word_addin_manifest_get
      parameters:
      - description: Override the deployment origin embedded in the manifest. Defaults
          to the request's effective origin (reverse-proxy aware). No trailing slash.
        in: query
        name: deployment_origin
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Override the deployment origin embedded in the manifest. Defaults
            to the request's effective origin (reverse-proxy aware). No trailing slash.
          examples:
          - https://lq.acme.example
          title: Deployment Origin
      - description: Branded name surfaced inside Word's ribbon and the task pane
          GetStarted message.
        in: query
        name: display_name
        required: false
        schema:
          default: LQ.AI
          description: Branded name surfaced inside Word's ribbon and the task pane
            GetStarted message.
          maxLength: 64
          title: Display Name
          type: string
      - description: ProviderName value the manifest surfaces to Microsoft 365 Admin
          Center; typically the operator org's name.
        in: query
        name: provider_name
        required: false
        schema:
          default: LegalQuants
          description: ProviderName value the manifest surfaces to Microsoft 365 Admin
            Center; typically the operator org's name.
          maxLength: 64
          title: Provider Name
          type: string
      responses:
        '200':
          content:
            application/xml: {}
          description: Rendered Office Add-in XML manifest.
        '403':
          description: Caller is not an admin user.
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Render the Word add-in manifest XML for sideload (M3-B1).
      tags:
      - admin
  /api/v1/auth/change-password:
    post:
      description: "POST /api/v1/auth/change-password.\n\nBearer-authenticated; the\
        \ bearer token's user is the one whose password\nis being changed. The current\
        \ password is required (defense against\na stolen access token; an attacker\
        \ who only has the token cannot\nrotate the credentials underneath the legitimate\
        \ user).\n\nBehavior:\n- Verifies `current_password` against the stored hash.\n\
        - Validates `new_password` against the configured minimum length and\n  \"\
        must differ from current\" policy.\n- Hashes and stores the new password.\n\
        - Clears `must_change_password` (the first-run forced-change flag).\n- Revokes\
        \ ALL active sessions for the user — the caller must log in\n  again with\
        \ the new password. This is intentional: it invalidates\n  any session that\
        \ may have been spawned with the old credential.\n\nReturns 204 on success,\
        \ 401 on wrong current password, 400 on policy\nviolation. The response body\
        \ for 400 names the violated rule so the\nUI can render a usable message."
      operationId: change_password_api_v1_auth_change_password_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
        required: true
      responses:
        '204':
          description: Password changed successfully
        '400':
          description: New password fails policy check
        '401':
          description: Current password is wrong
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Change the calling user's password
      tags:
      - auth
  /api/v1/auth/login:
    post:
      description: POST /api/v1/auth/login — see backend-openapi.yaml.
      operationId: login_api_v1_auth_login_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
          description: Successful Response
        '401':
          description: Invalid credentials
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
        '423':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MfaChallenge'
          description: MFA required
      summary: Authenticate with email and password
      tags:
      - auth
  /api/v1/auth/logout:
    post:
      description: 'POST /api/v1/auth/logout — bearer-authenticated.


        Revokes ALL active sessions for the calling user (conservative — see

        PRD §5.1; per-session logout can be added later if a use case

        materializes). The access token itself is stateless and remains valid

        until its TTL expires; clients are expected to discard it on logout.'
      operationId: logout_api_v1_auth_logout_post
      responses:
        '204':
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Revoke all active sessions for the calling user
      tags:
      - auth
  /api/v1/auth/mfa/disable:
    post:
      description: 'POST /api/v1/auth/mfa/disable — require password + MFA code together.


        Both checks are required so a stolen access token alone cannot

        weaken the user''s authentication posture, and an attacker who has

        the password but not the device cannot strip MFA.


        Wrong-password and wrong-code branches both return generic 401 so

        callers can''t tell which leg failed.'
      operationId: mfa_disable_api_v1_auth_mfa_disable_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MfaDisableRequest'
        required: true
      responses:
        '204':
          description: MFA disabled
        '400':
          description: MFA is not enabled
        '401':
          description: Wrong password or invalid MFA code
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Disable MFA after re-proving password and a current MFA code
      tags:
      - auth
  /api/v1/auth/mfa/enable:
    post:
      description: 'POST /api/v1/auth/mfa/enable — flip ``mfa_enabled`` after first
        verify.


        Requires that ``/mfa/setup`` has been called first (a pending

        ``totp_secret`` is on the user row). On success the user''s next

        login will receive the 423 challenge.'
      operationId: mfa_enable_api_v1_auth_mfa_enable_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MfaEnableRequest'
        required: true
      responses:
        '204':
          description: MFA enabled
        '400':
          description: Setup not started or code invalid
        '409':
          description: MFA already enabled
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Confirm TOTP enrollment with a verification code
      tags:
      - auth
  /api/v1/auth/mfa/setup:
    post:
      description: 'POST /api/v1/auth/mfa/setup — issue a fresh TOTP secret + recovery
        codes.


        Re-running setup before ``/mfa/enable`` overwrites the pending

        secret + codes (the user is restarting enrollment). Re-running

        after MFA is already enabled is a 409 ``mfa_already_enabled``;

        the user must call ``/mfa/disable`` first.'
      operationId: mfa_setup_api_v1_auth_mfa_setup_post
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MfaSetupResponse'
          description: Successful Response
        '409':
          description: MFA already enabled
      security:
      - OAuth2PasswordBearer: []
      summary: Enroll in TOTP-based MFA
      tags:
      - auth
  /api/v1/auth/mfa/verify:
    post:
      description: 'POST /api/v1/auth/mfa/verify — redeem an mfa_token + code → LoginResponse.


        On success, mints the same session shape as a regular login (the

        issuance path is shared — see ``_create_session`` and

        ``_login_response``).


        All failure modes (invalid token, expired token, missing user,

        MFA-not-enabled, wrong code) collapse into a single 401 so an

        attacker cannot probe which leg failed.'
      operationId: mfa_verify_api_v1_auth_mfa_verify_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MfaVerifyRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
          description: Successful Response
        '401':
          description: Invalid challenge token or code
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: Complete MFA challenge with TOTP or recovery code
      tags:
      - auth
  /api/v1/auth/refresh:
    post:
      description: POST /api/v1/auth/refresh — rotate the refresh token, mint a new
        access token.
      operationId: refresh_api_v1_auth_refresh_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          description: Successful Response
        '401':
          description: Invalid, expired, or revoked refresh token
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: Refresh access token using refresh token
      tags:
      - auth
  /api/v1/autonomous/memory:
    get:
      description: 'GET /api/v1/autonomous/memory


        Returns the caller''s non-deleted memory entries ordered by

        ``created_at DESC``.  Pass ``?state=proposed|kept|dismissed`` to

        filter by review state; omitting ``state`` returns all non-deleted

        entries.  Pass ``?source_session_id=`` to narrow to the memories a

        specific run proposed (the run''s "memories this run proposed" view).

        ``limit`` is clamped to [1, 200]; ``offset`` to [0, ∞).'
      operationId: list_memory_api_v1_autonomous_memory_get
      parameters:
      - in: query
        name: state
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/MemoryState'
          - type: 'null'
          title: State
      - in: query
        name: source_session_id
        required: false
        schema:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Source Session Id
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousMemoryListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the calling user's autonomous memory entries (non-deleted, newest
        first)
      tags:
      - autonomous
  /api/v1/autonomous/memory/{memory_id}:
    delete:
      description: 'DELETE /api/v1/autonomous/memory/{memory_id}


        Soft-deletes the entry by setting ``deleted_at=now(UTC)``.  Returns

        **200** with the updated (deleted) entry rather than 204 to avoid the

        FastAPI ``JSONResponse``/204 assertion pitfall (documented in

        CLAUDE.md).


        A subsequent GET excludes the entry; keep/dismiss/delete on a deleted

        entry return 404 (``_load_owned_memory`` filters ``deleted_at IS NULL``).


        Audited.'
      operationId: delete_memory_api_v1_autonomous_memory__memory_id__delete
      parameters:
      - in: path
        name: memory_id
        required: true
        schema:
          format: uuid
          title: Memory Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousMemoryRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Memory entry not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete an autonomous memory entry (returns 200 with updated entry)
      tags:
      - autonomous
  /api/v1/autonomous/memory/{memory_id}/dismiss:
    post:
      description: 'POST /api/v1/autonomous/memory/{memory_id}/dismiss


        Transitions ``proposed`` or ``kept`` → ``dismissed``.


        Returns the updated entry.  Audited.'
      operationId: dismiss_memory_api_v1_autonomous_memory__memory_id__dismiss_post
      parameters:
      - in: path
        name: memory_id
        required: true
        schema:
          format: uuid
          title: Memory Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousMemoryRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Memory entry not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Dismiss an autonomous memory entry
      tags:
      - autonomous
  /api/v1/autonomous/memory/{memory_id}/keep:
    post:
      description: 'POST /api/v1/autonomous/memory/{memory_id}/keep


        Transitions ``proposed`` or ``dismissed`` → ``kept``.  If

        ``body.content`` is provided, overwrites the entry''s text (edit-on-keep).


        **Re-keep semantics:** if the entry is already ``kept``, the action is

        allowed — content is updated if provided; ``kept_at`` is left as-is

        (preserves the original keep timestamp).


        Returns the updated entry.  Audited.'
      operationId: keep_memory_api_v1_autonomous_memory__memory_id__keep_post
      parameters:
      - in: path
        name: memory_id
        required: true
        schema:
          format: uuid
          title: Memory Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/MemoryKeepRequest'
              - type: 'null'
              title: Body
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousMemoryRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Memory entry not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Keep (approve) an autonomous memory entry; optional edit-on-keep
      tags:
      - autonomous
  /api/v1/autonomous/notifications:
    get:
      description: 'GET /api/v1/autonomous/notifications


        Returns the caller''s notifications ordered by ``created_at DESC``.

        Pass ``?unread=true`` to narrow to unread rows (``read_at IS NULL``);

        omitting it returns all the caller''s notifications. ``limit`` is

        clamped to [1, 200]; ``offset`` to [0, ∞).'
      operationId: list_notifications_api_v1_autonomous_notifications_get
      parameters:
      - in: query
        name: unread
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Unread
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousNotificationListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the calling user's autonomous notifications (newest first)
      tags:
      - autonomous
  /api/v1/autonomous/notifications/{notification_id}/read:
    post:
      description: 'POST /api/v1/autonomous/notifications/{notification_id}/read


        Sets ``read_at=now(UTC)`` if currently NULL. **Idempotent:** re-reading

        an already-read notification preserves the original ``read_at``. "Read"

        IS the dismiss action — the model has no ``dismissed_at``; a read

        notification drops out of the ``?unread=true`` list.


        Another user''s ``notification_id`` returns 404 (not 403) to avoid

        existence disclosure. Audited.'
      operationId: read_notification_api_v1_autonomous_notifications__notification_id__read_post
      parameters:
      - in: path
        name: notification_id
        required: true
        schema:
          format: uuid
          title: Notification Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousNotificationRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Notification not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Mark an autonomous notification read (the dismiss action; idempotent)
      tags:
      - autonomous
  /api/v1/autonomous/precedents:
    get:
      description: 'GET /api/v1/autonomous/precedents


        Returns the caller''s non-dismissed precedent entries (``dismissed_at

        IS NULL``) ordered by ``created_at DESC``.  Pass ``?pattern_kind=`` to

        filter to one classifier; omitting it returns all non-dismissed

        entries.  ``limit`` is clamped to [1, 200]; ``offset`` to [0, ∞).'
      operationId: list_precedents_api_v1_autonomous_precedents_get
      parameters:
      - in: query
        name: pattern_kind
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Pattern Kind
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrecedentEntryListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the calling user's precedent entries (non-dismissed, newest first)
      tags:
      - autonomous
  /api/v1/autonomous/precedents/{precedent_id}/dismiss:
    post:
      description: 'POST /api/v1/autonomous/precedents/{precedent_id}/dismiss


        Sets ``dismissed_at=now(UTC)`` so the entry drops out of the board.


        **Idempotent:** re-dismissing leaves the original ``dismissed_at``

        untouched (the entry is still loadable — ``_load_owned_precedent``

        does not filter dismissed rows).


        Another user''s ``precedent_id`` returns 404.  Audited.'
      operationId: dismiss_precedent_api_v1_autonomous_precedents__precedent_id__dismiss_post
      parameters:
      - in: path
        name: precedent_id
        required: true
        schema:
          format: uuid
          title: Precedent Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrecedentEntryRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Precedent entry not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Dismiss a precedent entry (idempotent)
      tags:
      - autonomous
  /api/v1/autonomous/precedents/{precedent_id}/promote:
    post:
      description: 'POST /api/v1/autonomous/precedents/{precedent_id}/promote


        Creates a ``proposed`` :class:`ProjectContextProposal` linking the

        precedent to ``body.project_id``.  The ``suggested_md`` snippet is

        **derived server-side** from the precedent''s ``summary``.


        This endpoint does **NOT** mutate ``projects.context_md`` — promotion

        is a proposal only; the user accepting it (``…/accept``) performs the

        authorized write (ADR 0013 D5).


        Another user''s ``precedent_id`` — or a ``project_id`` the caller does

        not own — returns 404.  Audited.'
      operationId: promote_precedent_api_v1_autonomous_precedents__precedent_id__promote_post
      parameters:
      - in: path
        name: precedent_id
        required: true
        schema:
          format: uuid
          title: Precedent Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PromotePrecedentRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectContextProposalRead'
          description: Proposal created
        '401':
          description: Not authenticated
        '404':
          description: Precedent or target project not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Propose promoting a precedent into a Project's context (proposal only)
      tags:
      - autonomous
  /api/v1/autonomous/project-context-proposals:
    get:
      description: 'GET /api/v1/autonomous/project-context-proposals


        Returns the caller''s proposals ordered by ``created_at DESC``.  Pass

        ``?state=proposed|accepted|rejected`` and/or ``?project_id=`` to

        filter.  ``limit`` is clamped to [1, 200]; ``offset`` to [0, ∞).'
      operationId: list_project_context_proposals_api_v1_autonomous_project_context_proposals_get
      parameters:
      - in: query
        name: state
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/ProposalState'
          - type: 'null'
          title: State
      - in: query
        name: project_id
        required: false
        schema:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Project Id
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectContextProposalListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the calling user's project-context proposals (newest first)
      tags:
      - autonomous
  /api/v1/autonomous/project-context-proposals/{proposal_id}/accept:
    post:
      description: 'POST /api/v1/autonomous/project-context-proposals/{proposal_id}/accept


        **The user-authorized write (ADR 0013 D5):** appends the proposal''s

        ``suggested_md`` to the target Project''s ``context_md`` (initializing

        it if NULL), sets ``state=''accepted''`` and ``accepted_at=now(UTC)``.


        **One-shot context append (guarded by ``accepted_at``):** the

        ``context_md`` write fires at most once per proposal lifetime —

        gated on whether the proposal has EVER been accepted

        (``accepted_at IS NULL``), NOT on its current ``state``. This is what

        makes accept→reject→accept safe: a ``rejected → accepted`` transition

        re-records ``state=''accepted''`` but does **NOT** re-append (C1 fix).

        On a re-accept the project-ownership load is skipped because no write

        happens — there is nothing to authorize.


        **``reject`` does NOT retroactively remove already-appended context:**

        an accepted-then-rejected proposal leaves its text in ``context_md``;

        removal/undo is out of scope here.


        Another user''s ``proposal_id`` returns 404.  Audited.'
      operationId: accept_project_context_proposal_api_v1_autonomous_project_context_proposals__proposal_id__accept_post
      parameters:
      - in: path
        name: proposal_id
        required: true
        schema:
          format: uuid
          title: Proposal Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectContextProposalRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Proposal not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Accept a proposal — append the suggested context to the Project (user-authorized
        write)
      tags:
      - autonomous
  /api/v1/autonomous/project-context-proposals/{proposal_id}/reject:
    post:
      description: 'POST /api/v1/autonomous/project-context-proposals/{proposal_id}/reject


        Sets ``state=''rejected''`` and ``rejected_at=now(UTC)``.  Does **NOT**

        touch ``projects.context_md``.


        Another user''s ``proposal_id`` returns 404.  Audited.'
      operationId: reject_project_context_proposal_api_v1_autonomous_project_context_proposals__proposal_id__reject_post
      parameters:
      - in: path
        name: proposal_id
        required: true
        schema:
          format: uuid
          title: Proposal Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectContextProposalRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Proposal not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Reject a proposal (does not touch Project context)
      tags:
      - autonomous
  /api/v1/autonomous/run-now:
    post:
      description: 'POST /api/v1/autonomous/run-now


        Spawn a single ``trigger_kind=''manual''`` session so the user can test

        a skill/playbook and inspect its receipt before arming a schedule or

        watch. Gated by opt-in (``AutonomousEnabledUser``); the spawned

        session runs under the same R4/R5/R6 brakes as every other session.

        Audited.'
      operationId: run_now_api_v1_autonomous_run_now_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AutonomousManualRunRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousSessionRead'
          description: Session spawned
        '401':
          description: Not authenticated
        '403':
          description: Autonomous layer not enabled for this user
        '404':
          description: Referenced project, playbook, or knowledge base not found
        '422':
          description: Invalid target (need exactly one of playbook_id/skill_ref)
      security:
      - OAuth2PasswordBearer: []
      summary: Run a skill or playbook once now (one-off manual autonomous session)
      tags:
      - autonomous
  /api/v1/autonomous/schedules:
    get:
      description: 'GET /api/v1/autonomous/schedules


        Returns the caller''s non-deleted schedules ordered by

        ``created_at DESC``.  Pass ``?enabled=true|false`` to filter; omitting

        it returns all non-deleted schedules.  ``limit`` is clamped to

        [1, 200]; ``offset`` to [0, ∞).'
      operationId: list_schedules_api_v1_autonomous_schedules_get
      parameters:
      - in: query
        name: enabled
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousScheduleListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the calling user's autonomous schedules (non-deleted, newest first)
      tags:
      - autonomous
    post:
      description: 'POST /api/v1/autonomous/schedules


        Validates ``cron_expr`` via :func:`~app.autonomous.cron.validate_cron_expr`

        (invalid → 422), creates the schedule row, and seeds

        ``next_run_at = next_run_after(cron_expr, now(UTC))`` so the

        dispatcher''s first eligible tick can pick it up.  Non-null FK targets

        are validated (DE-322): ``project_id`` and ``target_kb_id`` must be

        owned by the caller; ``playbook_id`` must be visible to the caller

        (own or built-in) — otherwise 404.  Returns the created

        :class:`~app.schemas.autonomous.AutonomousScheduleRead` (201).


        Audited.'
      operationId: create_schedule_api_v1_autonomous_schedules_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AutonomousScheduleCreate'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousScheduleRead'
          description: Schedule created
        '401':
          description: Not authenticated
        '404':
          description: Referenced project, playbook, or knowledge base not found
        '422':
          description: Invalid cron expression
      security:
      - OAuth2PasswordBearer: []
      summary: Create an autonomous schedule (cron-triggered run definition)
      tags:
      - autonomous
  /api/v1/autonomous/schedules/{schedule_id}:
    delete:
      description: 'DELETE /api/v1/autonomous/schedules/{schedule_id}


        Soft-deletes the schedule by setting ``deleted_at=now(UTC)``.  Returns

        **200** with the updated (deleted) entity rather than 204 to avoid the

        FastAPI ``JSONResponse``/204 assertion pitfall (documented in

        CLAUDE.md).


        A subsequent GET excludes the schedule; patch/delete on a deleted

        schedule return 404 (``_load_owned_schedule`` filters

        ``deleted_at IS NULL``).  The dispatcher also skips soft-deleted rows.


        Another user''s ``schedule_id`` returns 404.  Audited.'
      operationId: delete_schedule_api_v1_autonomous_schedules__schedule_id__delete
      parameters:
      - in: path
        name: schedule_id
        required: true
        schema:
          format: uuid
          title: Schedule Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousScheduleRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Schedule not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete an autonomous schedule (returns 200 with updated entity)
      tags:
      - autonomous
    patch:
      description: 'PATCH /api/v1/autonomous/schedules/{schedule_id}


        Partial update — only the provided fields change.  If ``cron_expr``

        changes it is re-validated (invalid → 422) and ``next_run_at`` is

        recomputed from ``now(UTC)``.  Toggling ``enabled`` is allowed.


        Another user''s ``schedule_id`` returns 404.  Returns the updated

        schedule (200).  Audited.'
      operationId: update_schedule_api_v1_autonomous_schedules__schedule_id__patch
      parameters:
      - in: path
        name: schedule_id
        required: true
        schema:
          format: uuid
          title: Schedule Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AutonomousScheduleUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousScheduleRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Schedule, referenced project, playbook, or knowledge base not
            found
        '422':
          description: Invalid cron expression
      security:
      - OAuth2PasswordBearer: []
      summary: Partially update an autonomous schedule (edit / enable / disable)
      tags:
      - autonomous
  /api/v1/autonomous/sessions:
    get:
      description: 'GET /api/v1/autonomous/sessions


        Returns the caller''s sessions ordered by ``created_at DESC``.

        ``limit`` is clamped to [1, 200]; ``offset`` to [0, ∞).'
      operationId: list_sessions_api_v1_autonomous_sessions_get
      parameters:
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousSessionListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the calling user's autonomous sessions (newest first, paginated)
      tags:
      - autonomous
  /api/v1/autonomous/sessions/{session_id}:
    get:
      description: 'GET /api/v1/autonomous/sessions/{session_id}


        Returns the session plus a live-reconstructed receipt (built from

        audit rows on every request — works for running and completed

        sessions). A completed session also has the receipt persisted in

        ``result``.


        Another user''s ``session_id`` returns 404 (not 403) to avoid

        existence disclosure.'
      operationId: get_session_api_v1_autonomous_sessions__session_id__get
      parameters:
      - in: path
        name: session_id
        required: true
        schema:
          format: uuid
          title: Session Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousSessionDetailResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Session not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fetch a single autonomous session with its full receipt
      tags:
      - autonomous
  /api/v1/autonomous/sessions/{session_id}/artifacts:
    get:
      description: 'GET /api/v1/autonomous/sessions/{session_id}/artifacts


        Returns the run''s persisted artifact references (the ``emit_artifact``

        chokepoint work-product, Donna ask #8) ordered by ``created_at ASC,

        id ASC``. Rows a run emits in its single executor commit typically

        share one ``created_at`` (transaction-stable ``now()``), so ``id`` is

        the deterministic tiebreaker that keeps LIMIT/OFFSET pagination

        stable — a repeatable order, NOT a guaranteed emission sequence (ids

        are random UUIDs). This differs intentionally from the newest-first

        autonomous lists: these are one run''s output, not a feed (mirrors the

        findings read above).


        Owner-gated by loading the owned session first (the artifacts table

        has no ``user_id`` — authz is via the parent session). Another user''s

        ``session_id`` — or a missing one — returns 404 (not 403) to avoid

        existence disclosure. ``limit`` is clamped to [1, 200]; ``offset`` to

        [0, ∞).


        ``document_id`` is NOT a column on ``autonomous_artifacts`` — it is

        enriched here with one batched query over the page''s non-null

        ``file_id`` values against the unique ``documents.file_id`` (1:1), so

        the UI can deep-link the KB document.


        Deletion semantics: a hard file-delete SET-NULLs ``file_id`` (the

        name/size metadata survives here; ``file_id`` and ``document_id``

        return as null). Deleting the *session* CASCADE-deletes these

        reference rows but never touches the KB document — the document

        outlives the session (it is the user''s deliverable).'
      operationId: list_session_artifacts_api_v1_autonomous_sessions__session_id__artifacts_get
      parameters:
      - in: path
        name: session_id
        required: true
        schema:
          format: uuid
          title: Session Id
          type: string
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousArtifactListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Session not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List a session's persisted document-grade artifacts (work-product,
        stable order)
      tags:
      - autonomous
  /api/v1/autonomous/sessions/{session_id}/findings:
    get:
      description: 'GET /api/v1/autonomous/sessions/{session_id}/findings


        Returns the run''s persisted findings (the ``emit_finding`` chokepoint

        work-product) ordered by ``created_at ASC, id ASC``. Rows a run emits

        in its single executor commit typically share one ``created_at``

        (server-side ``now()`` is transaction-stable in Postgres), so ``id``

        is the deterministic tiebreaker that keeps LIMIT/OFFSET pagination

        stable — a repeatable order, NOT a guaranteed emission sequence (ids

        are random UUIDs). This still differs intentionally from the

        newest-first autonomous lists: these are one run''s output, not a feed.


        Owner-gated by loading the owned session first (the findings table has

        no ``user_id`` — authz is via the parent session). Another user''s

        ``session_id`` — or a missing one — returns 404 (not 403) to avoid

        existence disclosure. ``limit`` is clamped to [1, 200]; ``offset`` to

        [0, ∞).'
      operationId: list_session_findings_api_v1_autonomous_sessions__session_id__findings_get
      parameters:
      - in: path
        name: session_id
        required: true
        schema:
          format: uuid
          title: Session Id
          type: string
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousFindingListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Session not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List a session's persisted findings (work-product, stable order)
      tags:
      - autonomous
  /api/v1/autonomous/sessions/{session_id}/halt:
    post:
      description: 'POST /api/v1/autonomous/sessions/{session_id}/halt


        Sets ``halt_state = ''halt_requested''`` so the next

        :func:`~app.autonomous.guard.guarded_tool_call` on the session''s

        R5 temporal brake trips and the executor transitions to ``halted``.


        **Idempotent:** if ``halt_state`` is already ``halt_requested`` or

        ``halted``, the endpoint returns the current session state with 200

        and writes NO duplicate audit row — callers may retry safely.


        Returns the updated :class:`~app.schemas.autonomous.AutonomousSessionRead`.'
      operationId: halt_session_api_v1_autonomous_sessions__session_id__halt_post
      parameters:
      - in: path
        name: session_id
        required: true
        schema:
          format: uuid
          title: Session Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousSessionRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Session not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Request an immediate halt for an autonomous session (idempotent)
      tags:
      - autonomous
  /api/v1/autonomous/sessions/{session_id}/ledger:
    get:
      description: 'GET /api/v1/autonomous/sessions/{session_id}/ledger


        Returns ``{chat_id, entries, gates}`` — the identical shape produced by

        the chat ``GET /{chat_id}/ledger`` endpoint — so the PR2-UI can reuse

        the chat ledger component directly without adaptation.  For a session

        ledger ``gates`` always has exactly one element (the single fiduciary-

        grade gate manufactured by ``build_session_ledger``).


        Reuses ``resolve_ledger_entries`` and ``resolve_gates`` — the same

        functions the chat ledger endpoint calls.


        Owner-gated via ``_load_session_for_reader``; a non-privileged non-owner''s

        ``session_id`` or a missing session returns 404 (not 403) to avoid

        existence disclosure. A privileged reader (admin/auditor) may read

        another user''s session ledger; that cross-user read writes one

        ``auditor_audit`` row (``session_ledger_viewed``). Returns 404 if the

        session exists but ``build_session_ledger`` has not been called yet

        (no hidden chat manufactured).'
      operationId: get_session_ledger_api_v1_autonomous_sessions__session_id__ledger_get
      parameters:
      - in: path
        name: session_id
        required: true
        schema:
          format: uuid
          title: Session Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Get Session Ledger Api V1 Autonomous Sessions  Session
                  Id  Ledger Get
                type: object
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Session not found or has no ledger
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Citation ledger + fiduciary gate for an autonomous session (WS-D PR2
        C5)
      tags:
      - autonomous
  /api/v1/autonomous/watches:
    get:
      description: 'GET /api/v1/autonomous/watches


        Returns the caller''s non-deleted watches ordered by

        ``created_at DESC``.  Pass ``?enabled=true|false`` and/or

        ``?knowledge_base_id=`` to filter; omitting them returns all

        non-deleted watches.  ``limit`` is clamped to [1, 200]; ``offset`` to

        [0, ∞).'
      operationId: list_watches_api_v1_autonomous_watches_get
      parameters:
      - in: query
        name: enabled
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
      - in: query
        name: knowledge_base_id
        required: false
        schema:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: Knowledge Base Id
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          default: 0
          title: Offset
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousWatchListResponse'
          description: Successful Response
        '401':
          description: Not authenticated
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the calling user's autonomous watches (non-deleted, newest first)
      tags:
      - autonomous
    post:
      description: 'POST /api/v1/autonomous/watches


        Validates the caller **owns** the target ``knowledge_base_id``

        (``KnowledgeBase.owner_id == user.id``) — a KB the caller cannot see

        returns 404 (conservative per-user isolation; KB-sharing is out of

        scope). A non-null ``playbook_id`` must be visible to the caller

        (own or built-in; DE-322) — otherwise 404. Creates the watch row.

        Returns the created

        :class:`~app.schemas.autonomous.AutonomousWatchRead` (201).


        Audited.'
      operationId: create_watch_api_v1_autonomous_watches_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AutonomousWatchCreate'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousWatchRead'
          description: Watch created
        '401':
          description: Not authenticated
        '404':
          description: Target knowledge base, referenced project, or playbook not
            found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Create an autonomous watch (KB-arrival-triggered run definition)
      tags:
      - autonomous
  /api/v1/autonomous/watches/{watch_id}:
    delete:
      description: 'DELETE /api/v1/autonomous/watches/{watch_id}


        Soft-deletes the watch by setting ``deleted_at=now(UTC)``.  Returns

        **200** with the updated (deleted) entity rather than 204 to avoid the

        FastAPI ``JSONResponse``/204 assertion pitfall (documented in

        CLAUDE.md).


        A subsequent GET excludes the watch; patch/delete on a deleted watch

        return 404 (``_load_owned_watch`` filters ``deleted_at IS NULL``). The

        KB-arrival trigger also skips soft-deleted rows.


        Another user''s ``watch_id`` returns 404.  Audited.'
      operationId: delete_watch_api_v1_autonomous_watches__watch_id__delete
      parameters:
      - in: path
        name: watch_id
        required: true
        schema:
          format: uuid
          title: Watch Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousWatchRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Watch not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete an autonomous watch (returns 200 with updated entity)
      tags:
      - autonomous
    patch:
      description: 'PATCH /api/v1/autonomous/watches/{watch_id}


        Partial update — only the provided fields change (``enabled`` /

        ``playbook_id`` / ``skill_ref``). The watch''s ``knowledge_base_id``

        is immutable: a watch is bound to its KB, so retargeting to a

        different KB means creating a new watch.


        Another user''s ``watch_id`` returns 404.  Returns the updated watch

        (200).  Audited.'
      operationId: update_watch_api_v1_autonomous_watches__watch_id__patch
      parameters:
      - in: path
        name: watch_id
        required: true
        schema:
          format: uuid
          title: Watch Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AutonomousWatchUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutonomousWatchRead'
          description: Successful Response
        '401':
          description: Not authenticated
        '404':
          description: Watch, referenced project, or playbook not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Partially update an autonomous watch (enable / disable / retarget)
      tags:
      - autonomous
  /api/v1/chats:
    get:
      description: Returns the caller's active chats by default. ``archived=true``
        returns archived chats only. ``project_id`` filters to chats inside a specific
        project. ``cursor`` and ``limit`` paginate; ``next_cursor`` in the response
        carries the next page's cursor (null when exhausted).
      operationId: list_chats_api_v1_chats_get
      parameters:
      - description: Filter to chats inside a specific project.
        in: query
        name: project_id
        required: false
        schema:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          description: Filter to chats inside a specific project.
          title: Project Id
      - description: When true, return archived chats only.
        in: query
        name: archived
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          description: When true, return archived chats only.
          title: Archived
      - description: Opaque cursor from a previous page's `next_cursor`.
        in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Opaque cursor from a previous page's `next_cursor`.
          title: Cursor
      - description: Page size; capped at 100.
        in: query
        name: limit
        required: false
        schema:
          default: 20
          description: Page size; capped at 100.
          maximum: 100
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatListResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the caller's chats (cursor-paginated)
      tags:
      - chats
    post:
      description: Create a chat owned by the caller. ``title`` defaults to "New chat"
        when omitted; the API auto-renames the chat from the first user message's
        first 80 chars on the first POST /messages call.
      operationId: create_chat_api_v1_chats_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Create a new chat
      tags:
      - chats
  /api/v1/chats/search:
    get:
      description: 'GET /api/v1/chats/search — Postgres FTS over chats + messages.


        Wave B — PRD §1.7 acceptance criterion: "search prior chats."

        Uses ``websearch_to_tsquery`` against the ``title_tsv`` /

        ``content_tsv`` generated columns (migration 0016) so the query

        parser is the friendly Google-flavored one (no operator escaping

        required). Results are ranked by ``ts_rank_cd`` and capped at

        ``limit``.


        Owner-scoped: only the caller''s own chats + messages are searched.

        Archived chats are excluded — the search affordance is for finding

        active work, not historic cleanup.'
      operationId: search_chats_api_v1_chats_search_get
      parameters:
      - in: query
        name: q
        required: true
        schema:
          maxLength: 500
          minLength: 1
          title: Q
          type: string
      - in: query
        name: limit
        required: false
        schema:
          default: 20
          maximum: 100
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatSearchResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Full-text search across the caller's chats + messages (PRD §1.7)
      tags:
      - chats
  /api/v1/chats/{chat_id}:
    delete:
      description: 'Sets ``archived_at`` on the chat. Hard-delete is owned by D6.
        Idempotent: a second delete on an already-archived chat returns 404.'
      operationId: delete_chat_api_v1_chats__chat_id__delete
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete a chat
      tags:
      - chats
    get:
      operationId: get_chat_api_v1_chats__chat_id__get
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fetch a single chat
      tags:
      - chats
    patch:
      operationId: update_chat_api_v1_chats__chat_id__patch
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Partial update of a chat
      tags:
      - chats
  /api/v1/chats/{chat_id}/ledger:
    get:
      description: 'Return the Citation Ledger for a chat, each entry resolved to
        its source

        identity + passage(s) read + verification status + provenance (ADR 0018 D4).


        Chat-scoped; ``?message_id=`` narrows to a single assistant turn. Ownership
        is

        enforced as in :func:`get_citations` (cross-user → 404). The ledger row holds

        no content — passages are resolved from the content layer at read time (P3).'
      operationId: get_chat_ledger_api_v1_chats__chat_id__ledger_get
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      - in: query
        name: message_id
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Message Id
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: true
                title: Response Get Chat Ledger Api V1 Chats  Chat Id  Ledger Get
                type: object
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Citation Ledger for a chat (one-click trace) — P1-A3
      tags:
      - chats
  /api/v1/chats/{chat_id}/messages:
    get:
      operationId: list_messages_api_v1_chats__chat_id__messages_get
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      - description: Opaque cursor from a previous page's `next_cursor`.
        in: query
        name: cursor
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Opaque cursor from a previous page's `next_cursor`.
          title: Cursor
      - description: Page size; capped at 100.
        in: query
        name: limit
        required: false
        schema:
          default: 20
          description: Page size; capped at 100.
          maximum: 100
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageListResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List messages in a chat (cursor-paginated, oldest-first)
      tags:
      - chats
    post:
      description: 'C3: persists the user message, forwards to the gateway, persists
        the assistant message (or streams SSE chunks and persists the assistant row
        at end-of-stream). Returns either a JSON body or an SSE stream depending on
        ``stream``.'
      operationId: send_message_api_v1_chats__chat_id__messages_post
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Post a user message; persist + forward to gateway + persist response
      tags:
      - chats
  /api/v1/chats/{chat_id}/messages/{message_id}/citations:
    get:
      description: 'Return citations persisted for a message.


        M2-A2 (this version) reads from ``message_citations`` (one row per

        citation) rather than the legacy ``messages.citations`` JSONB

        column. Each citation in the response carries the verifier''s

        verdict (``verified``, ``verification_method``,

        ``verification_confidence``, ``partial``) so the UI (M2-C2) can

        render the five citation states distinctly — including the

        paraphrase-judge "partial" verdict surfaced via the ``partial``

        flag (M2-C1).


        The legacy JSONB column is kept at its ``''[]''`` default by the

        chat-send path; readers should consume this endpoint, not the

        column. The column itself remains for backward compatibility with

        older clients and is slated for retirement by M2-C2.


        Owner-of-the-chat, admin, or the read-only cross-user ``auditor`` role

        can read (``_load_chat_for_reader``); everyone else — and a missing

        chat — gets an identical 404 (existence-safe), matching

        :func:`get_chat_ledger` / :func:`get_message_sources`. A privileged

        reader viewing another user''s chat writes one ``auditor_audit`` row

        (``citations_viewed``).'
      operationId: get_citations_api_v1_chats__chat_id__messages__message_id__citations_get
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      - in: path
        name: message_id
        required: true
        schema:
          title: Message Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  additionalProperties: true
                  type: object
                title: Response Get Citations Api V1 Chats  Chat Id  Messages  Message
                  Id  Citations Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: 'Get citations for a message (M2-A2: relational message_citations rows)'
      tags:
      - chats
  /api/v1/chats/{chat_id}/messages/{message_id}/sources:
    get:
      description: 'Return the external sources (case-law clusters) a message''s turn
        consulted.


        Retrieval-provenance from ``message_tool_sources`` — "sources consulted,"

        distinct from the verified quote rows of ``message_citations``. Returns ``[]``

        for a turn that consulted nothing; 404 when the message doesn''t exist in
        the

        chat. Chat ownership enforced as in :func:`get_citations`.'
      operationId: get_message_sources_api_v1_chats__chat_id__messages__message_id__sources_get
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      - in: path
        name: message_id
        required: true
        schema:
          title: Message Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  additionalProperties: true
                  type: object
                title: Response Get Message Sources Api V1 Chats  Chat Id  Messages  Message
                  Id  Sources Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Get external-source provenance (case law consulted) for a message (PR6c)
      tags:
      - chats
  /api/v1/chats/{chat_id}/receipts:
    get:
      description: Wave D.1 T5 (spec §7.6). Merges chronological events from ``messages``
        (+ denorm ``applied_skills``), ``inference_routing_log``, and ``audit_log``
        into one timestamp-ordered stream. Owner-of-chat, admin, or the read-only
        cross-user ``auditor`` role. Filter with ``?event_kinds=message,audit`` etc.
        Inference/error event ``detail`` includes ``anonymization_applied`` (bool
        — the source of truth for the UI's anonymization indicator) and ``message_id``
        (the assistant message the indicator pins to; may be null).
      operationId: get_chat_receipts_api_v1_chats__chat_id__receipts_get
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          format: uuid
          title: Chat Id
          type: string
      - description: 'Comma-separated subset of: message, inference, audit, skill,
          retrieval, error. Omit for all kinds.'
        in: query
        name: event_kinds
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: 'Comma-separated subset of: message, inference, audit, skill,
            retrieval, error. Omit for all kinds.'
          title: Event Kinds
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ReceiptEvent'
                title: Response Get Chat Receipts Api V1 Chats  Chat Id  Receipts
                  Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Chronological event log for a chat (replay-at-read)
      tags:
      - chats
  /api/v1/chats/{chat_id}/receipts/export.jsonl:
    get:
      description: 'Same payload as the JSON receipts endpoint, serialized as JSONL.


        ``Content-Type: application/jsonl`` + ``Content-Disposition: attachment;

        filename="chat-{id}-receipts.jsonl"`` so browsers trigger a download.


        Authorization mirrors :func:`get_chat_receipts` (owner, admin, or

        ``auditor``); a privileged cross-user export writes its own

        ``receipts_exported`` audit row (distinct from the read endpoint''s

        ``receipts_viewed``) rather than delegating to the read handler.'
      operationId: export_chat_receipts_api_v1_chats__chat_id__receipts_export_jsonl_get
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          format: uuid
          title: Chat Id
          type: string
      - in: query
        name: event_kinds
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Event Kinds
      responses:
        '200':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Export receipts as JSONL (one event per line)
      tags:
      - chats
  /api/v1/chats/{chat_id}/tool-calls/{pending_call_id}:
    post:
      description: 'POST /{chat_id}/tool-calls/{pending_call_id} — approve or deny
        a pending tool call.


        Loads the pending row (owner-scoped, single-use), resolves the decision,

        executes the tool or feeds a denial message, then resumes run_chat_tool_loop

        and streams the outcome as SSE frames.'
      operationId: resume_tool_call_api_v1_chats__chat_id__tool_calls__pending_call_id__post
      parameters:
      - in: path
        name: chat_id
        required: true
        schema:
          title: Chat Id
          type: string
      - in: path
        name: pending_call_id
        required: true
        schema:
          title: Pending Call Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Approve or deny a pending destructive chat tool-call; resumes the turn
      tags:
      - chats
  /api/v1/enhance-prompt:
    post:
      description: 'POST /api/v1/enhance-prompt — invoke the enhance-prompt skill.


        The skill is applied via the standard gateway prompt-assembly path

        (``lq_ai_skills=[''enhance-prompt'']``), so user/team-scope shadows

        of the skill are honored per the D8.1b resolver. Returns the

        structured expansion + an interaction_id the frontend uses to

        update ``used`` / ``edited_before_use`` post-hoc.'
      operationId: enhance_prompt_api_v1_enhance_prompt_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnhancePromptRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnhancePromptResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Expand a short prompt into a structured legal prompt (PRD §3.2)
      tags:
      - enhance-prompt
  /api/v1/enhance-prompt/{interaction_id}:
    patch:
      description: 'PATCH /api/v1/enhance-prompt/{id} — update used/edited flags.


        The frontend calls this after the user acts on the preview:

        * Submit as-is → ``used=true``.

        * Edit then submit → ``used=true``, ``edited_before_use=true``.

        * Skip / dismiss → no PATCH needed (defaults stand).


        Owner-only (404 if the row exists but belongs to another user —

        same id-probing posture as the user_skills + chats endpoints).

        Idempotent: re-applying the same flags returns 200 without

        writing to the DB.'
      operationId: update_enhance_prompt_outcome_api_v1_enhance_prompt__interaction_id__patch
      parameters:
      - in: path
        name: interaction_id
        required: true
        schema:
          format: uuid
          title: Interaction Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnhancePromptOutcomeUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnhancePromptResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Record what the user did with the enhancement preview (PRD §3.2)
      tags:
      - enhance-prompt
  /api/v1/files:
    post:
      description: 'Streaming multipart upload. The body is streamed to MinIO without
        ever loading the whole file into memory; a SHA-256 is computed as bytes flow
        past, and metadata is persisted with `ingestion_status=''pending''`. The document
        pipeline (Task C5) picks the row up asynchronously and flips status through
        `processing` → `ready` (or `failed`).


        Per-request size limit: `LQ_AI_MAX_UPLOAD_SIZE_MB` (default 100). Exceeding
        it returns 413 with `code=payload_too_large`.'
      operationId: upload_file_api_v1_files_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_upload_file_api_v1_files_post'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileMetadata'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Upload a file
      tags:
      - files
  /api/v1/files/{file_id}:
    delete:
      description: 'Sets `deleted_at` on the row; the MinIO object is left in place
        and reaped later by D6 (per-user export+delete) or a future GC sweep, per
        `docs/adr/0005-file-storage-soft-delete-and-key-scheme.md`. Idempotent: deleting
        an already-soft-deleted file or a missing file returns 404.'
      operationId: delete_file_api_v1_files__file_id__delete
      parameters:
      - in: path
        name: file_id
        required: true
        schema:
          title: File Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete a file
      tags:
      - files
    get:
      description: 'Return the canonical ``File`` shape for ``file_id``.


        Returns 404 if the file does not exist, has been soft-deleted, or

        is owned by another user. (Per CLAUDE.md and the C4 brief: the

        cross-user case returns 404 rather than 403 to avoid leaking

        existence information.)'
      operationId: get_file_api_v1_files__file_id__get
      parameters:
      - in: path
        name: file_id
        required: true
        schema:
          title: File Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileMetadata'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Get file metadata
      tags:
      - files
  /api/v1/files/{file_id}/content:
    get:
      description: "Stream the original bytes from MinIO with the right headers.\n\
        \nHeaders set:\n* ``Content-Type``: from the stored ``mime_type``.\n* ``Content-Length``:\
        \ from the stored ``size_bytes``.\n* ``Content-Disposition``: ``attachment;\
        \ filename=\"...\"`` with an\n  RFC 5987 ``filename*`` for non-ASCII filenames.\n\
        * ``X-Content-Type-Options: nosniff``: defensive — clients (esp.\n  browsers)\
        \ must not infer a MIME from sniffing."
      operationId: get_file_content_api_v1_files__file_id__content_get
      parameters:
      - in: path
        name: file_id
        required: true
        schema:
          title: File Id
          type: string
      responses:
        '200':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Download original file content
      tags:
      - files
  /api/v1/inference/current-tier:
    get:
      description: 'GET /api/v1/inference/current-tier — derive tier from gateway
        config.


        The lookup runs against the gateway''s live model list (``/v1/models``)

        which already carries ``routed_inference_tier`` per entry (see

        ``GatewayConfig.iter_resolved_models``). Returns 404 when the

        (provider, model) pair isn''t configured — the UI can fall back to

        rendering "Tier unknown" rather than guessing.'
      operationId: get_current_tier_api_v1_inference_current_tier_get
      parameters:
      - in: query
        name: provider
        required: true
        schema:
          maxLength: 200
          minLength: 1
          title: Provider
          type: string
      - in: query
        name: model
        required: true
        schema:
          maxLength: 200
          minLength: 1
          title: Model
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentTierResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Derive the routed inference tier for a (provider, model) pair
      tags:
      - inference
  /api/v1/inference/override-tier-floor:
    post:
      description: Wave D.1 T4. Admin-only override that re-runs the original user
        prompt with ``tier_floor=None`` for this turn. Writes a new ``kind='ai'``
        message, an inference routing log row (via the gateway, per the B4 invariant),
        and an audit log row capturing the override reason.
      operationId: override_tier_floor_api_v1_inference_override_tier_floor_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OverrideRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverrideResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Re-run a refused inference with the tier floor lifted (admin only)
      tags:
      - inference
  /api/v1/inference/tier-config:
    get:
      description: 'GET /api/v1/inference/tier-config — operator''s tier policy.


        User-accessible (not admin-only) — the policy is a deployment-level

        disclosure the UI uses to render allowed/disallowed tier badges.

        The admin-write surface lives at ``PATCH /api/v1/admin/tier-policy``.'
      operationId: get_tier_config_api_v1_inference_tier_config_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TierConfigResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Read the deployment's tier policy (allowed tiers + minima)
      tags:
      - inference
  /api/v1/integrations/slack/workspaces:
    post:
      description: "Persist (or upsert) the workspace record from the bridge.\n\n\
        Upserts on ``team_id``. On conflict:\n  * ``team_name`` is refreshed from\
        \ the request body (operator may\n    have renamed the workspace in Slack).\n\
        \  * ``bot_token_encrypted`` is replaced (Slack rotates tokens on\n    re-install).\n\
        \  * ``bot_user_id`` is refreshed (Slack may have issued a new bot\n    user\
        \ across the re-install).\n  * ``installer_slack_user_id`` is refreshed (a\
        \ different operator\n    may be reinstalling).\n  * ``scope`` is replaced\
        \ (consent may have changed).\n  * ``deleted_at`` is set back to NULL (re-install\
        \ revives soft-\n    deleted rows).\n  * ``installed_at`` stays at the original\
        \ install time; the\n    re-install does not reset the audit timestamp. Operators\
        \ can\n    infer re-install activity from the bot-token ciphertext\n    changing\
        \ without the install timestamp moving."
      operationId: upsert_slack_workspace_api_v1_integrations_slack_workspaces_post
      parameters:
      - in: header
        name: Authorization
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Authorization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SlackWorkspaceCreate'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SlackWorkspaceResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: Upsert Slack Workspace
      tags:
      - integrations-slack
  /api/v1/integrations/teams/tenants:
    post:
      description: "Persist (or upsert) the tenant record from the teams-bridge.\n\
        \nUpserts on ``tenant_id``. On conflict:\n  * ``tenant_name`` refreshed (operator\
        \ may have renamed the org).\n  * ``installer_oid`` refreshed (a different\
        \ admin may be\n    reinstalling).\n  * ``deleted_at`` reset to NULL.\n  *\
        \ ``installed_at`` stays at the original install time."
      operationId: upsert_teams_tenant_api_v1_integrations_teams_tenants_post
      parameters:
      - in: header
        name: Authorization
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Authorization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeamsTenantCreate'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamsTenantResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: Upsert Teams Tenant
      tags:
      - integrations-teams
  /api/v1/internal/organization-profile:
    get:
      description: 'Return the Organization Profile as a synthesized Skill payload.


        Auth is the same shared-secret as ``/internal/skills/{name}``. The

        response shape is a :class:`Skill`-compatible JSON body so the

        gateway can plug it into the assembler without a special-case

        parser. ``content_md`` carries the operator-edited Markdown body;

        ``content_yaml`` carries synthesized frontmatter declaring

        ``is_organization_profile: true`` so consumers can identify the

        Profile by metadata rather than name string.


        Returns 404 when no Profile row exists OR when its body is empty —

        the gateway treats both as "no profile to prepend" and the single

        error code keeps the branch simple.'
      operationId: get_organization_profile_internal_api_v1_internal_organization_profile_get
      parameters:
      - in: header
        name: X-LQ-AI-Gateway-Key
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: X-Lq-Ai-Gateway-Key
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: Get Organization Profile Internal
      tags:
      - internal
  /api/v1/internal/skills/{skill_name}:
    get:
      description: "Return resolved skill detail for ``skill_name``.\n\nSame response\
        \ shape as ``GET /api/v1/skills/{skill_name}`` — the\nfull ``Skill`` from\
        \ ``docs/api/backend-openapi.yaml``, with\n``None``-valued optionals dropped\
        \ for compactness. Auth is via\n``X-LQ-AI-Gateway-Key`` (constant-time compare);\
        \ 401 on bad key,\n404 on unknown skill name.\n\nResolution order (ADR 0012\
        \ + D8.1b):\n\n1. ``user_id`` set + non-archived user-scope row for that user\n\
        \   at this slug → return the user shadow.\n2. ``user_id`` set + non-archived\
        \ team-scope row at this slug\n   owned by any team that user belongs to →\
        \ return the team\n   shadow. Multi-team conflicts resolve to the row with\
        \ the\n   most recent ``updated_at``.\n3. Filesystem-canonical registry →\
        \ return the built-in.\n4. 404 if none of the three match.\n\nThe gateway\
        \ calls this with the authenticated user's UUID during\nC2 prompt assembly\
        \ so shadows actually shape the system prompt;\ncallers that don't care about\
        \ user-scope (e.g., admin tooling)\nomit ``user_id`` and get the registry\
        \ view directly.\n\nCache key strategy (D8.1b decision): the gateway-side\
        \ cache stays\nkeyed on ``(name, user_id)``. Team-membership changes are\n\
        operator-mediated and rare; the existing 60s skill-cache TTL\nabsorbs propagation\
        \ lag without a per-membership signature in\nthe key. Re-evaluate if team-membership\
        \ churn becomes routine."
      operationId: get_skill_internal_api_v1_internal_skills__skill_name__get
      parameters:
      - in: path
        name: skill_name
        required: true
        schema:
          title: Skill Name
          type: string
      - in: query
        name: user_id
        required: false
        schema:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          title: User Id
      - in: header
        name: X-LQ-AI-Gateway-Key
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: X-Lq-Ai-Gateway-Key
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: Get Skill Internal
      tags:
      - internal
  /api/v1/knowledge-bases:
    get:
      operationId: list_kbs_api_v1_knowledge_bases_get
      parameters:
      - description: When true, return only archived KBs.
        in: query
        name: archived
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          description: When true, return only archived KBs.
          title: Archived
      - description: Filter by project association.
        in: query
        name: project_id
        required: false
        schema:
          anyOf:
          - format: uuid
            type: string
          - type: 'null'
          description: Filter by project association.
          title: Project Id
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/KnowledgeBaseResponse'
                title: Response List Kbs Api V1 Knowledge Bases Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the caller's knowledge bases
      tags:
      - knowledge-bases
    post:
      operationId: create_kb_api_v1_knowledge_bases_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KnowledgeBaseCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeBaseResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Create a knowledge base
      tags:
      - knowledge-bases
  /api/v1/knowledge-bases/{kb_id}:
    delete:
      operationId: delete_kb_api_v1_knowledge_bases__kb_id__delete
      parameters:
      - in: path
        name: kb_id
        required: true
        schema:
          title: Kb Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete a knowledge base
      tags:
      - knowledge-bases
    get:
      operationId: get_kb_api_v1_knowledge_bases__kb_id__get
      parameters:
      - in: path
        name: kb_id
        required: true
        schema:
          title: Kb Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeBaseResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fetch a single knowledge base
      tags:
      - knowledge-bases
    patch:
      operationId: update_kb_api_v1_knowledge_bases__kb_id__patch
      parameters:
      - in: path
        name: kb_id
        required: true
        schema:
          title: Kb Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KnowledgeBaseUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeBaseResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Partial update of a knowledge base
      tags:
      - knowledge-bases
  /api/v1/knowledge-bases/{kb_id}/files:
    get:
      description: Returns the files currently attached to the KB (the ``File`` shape
        from ``GET /files/{id}`` plus the ``attached_at`` timestamp from the join).
        Owner-scoped — cross-user / unknown KB id returns 404. Soft-deleted files
        are excluded; an attachment that points at a deleted file is filtered out
        at the join. Sorted by ``attached_at DESC`` so the most recent uploads surface
        first.
      operationId: list_kb_files_api_v1_knowledge_bases__kb_id__files_get
      parameters:
      - in: path
        name: kb_id
        required: true
        schema:
          title: Kb Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/KBFileResponse'
                title: Response List Kb Files Api V1 Knowledge Bases  Kb Id  Files
                  Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the files attached to a knowledge base
      tags:
      - knowledge-bases
    post:
      description: The file must be owned by the caller and have completed the C5
        ingest pipeline (``ingestion_status='ready'``). Files in any other state return
        422. After successful attach, an embed job is enqueued so chunks with NULL
        embeddings get vectors written; the query path also covers this lazily via
        embed-on-read.
      operationId: attach_file_api_v1_knowledge_bases__kb_id__files_post
      parameters:
      - in: path
        name: kb_id
        required: true
        schema:
          title: Kb Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/app__schemas__knowledge__AttachFileRequest'
        required: true
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Attach a file to a knowledge base
      tags:
      - knowledge-bases
  /api/v1/knowledge-bases/{kb_id}/files/{file_id}:
    delete:
      operationId: detach_file_api_v1_knowledge_bases__kb_id__files__file_id__delete
      parameters:
      - in: path
        name: kb_id
        required: true
        schema:
          title: Kb Id
          type: string
      - in: path
        name: file_id
        required: true
        schema:
          title: File Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Detach a file from a knowledge base
      tags:
      - knowledge-bases
  /api/v1/knowledge-bases/{kb_id}/query:
    post:
      operationId: query_kb_api_v1_knowledge_bases__kb_id__query_post
      parameters:
      - in: path
        name: kb_id
        required: true
        schema:
          title: Kb Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KBQueryRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KBQueryResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Hybrid (vector + FTS) search over the knowledge base
      tags:
      - knowledge-bases
  /api/v1/mcp/oauth:
    get:
      description: 'GET /api/v1/mcp/oauth — per-user OAuth connection status for all
        connectable servers.


        PR4d Ask 1.  ActiveUser-gated (bearer).  Returns one entry per configured

        OAuth-type MCP server with the calling user''s connection state

        (``connected``, ``scopes``, ``expires_at``).  Token bytes are NEVER

        returned — this is a status-only surface.'
      operationId: list_mcp_oauth_api_v1_mcp_oauth_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPOAuthServersResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List Mcp Oauth
      tags:
      - mcp-oauth
  /api/v1/mcp/oauth/{server}:
    delete:
      description: 'DELETE /api/v1/mcp/oauth/{server} — revoke stored tokens (local
        only).


        Idempotent: deleting when no token is stored still returns 204.

        Audit row written only when a row was actually removed.'
      operationId: disconnect_mcp_oauth_api_v1_mcp_oauth__server__delete
      parameters:
      - in: path
        name: server
        required: true
        schema:
          title: Server
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Disconnect Mcp Oauth
      tags:
      - mcp-oauth
  /api/v1/mcp/oauth/{server}/authorize:
    get:
      description: 'GET /api/v1/mcp/oauth/{server}/authorize — start the OAuth flow.


        Builds the PKCE authorize URL for *server* and redirects the user''s browser

        to the authorization server.  Audit happens on successful callback, not here.


        ``return_url`` (optional) is an operator-allowlisted frontend URL.  When

        supplied, the callback 302-redirects the browser back to that URL on

        completion (success or exchange error).  The origin of *return_url* MUST be

        in ``lq_ai_cors_origins``; unrecognised origins are rejected with 400 (no

        open redirect).'
      operationId: authorize_mcp_oauth_api_v1_mcp_oauth__server__authorize_get
      parameters:
      - in: path
        name: server
        required: true
        schema:
          title: Server
          type: string
      - in: query
        name: return_url
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Return Url
      responses:
        '302':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Authorize Mcp Oauth
      tags:
      - mcp-oauth
  /api/v1/mcp/oauth/{server}/callback:
    get:
      description: 'GET /api/v1/mcp/oauth/{server}/callback — receive the AS redirect.


        PUBLIC — no bearer auth.  The user is bound to the exchange via the

        single-use, TTL-bounded mcp_oauth_state row inside exchange_code.


        When a ``return_url`` was stored on the state row (set at authorize-time):

        * Success → 302 to ``{return_url}?mcp_connected={server}``

        * Exchange error → 302 to ``{return_url}?mcp_error={code}&server={server}``


        When no ``return_url`` was stored (back-compat):

        * Success → 200 ``MCPOAuthCallbackResponse``

        * Exchange error → raises the exception (existing JSON-error behaviour)


        The callback NEVER reads a redirect target from its own query string.  The

        only trusted ``return_url`` is the one read from the state row (validated

        + stored at authorize-time).  If the state row is unknown, ``return_url``

        is None and we fall back to JSON error (fail safe — no trusted target).'
      operationId: mcp_oauth_callback_api_v1_mcp_oauth__server__callback_get
      parameters:
      - in: path
        name: server
        required: true
        schema:
          title: Server
          type: string
      - in: query
        name: code
        required: true
        schema:
          title: Code
          type: string
      - in: query
        name: state
        required: true
        schema:
          title: State
          type: string
      - in: query
        name: iss
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Iss
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPOAuthCallbackResponse'
          description: Token exchanged (no return_url)
        '302':
          description: Browser redirected to return_url (return_url was supplied)
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      summary: Mcp Oauth Callback
      tags:
      - mcp-oauth
  /api/v1/mcp/oauth/{server}/status:
    get:
      description: 'GET /api/v1/mcp/oauth/{server}/status — check connection state.


        Returns whether the calling user has a stored token for *server*, plus

        the granted scopes and expiry.  Token bytes are NEVER returned.'
      operationId: status_mcp_oauth_api_v1_mcp_oauth__server__status_get
      parameters:
      - in: path
        name: server
        required: true
        schema:
          title: Server
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPOAuthStatusResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Status Mcp Oauth
      tags:
      - mcp-oauth
  /api/v1/models:
    get:
      description: "Return the gateway's merged ``GET /v1/models`` payload verbatim.\n\
        \nErrors from the gateway client propagate as :class:`LQAIError`\nsubclasses;\
        \ the global exception handler in :mod:`app.main`\ntranslates them to the\
        \ canonical ``{\"detail\": {\"code\", ...}}``\nenvelope. Specifically:\n\n\
        * Gateway 401 (bad shared-secret) → ``GatewayUnreachable`` /\n  HTTP 503 with\
        \ ``code = \"gateway_unreachable\"``. The user must not\n  see \"the operator\
        \ misconfigured the gateway key\".\n* Network failure → ``GatewayUnreachable``\
        \ / HTTP 503.\n* Timeout → ``GatewayTimeout`` / HTTP 504.\n* Structured gateway\
        \ errors → mapped per\n  :func:`app.errors.map_gateway_error_code`.\n\nThe\
        \ success body is forwarded as-is — the client side already\nunderstands the\
        \ gateway's shape (per the OpenAPI sketch)."
      operationId: list_models_api_v1_models_get
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List Models
      tags:
      - models
  /api/v1/organization-profile:
    get:
      description: 'GET /api/v1/organization-profile — readable by any authenticated
        user.


        Returns ``content_md=""`` plus null timestamps when no admin has

        set a Profile yet — the read path never 404s. The transparency

        principle (PRD §1.3) requires every user to be able to see what''s

        shaping their output; admins have the *write* side via PUT.'
      operationId: get_organization_profile_api_v1_organization_profile_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationProfileResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Get the deployment's Organization Profile
      tags:
      - organization-profile
    put:
      description: 'PUT /api/v1/organization-profile — admin upsert.


        Idempotent: if a row exists, update its ``content_md`` +

        ``updated_by`` (the trigger maintains ``updated_at``); otherwise

        insert a fresh row. The singleton index guarantees the write

        converges to a single row regardless of concurrency.


        Audit-logged via the D3 helper as ``organization_profile.updated``

        so admins can trace who changed the Profile and when.'
      operationId: put_organization_profile_api_v1_organization_profile_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationProfileUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationProfileResponse'
          description: Successful Response
        '401':
          description: Bearer token missing or invalid
        '403':
          description: Caller is not an admin
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update the Organization Profile (admin only)
      tags:
      - organization-profile
  /api/v1/organization-profile/raw:
    get:
      description: 'GET /api/v1/organization-profile/raw — content-typed Markdown.


        Convenience endpoint for the Skill Inspector (§3.4) and any UI

        surface that wants to render the Profile as Markdown without

        JSON-decoding the body. Returns an empty 200 if no Profile is

        set yet (rather than 404), consistent with the JSON GET above.'
      operationId: get_organization_profile_raw_api_v1_organization_profile_raw_get
      responses:
        '200':
          content:
            text/markdown: {}
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Get the Profile as raw Markdown (text/markdown)
      tags:
      - organization-profile
  /api/v1/playbook-executions/{execution_id}:
    get:
      description: 'Return the current state of one execution.


        Returns the row regardless of completion state: ``pending`` /

        ``running`` / ``completed`` / ``error``. Callers poll this

        endpoint until they see a terminal state (``completed`` or

        ``error``).'
      operationId: get_playbook_execution_api_v1_playbook_executions__execution_id__get
      parameters:
      - in: path
        name: execution_id
        required: true
        schema:
          format: uuid
          title: Execution Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaybookExecution'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Poll the current state of a playbook execution.
      tags:
      - playbooks
  /api/v1/playbooks:
    get:
      description: "List playbooks the caller can see.\n\nVisibility rules (mirroring\
        \ the execute endpoint):\n\n* Admins see all playbooks.\n* Non-admins see\
        \ playbooks they authored OR built-in playbooks\n  (``created_by IS NULL``\
        \ — created by the seed migration).\n\nPositions are NOT inlined in the list\
        \ response; clients fetch the\ndetail endpoint when they need them. This keeps\
        \ the list response\nbounded even when a playbook has dozens of positions."
      operationId: list_playbooks_api_v1_playbooks_get
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Playbook'
                title: Response List Playbooks Api V1 Playbooks Get
                type: array
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List playbooks visible to the caller.
      tags:
      - playbooks
    post:
      description: 'Create a playbook owned by the caller — M3-A6 Phase 2.


        Sets ``created_by = user.id`` unconditionally. Admins do not get to

        create "built-in" playbooks (``created_by IS NULL``) through this

        endpoint — built-ins ship via seed migration only (matches the

        "admins can fork built-ins but not edit them in place" posture

        from the M3-A6 prep doc).'
      operationId: create_playbook_api_v1_playbooks_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaybookCreate'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Playbook'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Create a new playbook.
      tags:
      - playbooks
  /api/v1/playbooks/easy:
    post:
      description: 'Kick off an Easy Playbook generation against the supplied document
        corpus.


        Authorization: the caller must own every document in

        ``document_ids``. Cross-user or non-existent ids collapse into a

        single 404 (no information leakage), matching the M3-A2 executor''s

        posture.


        Creates an :class:`EasyPlaybookGeneration` row at

        ``status=''pending''`` and enqueues the ARQ worker job on the

        ``arq:m3a6`` queue. Returns 202 immediately with the row id; the

        wizard''s Step 2 polls

        ``GET /api/v1/playbooks/easy/{generation_id}`` until status

        reaches ``completed`` or ``error``.


        Per the M3-A6 quality bar, "generation completed" does NOT mean

        the playbook is fit for use — the wizard''s Step 3 inline editor

        is where the user-attorney validates and edits before the final

        save (which POSTs to ``/api/v1/playbooks`` like any other

        playbook create).'
      operationId: create_easy_playbook_generation_api_v1_playbooks_easy_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EasyPlaybookGenerationCreate'
        required: true
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EasyPlaybookGeneration'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Start an Easy Playbook generation run.
      tags:
      - playbooks
  /api/v1/playbooks/easy/{generation_id}:
    get:
      description: 'Return the current state of one Easy Playbook generation.


        Caller must be the row''s ``user_id`` OR an admin. Cross-user /

        missing rows collapse into 404. The wizard''s Step 2 polls this

        endpoint every few seconds until ``status`` reaches a terminal

        state (``completed`` or ``error``).'
      operationId: get_easy_playbook_generation_api_v1_playbooks_easy__generation_id__get
      parameters:
      - in: path
        name: generation_id
        required: true
        schema:
          format: uuid
          title: Generation Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EasyPlaybookGeneration'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Poll an Easy Playbook generation row.
      tags:
      - playbooks
  /api/v1/playbooks/{playbook_id}:
    delete:
      description: "Soft-delete the playbook by setting ``deleted_at`` — M3-A6 Phase\
        \ 2.\n\nAuthorization mirrors :func:`update_playbook`:\n\n* Built-ins are\
        \ immutable (403) — operators fork built-ins; they do\n  not delete them.\n\
        * Non-admin callers must own the playbook (404 otherwise).\n* Already soft-deleted\
        \ rows return 404 (the row is invisible to all\n  callers via :func:`_load_visible_playbook`).\n\
        \nThe ``playbook_positions`` rows are NOT deleted; they continue to\nreference\
        \ the now-soft-deleted playbook. This preserves the\nrelationship for any\
        \ historical ``playbook_executions`` that\nreferenced positions by id. A future\
        \ hard-purge sweep can drop both\npositions and the tombstone playbook together."
      operationId: delete_playbook_api_v1_playbooks__playbook_id__delete
      parameters:
      - in: path
        name: playbook_id
        required: true
        schema:
          format: uuid
          title: Playbook Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete a playbook.
      tags:
      - playbooks
    get:
      description: 'Return the playbook header + positions + fallback tiers.


        Visibility: admins see all; non-admins see playbooks they authored

        or built-in playbooks (``created_by IS NULL``). 404 (not 403) on

        unauthorized access — mirrors the playbook-execute handler.'
      operationId: get_playbook_api_v1_playbooks__playbook_id__get
      parameters:
      - in: path
        name: playbook_id
        required: true
        schema:
          format: uuid
          title: Playbook Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Playbook'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Get a playbook with its full position list.
      tags:
      - playbooks
    patch:
      description: 'Patch the playbook header; atomically replace positions if supplied
        — M3-A6 Phase 2.


        Authorization: built-in playbooks (``created_by IS NULL``) are

        immutable through this endpoint by anyone, including admins —

        operators ship built-ins via migration and the admin-fork-then-edit

        path is the canonical mutation route. Non-built-in playbooks can be

        edited by their author OR by an admin. Cross-user / soft-deleted

        cases return 404 (no information leakage), matching the read paths.


        Position replacement is atomic: when ``body.positions`` is supplied,

        every existing position row for this playbook is deleted and the

        body''s list is inserted in a single transaction. To leave positions

        alone, omit the field (``None``); to clear all positions, send an

        empty list ``[]``.'
      operationId: update_playbook_api_v1_playbooks__playbook_id__patch
      parameters:
      - in: path
        name: playbook_id
        required: true
        schema:
          format: uuid
          title: Playbook Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaybookUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Playbook'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update a playbook's header and (optionally) replace its positions.
      tags:
      - playbooks
  /api/v1/playbooks/{playbook_id}/execute:
    post:
      description: 'Create a :class:`PlaybookExecution` row and schedule the workflow.


        Returns 202 immediately with the row at status ``''pending''``. The

        workflow promotes it to ``''running''`` shortly after the response

        is sent, and to ``''completed''`` / ``''error''`` once the four-node

        graph completes.'
      operationId: execute_playbook_api_v1_playbooks__playbook_id__execute_post
      parameters:
      - in: path
        name: playbook_id
        required: true
        schema:
          format: uuid
          title: Playbook Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaybookExecutionCreate'
        required: true
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaybookExecution'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Kick off a playbook execution against a target document.
      tags:
      - playbooks
  /api/v1/projects:
    get:
      description: Returns the caller's active projects by default. ``archived=true``
        returns archived projects only; ``archived=false`` is equivalent to omitting
        the parameter. Sandbox matters (``is_sandbox=true``) are excluded by default;
        pass ``include_sandbox=true`` to surface them alongside regular matters, or
        ``only_sandbox=true`` to return only sandboxes (Wave D.2 Task 2.3).
      operationId: list_projects_api_v1_projects_get
      parameters:
      - description: When true, return only archived projects.
        in: query
        name: archived
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          description: When true, return only archived projects.
          title: Archived
      - description: Include sandbox matters in results.
        in: query
        name: include_sandbox
        required: false
        schema:
          default: false
          description: Include sandbox matters in results.
          title: Include Sandbox
          type: boolean
      - description: Return only sandbox matters.
        in: query
        name: only_sandbox
        required: false
        schema:
          default: false
          description: Return only sandbox matters.
          title: Only Sandbox
          type: boolean
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ProjectResponse'
                title: Response List Projects Api V1 Projects Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the caller's projects
      tags:
      - projects
    post:
      description: Create a new project owned by the caller. ``slug`` is generated
        from ``name`` if omitted; collisions with the caller's existing active projects
        are resolved with a numeric suffix (``-2``, ``-3``, …). Returns the canonical
        ``Project`` shape.
      operationId: create_project_api_v1_projects_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectCreateRequest'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Create a project
      tags:
      - projects
  /api/v1/projects/sandbox/ensure:
    post:
      description: Idempotent find-or-create for the per-user *try-it sandbox* project.
        The sandbox is a system-managed matter (slug ``__sandbox__``) used to scope
        skill try-it conversations that should not count toward billable matter activity.
        First call returns 201 with a fresh row; subsequent calls return 200 with
        the same row. After the sandbox is soft-deleted via the normal DELETE endpoint,
        the next ensure call recreates it. Concurrent callers see the same row — the
        per-owner-active partial unique index on ``slug`` plus ``ON CONFLICT DO NOTHING``
        make this race-free.
      operationId: ensure_sandbox_api_v1_projects_sandbox_ensure_post
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Find or create the caller's try-it sandbox matter (Wave D.2)
      tags:
      - projects
  /api/v1/projects/{project_id}:
    delete:
      description: 'Sets ``archived_at`` on the row. Hard-delete is owned by D6. Idempotent:
        a second delete on an already-archived project returns 404.'
      operationId: delete_project_api_v1_projects__project_id__delete
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete a project
      tags:
      - projects
    get:
      operationId: get_project_api_v1_projects__project_id__get
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fetch a single project (with attachments)
      tags:
      - projects
    patch:
      operationId: update_project_api_v1_projects__project_id__patch
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectUpdateRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Partial update of a project
      tags:
      - projects
  /api/v1/projects/{project_id}/files:
    post:
      description: 'Body: ``{file_id}``. The caller must own both the project and
        the file (cross-user → 404 on either side). Re-attaching an already-attached
        file returns 409.'
      operationId: attach_file_api_v1_projects__project_id__files_post
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/app__api__projects__AttachFileRequest'
        required: true
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Attach a file to a project
      tags:
      - projects
  /api/v1/projects/{project_id}/files/{file_id}:
    delete:
      description: 'Idempotent: removing a file that is not attached returns 404.
        The file row itself is untouched (use DELETE /api/v1/files/{id} for the file).'
      operationId: detach_file_api_v1_projects__project_id__files__file_id__delete
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      - in: path
        name: file_id
        required: true
        schema:
          title: File Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Detach a file from a project
      tags:
      - projects
  /api/v1/projects/{project_id}/knowledge-bases:
    post:
      description: 'Body: ``{knowledge_base_id}``. The caller must own both the project
        and the KB (cross-user → 404 on either side). Idempotent — re-attaching an
        already-attached KB returns 200 with the current project state. Audit action:
        ``project.knowledge_base_attached``.'
      operationId: attach_knowledge_base_api_v1_projects__project_id__knowledge_bases_post
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachKnowledgeBaseRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Attach a knowledge base to a matter
      tags:
      - projects
  /api/v1/projects/{project_id}/knowledge-bases/{kb_id}:
    delete:
      description: 'Idempotent — detaching a KB that is not attached returns 204.
        The KB row itself is untouched (use ``DELETE /api/v1/knowledge-bases/{id}``
        for the KB). Audit action: ``project.knowledge_base_detached`` (only written
        when a join row is actually removed).'
      operationId: detach_knowledge_base_api_v1_projects__project_id__knowledge_bases__kb_id__delete
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      - in: path
        name: kb_id
        required: true
        schema:
          title: Kb Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Detach a knowledge base from a matter
      tags:
      - projects
  /api/v1/projects/{project_id}/skills:
    post:
      description: 'Body: ``{skill_name}``. The skill must exist in the in-memory
        registry (registry-wide; no per-user check). Re-attaching an already-attached
        skill returns 409.'
      operationId: attach_skill_api_v1_projects__project_id__skills_post
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachSkillRequest'
        required: true
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Attach a skill to a project
      tags:
      - projects
  /api/v1/projects/{project_id}/skills/{skill_name}:
    delete:
      operationId: detach_skill_api_v1_projects__project_id__skills__skill_name__delete
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          title: Project Id
          type: string
      - in: path
        name: skill_name
        required: true
        schema:
          title: Skill Name
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Detach a skill from a project
      tags:
      - projects
  /api/v1/research/capabilities:
    get:
      operationId: capabilities_api_v1_research_capabilities_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResearchCapabilities'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Capabilities
      tags:
      - research
  /api/v1/research/clusters/{cluster_id}:
    get:
      operationId: get_cluster_api_v1_research_clusters__cluster_id__get
      parameters:
      - in: path
        name: cluster_id
        required: true
        schema:
          title: Cluster Id
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClusterView'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Get Cluster
      tags:
      - research
  /api/v1/research/find-in-case:
    post:
      operationId: find_in_case_api_v1_research_find_in_case_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FindInCaseRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FindInCaseResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Find In Case
      tags:
      - research
  /api/v1/research/opinions/{opinion_id}:
    get:
      operationId: read_opinion_api_v1_research_opinions__opinion_id__get
      parameters:
      - in: path
        name: opinion_id
        required: true
        schema:
          title: Opinion Id
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OpinionText'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Read Opinion
      tags:
      - research
  /api/v1/research/search:
    post:
      operationId: search_api_v1_research_search_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Search
      tags:
      - research
  /api/v1/research/sources:
    get:
      description: 'List available content-authority sources from the registry.


        Returns one entry per registered source type, indicating whether it is

        enabled (configured in the gateway) and its safe metadata fields.

        Auth keys and cost fields are never included (P3 / ADR 0016).'
      operationId: list_sources_api_v1_research_sources_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SourcesResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List Sources
      tags:
      - research
  /api/v1/research/verify-citations:
    post:
      operationId: verify_citations_api_v1_research_verify_citations_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyCitationsRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerifyCitationsResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Verify Citations
      tags:
      - research
  /api/v1/saved-prompts:
    get:
      description: 'GET /api/v1/saved-prompts — all of the caller''s saved prompts.


        Sorted by ``updated_at DESC`` so the most recently touched prompt

        appears first; the ``(user_id, updated_at DESC)`` index covers

        this query.'
      operationId: list_prompts_api_v1_saved_prompts_get
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/SavedPromptResponse'
                title: Response List Prompts Api V1 Saved Prompts Get
                type: array
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: List the calling user's saved prompts (newest first)
      tags:
      - saved-prompts
    post:
      description: POST /api/v1/saved-prompts — create + return the new row.
      operationId: create_prompt_api_v1_saved_prompts_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SavedPromptCreate'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedPromptResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Create a new saved prompt for the calling user
      tags:
      - saved-prompts
  /api/v1/saved-prompts/{prompt_id}:
    delete:
      description: DELETE /api/v1/saved-prompts/{id} — owner-only delete.
      operationId: delete_prompt_api_v1_saved_prompts__prompt_id__delete
      parameters:
      - in: path
        name: prompt_id
        required: true
        schema:
          format: uuid
          title: Prompt Id
          type: string
      responses:
        '204':
          description: Successful Response
        '404':
          description: Saved prompt not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Delete a saved prompt (owner-only)
      tags:
      - saved-prompts
    get:
      description: 'GET /api/v1/saved-prompts/{id} — single prompt by id.


        Returns 404 if the id doesn''t exist OR belongs to another user;

        see :func:`_load_owned`.'
      operationId: get_prompt_api_v1_saved_prompts__prompt_id__get
      parameters:
      - in: path
        name: prompt_id
        required: true
        schema:
          format: uuid
          title: Prompt Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedPromptResponse'
          description: Successful Response
        '404':
          description: Saved prompt not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fetch a single saved prompt (owner-only)
      tags:
      - saved-prompts
    patch:
      description: PATCH /api/v1/saved-prompts/{id} — partial update.
      operationId: update_prompt_api_v1_saved_prompts__prompt_id__patch
      parameters:
      - in: path
        name: prompt_id
        required: true
        schema:
          format: uuid
          title: Prompt Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SavedPromptUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedPromptResponse'
          description: Successful Response
        '404':
          description: Saved prompt not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update a saved prompt (partial; owner-only)
      tags:
      - saved-prompts
  /api/v1/skills:
    get:
      description: "Return summaries for skills visible to the caller.\n\nMerging\
        \ semantics (per ADR 0012):\n\n* ``scope=all`` (default) — caller's user-scope\
        \ rows first, then\n  built-ins. Dedup on slug: a user shadow hides the built-in\
        \ of\n  the same slug from this listing.\n* ``scope=user`` — caller's user-scope\
        \ rows only.\n* ``scope=builtin`` — filesystem built-ins only (no shadow merge).\n\
        * ``scope=team`` — empty list until D8.1 (team scope deferred).\n\nThe ``tag``\
        \ filter applies to both layers; a built-in matches if\nits ``lq_ai.tags``\
        \ contains the tag, a user shadow matches if its\n``tags`` column contains\
        \ it."
      operationId: list_skills_api_v1_skills_get
      parameters:
      - in: query
        name: scope
        required: false
        schema:
          default: all
          pattern: ^(builtin|user|team|all)$
          title: Scope
          type: string
      - in: query
        name: tag
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Tag
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List Skills
      tags:
      - skills
  /api/v1/skills/autocomplete:
    get:
      description: "Typeahead for the chat composer's skill picker.\n\nTwo modes:\n\
        \n* **Empty ``q``** — return the caller's most-recently-used skills\n  (by\
        \ ``messages.applied_skills``), capped at ``limit``. If the user\n  has fewer\
        \ recents than ``limit``, fill the tail with the\n  alphabetical merged catalog\
        \ (skipping anything already in the\n  recents block). Brand-new users with\
        \ no chat activity see the\n  alphabetical fallback exclusively.\n* **Non-empty\
        \ ``q``** — rank the merged catalog by the three signals\n  in ``_rank_autocomplete_match``\
        \ (slash-alias prefix > slug prefix\n  > title substring) and return up to\
        \ ``limit`` non-zero matches.\n\nShadowing: a user-scope row at the same slug\
        \ as a built-in hides the\nbuilt-in from results (ADR 0012). The ``scope``\
        \ field on each result\nlets the UI render the right affordance.\n\nBounds:\
        \ ``limit`` is hard-clamped via FastAPI's ``Query(ge=1, le=25)``\n— a request\
        \ for ``limit=50`` returns 422 rather than silently\ntruncating, which keeps\
        \ the contract explicit for the frontend."
      operationId: autocomplete_skills_api_v1_skills_autocomplete_get
      parameters:
      - description: Substring to match. Empty = recents.
        in: query
        name: q
        required: false
        schema:
          default: ''
          description: Substring to match. Empty = recents.
          title: Q
          type: string
      - in: query
        name: limit
        required: false
        schema:
          default: 10
          maximum: 25
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkillAutocompleteResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Autocomplete skills by /alias / slug / title (Wave D.2)
      tags:
      - skills
  /api/v1/skills/{skill_name}:
    get:
      description: "Return full skill detail using the D8.1b resolution stack.\n\n\
        Resolution order (per ADR 0012 + D8.1b):\n\n1. The caller's non-archived user-scope\
        \ shadow at this slug.\n2. The newest non-archived team-scope shadow at this\
        \ slug owned\n   by any team the caller is a member of.\n3. The filesystem-canonical\
        \ built-in.\n\n404 if none of the three match. Member-of-team is sufficient\
        \ for\nread; mutate rights stay admin-only at the management endpoints."
      operationId: get_skill_api_v1_skills__skill_name__get
      parameters:
      - in: path
        name: skill_name
        required: true
        schema:
          title: Skill Name
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Get Skill
      tags:
      - skills
  /api/v1/skills/{skill_name}/contents:
    get:
      description: 'Return the full skill payload — SKILL.md + reference + example
        files.


        Same shape as ``GET /skills/{skill_name}`` (the user-facing read

        endpoint). PRD §3.4 names this endpoint explicitly as the contract

        the "view this skill" affordance + the skill inspector side panel

        target; exposing it under a distinct URL lets the frontend use URL

        semantics ("/contents") to signal inspection intent, even though

        the response body is the same as the base GET. Applies the same

        D8.1b resolution stack (user > team > built-in).'
      operationId: get_skill_contents_api_v1_skills__skill_name__contents_get
      parameters:
      - in: path
        name: skill_name
        required: true
        schema:
          title: Skill Name
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Full skill contents for the skill inspector (PRD §3.4)
      tags:
      - skills
  /api/v1/skills/{skill_name}/fork:
    post:
      description: 'Fork a filesystem-canonical built-in into a new user-scope row.


        Mirrors the OpenAPI sketch''s contract (``new_name`` + ``scope`` in

        the body). D8 only implements ``scope=''user''``; ``scope=''team''`` is

        rejected with a 400 pointing at D8.1.


        If ``new_name`` matches an existing user-scope slug for this user,

        409. If ``new_name`` is omitted or equals the source slug, the new

        row becomes a same-slug shadow — which is the expected "I want my

        own version of nda-review" UX.


        Unknown body fields (e.g., ``{"name": "..."}`` instead of

        ``{"new_name": "..."}``) are rejected with a 422 rather than

        silently dropped — see :class:`SkillForkBody`.'
      operationId: fork_skill_api_v1_skills__skill_name__fork_post
      parameters:
      - in: path
        name: skill_name
        required: true
        schema:
          title: Skill Name
          type: string
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/SkillForkBody'
              - type: 'null'
              title: Payload
      responses:
        '201':
          content:
            application/json:
              schema: {}
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fork Skill
      tags:
      - skills
  /api/v1/skills/{skill_name}/inputs:
    get:
      description: 'Return the skill''s declared inputs (required + optional).


        The PRD §3.4 skill-input-form pattern: skills declare inputs in

        their frontmatter so the UI can render a structured form rather

        than letting the model ask for missing context conversationally.

        Used by Enhance Prompt''s "missing required input" surface and by

        any skill author who wants their skill to feel form-driven.


        Resolution mirrors ``GET /skills/{name}``: user > team > built-in.

        For user/team-scope rows the inputs live in ``frontmatter_extra``

        (under either the top-level ``inputs`` key or ``lq_ai.inputs``);

        for built-ins they ride the parsed SkillFrontmatter. Returns an

        empty (name-only) shape when the skill declares no inputs.'
      operationId: get_skill_inputs_api_v1_skills__skill_name__inputs_get
      parameters:
      - in: path
        name: skill_name
        required: true
        schema:
          title: Skill Name
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkillInputs'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Declared inputs (form schema) for the skill-input-form pattern
      tags:
      - skills
  /api/v1/tabular/execute:
    post:
      description: 'Kick off a tabular execution against the supplied document corpus.


        Authorization: the caller must own every document in

        ``document_ids``. Cross-user or non-existent ids collapse into a

        single 404 (no information leakage), matching the M3-A6 Easy

        Playbook posture.


        Creates a :class:`TabularExecution` row at ``status=''pending''``

        and enqueues the ARQ worker job on the shared playbook queue

        (per Decision C-3). Returns 202 immediately with the row; the

        result-view polls

        ``GET /api/v1/tabular/executions/{id}`` until status reaches

        a terminal state.


        Per the M3-C2 quality bar, "execution completed" does NOT mean

        every cell is correct — the result-view''s per-cell citation

        drawer is where the user-attorney validates extractions before

        relying on them.'
      operationId: create_tabular_execution_api_v1_tabular_execute_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TabularExecutionCreate'
        required: true
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TabularExecutionResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Start a tabular execution.
      tags:
      - tabular
  /api/v1/tabular/executions:
    get:
      description: 'Return the caller''s tabular executions, recent-first, soft-deleted
        excluded.


        Admins see all executions; non-admins see only their own

        (matches the Easy Playbook list posture).'
      operationId: list_tabular_executions_api_v1_tabular_executions_get
      parameters:
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TabularExecutionSummary'
                title: Response List Tabular Executions Api V1 Tabular Executions
                  Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the caller's tabular executions.
      tags:
      - tabular
  /api/v1/tabular/executions/{execution_id}:
    delete:
      description: Soft-delete (sets ``deleted_at``). Already-deleted rows return
        404.
      operationId: delete_tabular_execution_api_v1_tabular_executions__execution_id__delete
      parameters:
      - in: path
        name: execution_id
        required: true
        schema:
          format: uuid
          title: Execution Id
          type: string
      responses:
        '204':
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete a tabular execution.
      tags:
      - tabular
    get:
      description: 'Return one tabular execution — full state + grid results.


        Authorization: caller must be the row''s ``user_id`` OR an admin.

        Cross-user / missing / soft-deleted rows collapse into 404.'
      operationId: get_tabular_execution_api_v1_tabular_executions__execution_id__get
      parameters:
      - in: path
        name: execution_id
        required: true
        schema:
          format: uuid
          title: Execution Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TabularExecutionResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Get a tabular execution by id.
      tags:
      - tabular
  /api/v1/tabular/executions/{execution_id}/cancel:
    post:
      description: 'Set status to ``cancelled``.


        The worker''s per-cell loop checks the row status at each cell

        boundary; on cancellation it stops accepting new cells and the

        aggregate node writes whatever partial results landed. Already-

        terminal rows (``completed`` / ``failed`` / ``cancelled``) are

        a 409 — operators wanting a clean re-run start a new execution

        via ``POST /tabular/execute``.'
      operationId: cancel_tabular_execution_api_v1_tabular_executions__execution_id__cancel_post
      parameters:
      - in: path
        name: execution_id
        required: true
        schema:
          format: uuid
          title: Execution Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TabularExecutionResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Cancel a pending or running tabular execution.
      tags:
      - tabular
  /api/v1/tabular/executions/{execution_id}/export:
    get:
      description: "Stream the tabular grid in XLSX or CSV.\n\nBoth formats include\
        \ the document column (leftmost) plus one column\nper declared column spec,\
        \ in spec order. Cells with\n``confidence='failed'`` render as ``\"(failed)\"\
        `` rather than an\nempty string so operators can spot the gap without cross-referencing\n\
        the source execution.\n\nCitation surfacing differs by format:\n\n* **XLSX**\
        \ — each grid cell with at least one citation carries an\n  openpyxl ``Comment``\
        \ listing the citation IDs and confidences.\n  Operators can hover any cell\
        \ in Excel / Numbers / Google Sheets\n  to see the source. (Up to 5 citations\
        \ per comment; the cell\n  retains the full count.)\n* **CSV** — a trailing\
        \ ``citation_links`` column per row carries a\n  semicolon-separated list\
        \ of ``\"<column_name>:<citation_id>\"``\n  pairs across every cell in the\
        \ row. Empty when no cell in the\n  row had citations.\n\nThe execution must\
        \ be in ``status='completed'``. Pending / running\n/ cancelled / failed rows\
        \ return 409 — partial grids would mislead\ndownstream consumers. (DE-XXX\
        \ candidate: a separate\n``allow_partial=true`` query param to export partial\
        \ grids with a\nvisible watermark; deferred until an operator surfaces the\
        \ need.)"
      operationId: export_tabular_execution_api_v1_tabular_executions__execution_id__export_get
      parameters:
      - in: path
        name: execution_id
        required: true
        schema:
          format: uuid
          title: Execution Id
          type: string
      - description: Export format. `xlsx` carries citations as cell comments; `csv`
          flattens citations into a `citation_links` column.
        in: query
        name: format
        required: false
        schema:
          default: xlsx
          description: Export format. `xlsx` carries citations as cell comments; `csv`
            flattens citations into a `citation_links` column.
          enum:
          - xlsx
          - csv
          title: Format
          type: string
      responses:
        '200':
          content:
            application/json:
              schema: {}
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet: {}
            text/csv: {}
          description: Streaming export — Content-Type matches format.
        '409':
          description: Execution not in terminal status — cannot export.
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Export a completed tabular execution as XLSX or CSV.
      tags:
      - tabular
  /api/v1/tabular/preview-cost:
    post:
      description: 'Synchronous cost preview — no execution row is created.


        The UI calls this before showing the confirmation modal so the

        operator sees the cell-count + estimated cost + per-tier

        breakdown (Decision C-5). The estimator''s rolling average

        (M2-E2 pattern) caches per ~5 minutes in-process; rapid re-previews

        during column-spec editing don''t thrash the DB.


        Ensemble columns preview higher (Donna #6): the estimator adds one

        ensemble-pass cost (N judge calls) per ensemble cell. We fetch the

        gateway''s resolved ensemble config so the premium reflects the

        deployment''s actual judge models + default; the config is

        process-cached and ``get_citation_engine_ensemble_config`` already

        returns ``None`` on any gateway error / unreachable, so an

        unconfigured or down gateway simply yields no premium (graceful

        degrade — the preview stays a non-fatal pre-flight).


        Authorization is the router-level ``get_active_user`` gate — any

        authenticated caller may preview costs against their own document

        selection. The document-ownership check is enforced at execute

        time; preview doesn''t load the documents.'
      operationId: preview_tabular_cost_api_v1_tabular_preview_cost_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TabularPreviewCostRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TabularPreviewCostResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Preview the cost of a proposed tabular execution.
      tags:
      - tabular
  /api/v1/teams:
    get:
      description: 'GET /api/v1/teams — caller''s teams with ``caller_role`` populated.


        Optional ``role`` filter (D8.1c) restricts to memberships of a

        specific role. The UI uses ``?role=admin`` for the team-scope

        skill creation picker so only teams the caller can mutate render.'
      operationId: list_my_teams_api_v1_teams_get
      parameters:
      - in: query
        name: role
        required: false
        schema:
          anyOf:
          - pattern: ^(admin|member)$
            type: string
          - type: 'null'
          title: Role
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TeamSummary'
                title: Response List My Teams Api V1 Teams Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List teams the caller belongs to (newest first)
      tags:
      - teams
  /api/v1/teams/{team_id}:
    get:
      operationId: get_my_team_api_v1_teams__team_id__get
      parameters:
      - in: path
        name: team_id
        required: true
        schema:
          format: uuid
          title: Team Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamResponse'
          description: Successful Response
        '404':
          description: Team does not exist OR you are not a member
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fetch a team the caller belongs to (with roster)
      tags:
      - teams
  /api/v1/user-skills:
    get:
      description: "GET /api/v1/user-skills — non-archived rows the caller can edit.\n\
        \nScope filter (D8.1c — defaults to ``user`` for back-compat):\n\n* ``user``\
        \ — the caller's user-scope rows only (the D8 behavior).\n* ``team`` — team-scope\
        \ rows from any team where the caller is an\n  admin. Members can read team\
        \ skills in the chat picker but can't\n  mutate, so the management list returns\
        \ admin-only rows.\n* ``all`` — both, sorted together by ``updated_at DESC,\
        \ id DESC``.\n\nSort: ``updated_at DESC`` then ``id DESC`` for deterministic\
        \ tie-\nbreaking (mirrors the saved_prompts convention)."
      operationId: list_user_skills_api_v1_user_skills_get
      parameters:
      - in: query
        name: scope
        required: false
        schema:
          default: user
          pattern: ^(user|team|all)$
          title: Scope
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UserSkillResponse'
                title: Response List User Skills Api V1 User Skills Get
                type: array
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: List the caller's editable skills (user-scope by default; team-scope
        via ?scope)
      tags:
      - user-skills
    post:
      description: "POST /api/v1/user-skills — create a fresh user- or team-scope\
        \ skill.\n\nScope branches:\n\n* ``scope='user'`` (default) — owned by the\
        \ caller. Slug collision with\n  a filesystem built-in is **allowed** (the\
        \ shadow case per ADR 0012).\n  Slug collision with the caller's own non-archived\
        \ user-scope skills\n  returns 409.\n* ``scope='team'`` — owned by ``owner_team_id``.\
        \ Caller must be a\n  team-admin member of that team or 404 (id-probing-safe\
        \ — same\n  privacy posture as cross-user reads). Slug collision within the\n\
        \  team's non-archived rows returns 409. Slug collision with a\n  built-in\
        \ OR with any user-scope row is permitted (shadowing semantics\n  cascade\
        \ per D8.1b resolver: user > team > built-in).\n\n422 fires when ``scope``\
        \ and ``owner_team_id`` are inconsistent\n(team scope missing the team id,\
        \ user scope carrying a team id)."
      operationId: create_user_skill_api_v1_user_skills_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserSkillCreate'
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSkillResponse'
          description: Successful Response
        '404':
          description: Team referenced by owner_team_id not found
        '409':
          description: Slug already exists in this scope
        '422':
          description: scope / owner_team_id combination invalid
      security:
      - OAuth2PasswordBearer: []
      summary: Create a new user- or team-scope skill
      tags:
      - user-skills
  /api/v1/user-skills/{skill_id}:
    delete:
      description: 'DELETE /api/v1/user-skills/{id} — soft-delete via ``archived_at``.


        Archived rows free the slug for a new user skill at the same slug.

        The audit log preserves the row''s identity so deletion is traceable

        even after the slug has been reused.'
      operationId: delete_user_skill_api_v1_user_skills__skill_id__delete
      parameters:
      - in: path
        name: skill_id
        required: true
        schema:
          format: uuid
          title: Skill Id
          type: string
      responses:
        '204':
          description: Successful Response
        '404':
          description: User skill not found
        '410':
          description: User skill is already archived
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Soft-delete a user-scope skill (owner-only)
      tags:
      - user-skills
    get:
      description: 'Owner-only for user-scope; team-admin-only for team-scope rows.


        Non-admin team members read team skills through the merged picker

        (``GET /api/v1/skills/{slug}``), not through this management

        endpoint — same posture as filesystem built-ins.'
      operationId: get_user_skill_api_v1_user_skills__skill_id__get
      parameters:
      - in: path
        name: skill_id
        required: true
        schema:
          format: uuid
          title: Skill Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSkillResponse'
          description: Successful Response
        '404':
          description: User skill not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Fetch a single user/team-scope skill (owner or team-admin)
      tags:
      - user-skills
    patch:
      description: 'PATCH /api/v1/user-skills/{id} — partial update.


        The audit row''s ``details.version_before`` / ``version_after`` make

        the *fact* of edits forensically traceable per ADR 0012 §5; the

        *content* of prior versions is not preserved (no history table in

        D8). An idempotent PATCH with no actual changes returns the current

        row without writing an audit row.'
      operationId: update_user_skill_api_v1_user_skills__skill_id__patch
      parameters:
      - in: path
        name: skill_id
        required: true
        schema:
          format: uuid
          title: Skill Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserSkillUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSkillResponse'
          description: Successful Response
        '404':
          description: User skill not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update a user-scope skill (partial; owner-only)
      tags:
      - user-skills
  /api/v1/user-skills/{skill_id}/versions:
    get:
      description: 'GET /api/v1/user-skills/{id}/versions — most-recent-first audit
        feed.


        Returns the ``audit_log`` rows for ``resource_type=''user_skill''``

        keyed by this skill id. ``user_skill.created`` lands last in the

        list because the result is ordered ``timestamp DESC`` — the timeline

        UI renders top-down, newest at the top.


        Access posture mirrors the mutable surface: user-scope rows are

        visible only to the owner; team-scope rows only to team-admins.

        Non-admin team members read the merged skill via ``GET /skills/{slug}``

        but the audit feed is part of the management surface, not the

        consumption surface, so the read gate is the mutate gate. 404 for

        "no such id" and "exists but not yours" alike (id-probing-safe;

        matches the load-mutable convention elsewhere in this router).


        ``include_archived=True`` because a freshly-archived skill''s

        timeline still needs to render for the management UI; ``_load_mutable``

        handles the auth check uniformly across active and archived rows.'
      operationId: list_user_skill_versions_api_v1_user_skills__skill_id__versions_get
      parameters:
      - in: path
        name: skill_id
        required: true
        schema:
          format: uuid
          title: Skill Id
          type: string
      - in: query
        name: limit
        required: false
        schema:
          default: 50
          maximum: 200
          minimum: 1
          title: Limit
          type: integer
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSkillVersionsResponse'
          description: Successful Response
        '404':
          description: User skill not found
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Audit-log view of edits for this user-skill (Wave D.2)
      tags:
      - user-skills
  /api/v1/users/me:
    get:
      description: 'GET /api/v1/users/me — return the calling user''s public profile.


        Reachable while `must_change_password=true` so the client can read the

        flag and route to the change-password flow. Other authenticated

        endpoints are gated until the user clears it.'
      operationId: get_me_api_v1_users_me_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPublic'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Get current user
      tags:
      - users
    patch:
      description: 'PATCH /api/v1/users/me — edit the caller''s own profile.


        Caller-scoped: mutates only the authenticated user''s own row; no user

        id is ever accepted from the path or body. Currently updates

        ``display_name`` only (trimmed, non-empty, length-guarded by

        ``UserProfileUpdate``). Email self-service editing is intentionally

        out of scope (re-verification / MFA / uniqueness).


        Writes a ``user.profile_updated`` audit row enumerating the changed

        fields, mirroring ``patch_me_preferences``. Returns the updated

        ``UserPublic`` — the same shape ``GET /users/me`` returns.'
      operationId: patch_me_api_v1_users_me_patch
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserProfileUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPublic'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update the calling user's own profile (display_name)
      tags:
      - users
  /api/v1/users/me/delete:
    post:
      description: "POST /api/v1/users/me/delete — soft-schedule with a grace period.\n\
        \nIdempotent: if the user already has a pending deletion the existing\nschedule\
        \ is returned. This prevents accidental \"extension\" of the\ngrace period\
        \ via repeated calls.\n\nSide-effects:\n\n1. ``deletion_scheduled_at = now()\
        \ + gdpr_grace_period_days``.\n2. All of the user's active ``user_sessions``\
        \ are revoked. The user\n   can still log in during the grace period (to call\
        \ cancel) — the\n   gate is on ``deleted_at``, not ``deletion_scheduled_at``.\n\
        3. An ``audit_log`` row records the request.\n\nLogin during the grace period\
        \ is preserved so the user retains the\nability to cancel; hard delete itself\
        \ runs in the daily worker\ncron."
      operationId: delete_me_api_v1_users_me_delete_post
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteScheduledResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Schedule account deletion (GDPR Article 17)
      tags:
      - users
  /api/v1/users/me/delete/cancel:
    post:
      description: 'POST /api/v1/users/me/delete/cancel — clear a pending schedule.


        Returns 400 if there is no pending deletion. The grace-period

        window itself is the only valid cancel surface — once the worker

        has hard-deleted the user, there is no row to recover.'
      operationId: cancel_delete_me_api_v1_users_me_delete_cancel_post
      responses:
        '204':
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Cancel a pending account deletion
      tags:
      - users
  /api/v1/users/me/export:
    post:
      description: 'POST /api/v1/users/me/export — async; returns a job id for polling.


        Inserts a `user_export_jobs` row with status=''queued'', enqueues the

        arq worker job, and returns 202. The worker runs the ZIP build +

        MinIO upload out-of-band.


        Re-calling while a queued/processing job exists is allowed —

        operators may want a fresh export after deleting some content.

        The two jobs run independently.'
      operationId: export_me_api_v1_users_me_export_post
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: Queue a per-user data export
      tags:
      - users
  /api/v1/users/me/export/{job_id}:
    get:
      description: 'GET /api/v1/users/me/export/{job_id}.


        Returns 404 if the job doesn''t exist or belongs to another user

        (don''t leak existence cross-user). On a completed job, returns a

        presigned download URL valid for 24h. On a job whose bundle has

        expired (storage_key cleared by the GC cron), the response carries

        no URL and the status remains ''completed'' for audit clarity — the

        caller can re-request export to get a fresh bundle.'
      operationId: get_export_job_api_v1_users_me_export__job_id__get
      parameters:
      - in: path
        name: job_id
        required: true
        schema:
          title: Job Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExportJobResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Poll a user-export job
      tags:
      - users
  /api/v1/users/me/preferences:
    get:
      description: 'GET /api/v1/users/me/preferences — preferences slice of the profile.


        Returns the same data ``GET /users/me`` does for the preferences

        fields; this dedicated endpoint exists so frontends can subscribe

        to preferences changes without re-fetching the whole user object.'
      operationId: get_me_preferences_api_v1_users_me_preferences_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPreferencesResponse'
          description: Successful Response
      security:
      - OAuth2PasswordBearer: []
      summary: 'Read the calling user''s preferences (Wave A: reasoning_visibility)'
      tags:
      - users
    patch:
      description: 'PATCH /api/v1/users/me/preferences — partial update.


        Only the fields the user actually supplies move. An idempotent

        PATCH (same value re-supplied) returns 200 without writing an

        audit row, matching the user_skills / saved_prompts pattern.

        Writes ``user.preferences_updated`` audit rows when anything

        actually changes, with details enumerating the changed fields and

        before/after values (so a privacy audit can reconstruct the

        preference history).'
      operationId: patch_me_preferences_api_v1_users_me_preferences_patch
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserPreferencesUpdate'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPreferencesResponse'
          description: Successful Response
        '422':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
          description: Validation Error
      security:
      - OAuth2PasswordBearer: []
      summary: Update the calling user's preferences (partial)
      tags:
      - users
  /api/v1/word-addin/version:
    get:
      description: Unauthenticated endpoint the task pane consults on mount. Returns
        the deployment's version + the add-in version range this deployment is compatible
        with. The task pane compares its bundled version (baked in by webpack at build
        time) to the range; out-of-range bundles surface an 'Update needed' overlay
        rather than getting stuck against a breaking-change API call.
      operationId: get_version_api_v1_word_addin_version_get
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WordAddinVersionResponse'
          description: Successful Response
      summary: Version handshake for the Word add-in task pane (M3-B8).
      tags:
      - word-addin
  /health:
    get:
      description: 'Liveness probe — returns 200 as soon as the process is serving
        requests.


        Per K8s liveness convention: this answers "is the process alive?" and is

        independent of whether downstream dependencies are reachable. Used by

        docker-compose healthchecks and orchestration platforms.'
      operationId: health_health_get
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
      summary: Health
      tags:
      - meta
  /ready:
    get:
      description: 'Readiness probe — returns 200 when DB + Redis + MinIO + gateway
        are reachable.


        Per K8s readiness convention: this answers "can I serve user requests?"

        Returns 200 with per-dependency `ok: true` when everything is up.

        Returns 503 with per-dependency status (and `ok: false` for failed

        deps) when any dependency is unreachable. Liveness (`/health`) stays

        200 in either case so orchestrators don''t kill the pod for a transient

        dependency outage.'
      operationId: ready_ready_get
      responses:
        '200':
          content:
            application/json:
              schema: {}
          description: Successful Response
      summary: Ready
      tags:
      - meta
